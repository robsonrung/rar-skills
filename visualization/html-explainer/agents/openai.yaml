# Codex agent interface metadata; consumed by the Codex host, not read at runtime by the model.
interface:
  display_name: "HTML Explainer"
  short_description: "Drill-down HTML explainer for a codebase subsystem"
  default_prompt: "Use $html-explainer to build a self-contained drill-down HTML page explaining how this subsystem works, grounded in real code."
policy:
  allow_implicit_invocation: false
