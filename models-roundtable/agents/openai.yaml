# Codex-host interface/registration config for this skill (display metadata and
# default prompt shown in the Codex app). Not meant to be read at runtime.
interface:
  display_name: "Models Round Table"
  short_description: "Six models answer blind; judges + orchestrator reconcile"
  default_prompt: "Use $models-roundtable to answer this with the default six-seat panel including GLM 5.2 blind, then reconcile disagreements with a two-judge panel and a final call."
