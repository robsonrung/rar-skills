# Codex agent interface metadata; consumed by the Codex host, not read at runtime by the model.
interface:
  display_name: "Fable Mindset"
  short_description: "Apply the five moment working posture for engineering turns"
  default_prompt: "Use $fable-mindset to apply the working posture across the five moments of this turn — intake, diagnosis, decision, implementation, and reporting — applying only the moments that match the situation."
policy:
  allow_implicit_invocation: false
