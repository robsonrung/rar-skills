# Skill Audit Report — 2026-06-09

One analysis agent per skill. Goals: maximum simplicity and effectiveness, context-window economy, zero knowledge loss.

Verdicts: 2 good-as-is, 41 minor-improvements, 0 major-improvements (of 43 skills).

## architect-lens — minor-improvements
Footprint: SKILL.md 76 lines / ~900 words / ~1.2k tokens; 4 reference files 158 lines / ~1,640 words / ~2.2k tokens total, all loaded on demand

architect-lens is a well-built, lean skill that needs only small deduplication touch-ups. Progressive disclosure is done correctly: SKILL.md carries just enough of each lens to handle common cases standalone, while the four small reference files (taxonomy tables, -ilities checklist, ADR template, fitness-function map) are each pointed to with explicit "see X when Y" guidance and are never orphaned. The main flaw is modest duplication: the coupling-lens review workflow in SKILL.md (lines 61-65) is nearly restated verbatim in references/connascence.md ("How to use during review", lines 31-38), and the effort-proportionality rule appears twice in SKILL.md (lines 45 and 75). These cost only ~200 tokens, so the verdict is borderline good-as-is.

**Strengths:**
- Excellent frontmatter description: covers both lenses, lists concrete trigger phrasings ("what's the trade-off", "is this too coupled", "should I extract/split/merge"), and includes a negative trigger ("Do not use for pure bug-fixing or formatting") to prevent false positives.
- Correct progressive disclosure: SKILL.md keeps short inline summaries (common -ilities at line 25, connascence axes at lines 53-59, ADR significance test at line 41) so simple cases need no file reads, and explicitly instructs at line 76 to load references only on demand. Every reference file has clear when-to-read guidance; none are dead.
- Strong LLM-leverage patterns: numbered workflows for each lens, remedy tables in connascence.md mapping type -> smell -> fix, a characteristic -> check table in fitness-functions.md, a copy-paste ADR template, and explicit stop conditions ("Don't over-refactor", "Don't over-gate", "Stop early").
- Effort-proportionality and anti-gold-plating guardrails baked in (line 45, line 75, fitness-functions.md line 30), preventing the classic failure of the skill generating ADRs and CI gates for trivial decisions.
- Tiny total footprint (~3.4k tokens across everything) with no scripts/agents needed — appropriate, since both lenses are pure judgment work where model-driven reasoning is the right choice.

**Recommendations:**
- [low] **Deduplicate the coupling review workflow between SKILL.md and connascence.md** — SKILL.md lines 61-65 ("Review workflow: 1. Identify... 2. Judge by strength x locality x degree... 4. Don't over-refactor") and references/connascence.md lines 31-38 ("How to use during review: 1. Find... 2. Score strength x locality x degree... 4. Stop early") are near-identical 4-step lists; the strength/locality/degree axes and the distance rule are also stated in both files (SKILL.md lines 55-59 vs connascence.md lines 5-10). Since the reference is always loaded *in addition to* SKILL.md, trim connascence.md to just its unique content (the two taxonomy tables, the remedy column, and the cohesion companion check) and replace its "How to use during review" section with a one-line pointer back to the workflow in SKILL.md. Keep the axes definitions only in connascence.md's intro if needed for table context, or only in SKILL.md.
  - Knowledge preservation: Pure deduplication: the workflow steps survive verbatim in SKILL.md (the canonical copy, needed for standalone simple cases); only the redundant restatement in the reference is removed. The one unique nuance in the reference version ("Most remedies move coupling up the table toward Name and closer toward locality", connascence.md line 37) should be kept by folding it into the table intro or SKILL.md step 3.
- [low] **Merge the two effort-proportionality statements in SKILL.md** — Line 45 ("Keep it proportional: a small decision gets a two-line trade-off note inline; a significant one gets the full pass + an ADR") and line 75 ("Match effort to stakes. Don't generate an ADR or a fitness function for a one-line decision") say the same thing. Keep a single combined sentence in the Output style section (it applies to both lenses there), e.g. "Match effort to stakes: a small decision gets a two-line trade-off note inline; only significant ones get the full pass, an ADR, or a fitness function." Delete line 45.
  - Knowledge preservation: All three unique fragments (inline two-line note for small decisions, full pass + ADR for significant ones, no fitness functions for trivia) are merged into the single retained sentence; nothing is dropped, only the repetition.
- [low] **Optionally trim the duplicated remedy examples in SKILL.md coupling step 3** — SKILL.md line 64 lists three concrete remedies (magic value -> named constant, positional args -> named params, duplicated calc -> single source) that are rows in the connascence.md remedy table (lines 18-20). This duplication is arguably intentional (lets the agent fix trivial cases without a file read), so only apply this if you want maximum leanness: shorten to one example plus the existing pointer "the reference file maps each type to its remedy". If kept as-is, that is also defensible — flagging for completeness, not insisting.
  - Knowledge preservation: The full type->remedy mapping remains intact in references/connascence.md's tables; SKILL.md keeps one worked example plus the explicit pointer, so no remedy knowledge is lost, only the inline preview is shortened.

## architecture-styles — minor-improvements
Footprint: SKILL.md 97 lines / ~6.6 KB (~1.7k tokens); 6 reference files totaling 263 lines / ~14.6 KB (~3.7k tokens), loaded on demand one at a time (~450-750 tokens each)

This is a near-exemplary small skill: a lean 97-line SKILL.md holding only the decision workflow, selection matrix, and output contract, with per-style detail correctly pushed into six tiny reference files behind explicit "read the one you're leaning toward" guidance. Triggering, disambiguation from sibling skills (model-lens, architect-lens, design-patterns, ddd-tactical — all verified to exist in the repo), and the structured output template are all strong. Only two minor defects: a stray closing code fence at the end of references/traps.md, and traps.md not being wired into the numbered selection workflow even though its content (8 fallacies, three latencies) is positioned as a mandatory pre-commit check for distributed picks.

**Strengths:**
- Excellent progressive disclosure: SKILL.md carries only the workflow + matrix (~1.7k tokens); per-style topology/when-not-to detail lives in references/, with explicit load guidance ('read the one you're leaning toward', SKILL.md lines 35-36 and 70-79).
- Strong frontmatter description: covers the natural user phrasings ('should this be microservices', 'monolith vs distributed', 'which architecture style fits') and explicitly disambiguates from four sibling skills to prevent false-positive triggering — all four siblings verified present in the repo.
- Deterministic-where-it-should-be, judgment-where-it-should-be: a 5-step numbered workflow with concrete linguistic 'tells' (lines 23-27), a hard matrix (Appendix A) for scoring, and model judgment reserved for scoping and trade-off selection.
- Structured output contract (lines 85-94) gives a tight, reproducible answer shape with a runner-up and a 'watch out' field that forces the when-not-to check.
- Explicit anti-over-engineering stop condition (lines 96-97: 'layered, don't overthink it' and stop) — a rare and valuable guardrail.
- Cross-file deduplication is done right: traps.md summarizes bounded-context and event-vs-message in one line each and points to microservices.md / event-driven.md for the full treatment, rather than duplicating.
- Each reference file ends with the style's full characteristics line, so a reference is self-sufficient when loaded without re-loading the SKILL.md matrix.

**Recommendations:**
- [low] **Remove stray closing code fence at end of references/traps.md** — references/traps.md line 50 is a dangling ``` with no opening fence (verified via byte dump: file ends `microkernel.\n```\n`). Some markdown renderers/parsers will treat everything after it oddly, and it signals an editing slip. Delete the single line.
  - Knowledge preservation: Pure formatting fix; no content removed — the line contains no information.
- [medium] **Wire traps.md into the numbered workflow** — SKILL.md step 4 (line 35-36) instructs reading the matching references/<style>.md, and the references list (line 77-79) describes traps.md as 'check against any pick', but the 5-step workflow never tells the agent when to actually read it. Since traps.md itself opens with 'Check any candidate style against these before committing' and contains the 8-fallacies/three-latencies material that step 5's risk statement depends on, add one clause to step 4 or 5, e.g.: 'If recommending a distributed style (event-driven, microservices, space-based), also read references/traps.md before finalizing.' This converts a passive catalog entry into a reachable branch.
  - Knowledge preservation: No content moved or deleted — adds a single pointer sentence so existing traps.md knowledge is reliably reached at the right workflow moment.

## architecture_session_review — minor-improvements
Footprint: SKILL.md 122 lines / ~905 words / ~1.2k tokens; 4 reference files 276 lines / ~1,530 words / ~2k tokens total; agents/openai.yaml 3 lines. Untracked .DS_Store present in directory (not in git).

A well-constructed, lean skill. SKILL.md is checklist-driven with a clear output contract, a tiered pass system (light/standard/deep), and proper progressive disclosure — all four reference files have explicit "read X when Y" guidance and none are orphaned. The main weaknesses are a frontmatter description that over-triggers (the catch-all "code work where trade offs matter" matches nearly any coding task), and three areas of duplication between SKILL.md and the reference files (ADR template, risk ratings, modularity/boundary questions) that cost always-loaded tokens without adding value.

**Strengths:**
- Strong progressive disclosure: SKILL.md References section (lines 115-122) gives per-file read-when conditions, and every reference file opens with its own 'use this reference when' line — no unreachable files.
- Numbered checklists throughout instead of prose walls; the Trade Off Frame (lines 37-50) and Output Contract (lines 104-113) are concrete structured-output patterns LLMs follow reliably.
- The Architectural Significance Filter (lines 24-35) and tiered light/standard/deep passes (lines 16-19) give a clear stop condition — 'if none apply, keep the work at normal code review depth' — preventing over-application.
- Correctly model-driven: this is a judgment workflow with no mechanical steps, so the absence of scripts is appropriate, not a gap.
- References are themselves tight (66-74 lines each) with genuinely distinct content per file (styles, ADR/risk/diagrams, team checklists, quality-attribute lens).

**Recommendations:**
- [high] **Tighten the frontmatter description to reduce false-positive triggering** — SKILL.md line 3 ends with 'and code work where trade offs matter' — this matches essentially every nontrivial coding task and will activate the skill far beyond its intent. The earlier clause 'may affect software architecture, module boundaries, service boundaries...' already covers the legitimate triggers. Drop the catch-all and keep the concrete activity list (architecture reviews, refactors, migrations, feature design, PR review, ADR drafting).
  - Knowledge preservation: No knowledge is deleted — the nuance of when trade-off thinking applies is already encoded in the body's Architectural Significance Filter (lines 24-35), which is the correct place for that judgment; the description only loses redundant trigger bait.
- [medium] **Deduplicate the ADR template between SKILL.md and adr_risk.md** — SKILL.md lines 68-78 (7-item ADR structure plus 'weight on why' guidance) is an exact subset of references/adr_risk.md lines 9-25, which carries the same template with richer per-field detail plus a decision-quality test. Keep only the Decision Record Trigger sentence (line 66) in SKILL.md and replace the template with 'Use the ADR template in references/adr_risk.md.' Saves ~12 always-loaded lines.
  - Knowledge preservation: The template already exists in fuller form in adr_risk.md; the 'why over how' and 'include rejected options' guidance in SKILL.md lines 78 duplicates adr_risk.md lines 20-23 (decision quality test) — verify both points survive in the reference, merging any phrasing that is unique.
- [medium] **Deduplicate risk ratings between the Risk Pass and adr_risk.md** — SKILL.md lines 80-88 and references/adr_risk.md lines 27-46 both define low/medium/high ratings and the 'unknown technology is high risk' rule. Keep the short operational version in SKILL.md (it is needed in standard/deep passes, which are common) but strip the rating definitions from one of the two — simplest is to keep SKILL.md's 3-line rating scale and have adr_risk.md's Risk Analysis section carry only the per-risk record fields (Area/Attribute/Impact/Likelihood/Evidence/Mitigation/Verification) plus the unknown-technology rule, which SKILL.md folds into its 'High' bullet already ('or unknown technology', line 86).
  - Knowledge preservation: Pure deduplication: each unique element (rating scale, unknown-tech rule, per-risk record fields, mitigation/owner requirement) keeps exactly one canonical home; nothing is removed from the union of the two sections.
- [medium] **Merge the overlapping Boundary Review (SKILL.md) and Modularity Review (architecture_lens.md)** — SKILL.md Boundary Review (lines 53-62) and references/architecture_lens.md Modularity Review (lines 23-35) cover the same four concepts — cohesion, coupling, connascence, locality — with different question phrasings. SKILL.md adds data flow, runtime flow, and fitness function; the reference adds abstraction balance, degree, and the 'weaker coupling across wider boundaries' rule. Either (a) fold the reference's two unique questions and closing rule into SKILL.md's list and delete the reference section, or (b) keep SKILL.md's 7-item list as the canonical session checklist and trim the reference to only its unique additions. Option (b) preserves SKILL.md's role as the always-available pass.
  - Knowledge preservation: Merge, not delete: the union of both lists (cohesion, coupling, connascence, locality, data flow, runtime flow, fitness function, abstraction balance, degree, locality rule) is preserved in a single canonical location with a pointer from the other.
- [low] **Rename 'Codex Solo Risk Storming' in adr_risk.md to be agent-neutral** — references/adr_risk.md line 48 hardcodes 'Codex' in a section heading, but the skill ships with both a generic SKILL.md and agents/openai.yaml, and may be loaded by non-Codex agents. Rename to 'Solo Risk Storming'. The section content (5-step solo procedure plus 'do not pretend solo analysis is consensus') is valuable and unchanged.
  - Knowledge preservation: Heading rename only; all five steps and the consensus caveat are untouched.
- [low] **Remove the stray .DS_Store from the skill directory** — /Users/robson/Development/rar-skills/architecture_session_review/.DS_Store exists on disk (untracked). Add .DS_Store to the repo .gitignore if not already present and delete the file so packaged/portable copies of the skill stay clean.
  - Knowledge preservation: macOS Finder metadata only; contains no skill knowledge.

## architecturehardparts — minor-improvements
Footprint: SKILL.md 82 lines / ~1.0k tokens (loaded on trigger); references/catalog.md 155 lines / ~1.7k tokens and references/adrtemplate.md 45 lines / ~0.4k tokens (both on-demand); scripts/newadr.py 101 lines (executed, not read). Total ~3.2k tokens, of which only ~1k is always-loaded.

This is a well-built skill that already follows progressive disclosure correctly: a lean ~1k-token SKILL.md with an imperative 9-step workflow, and the heavy decision catalog plus ADR template split into reference files with explicit "read X when Y" conditions (SKILL.md steps 5, lines 32-36). The frontmatter description triggers on the right vocabulary and includes a negative scope clause. The remaining issues are small: the ADR template text is maintained in two places (adrtemplate.md and newadr.py), the ADR path convention is stated three times, the catalog's Tradeoff Analysis Technique section partially repeats SKILL.md workflow steps, and the script reference lacks a concrete invocation example.

**Strengths:**
- Progressive disclosure done right: SKILL.md is ~1k tokens and explicitly gates the two reference files with read-when conditions (SKILL.md lines 32-36: catalog for decomposition/granularity/sagas/etc., template for durable/multi-team decisions).
- Strong frontmatter trigger: covers the user phrasings that should activate it (coupling, monolith decomposition, sagas, ADRs, fitness functions) and includes an explicit anti-trigger ('Avoid for routine bug fixes unless architecture choices are involved').
- Imperative, evidence-first workflow with a numbered checklist, a structured Output Contract (7 named sections, lines 63-73), and a Gotchas section that acts as concrete stop conditions against the two most common failure modes (split-because-smaller, merge-because-chatty).
- Mechanical step is scripted: newadr.py handles slugging, dating, collision detection (exits 2 if file exists), dry-run mode, and JSON output, while leaving the judgment-heavy content to the model.
- catalog.md is genuinely reference material organized by decision type with concrete disintegrator/integrator lists rather than prose walls; nothing in it is needed at trigger time.

**Recommendations:**
- [medium] **Deduplicate the ADR template between adrtemplate.md and newadr.py** — The full ADR template body exists twice: references/adrtemplate.md lines 11-37 and the build_markdown() string in scripts/newadr.py lines 20-45 (with slightly different section prompts, e.g. the Context wording differs). These will drift. Make adrtemplate.md the canonical source and either (a) have newadr.py load the template from the sibling references/adrtemplate.md at runtime (it knows its own path via __file__), or (b) at minimum add a comment in build_markdown() and a note in adrtemplate.md stating the two must stay in sync, and reconcile the current wording differences.
  - Knowledge preservation: No content is deleted; the identical template text is consolidated into one canonical file (or cross-referenced), so every section heading and prompt sentence survives in adrtemplate.md. The script keeps full behavior.
- [medium] **Give a concrete invocation example for newadr.py in SKILL.md** — SKILL.md line 75 says only 'The helper script scripts/newadr.py can draft a markdown file.' Add a one-line example such as: python <skill_dir>/scripts/newadr.py --title "Split billing schema" --out-dir docs/adr --dry-run. This removes a guaranteed extra step (the agent reading the 101-line script to learn its flags) and clarifies that the path is skill-relative, not repo-relative. One line added to SKILL.md saves ~700 tokens of script-reading per use.
  - Knowledge preservation: Pure addition; no existing text is removed. The script's --help remains the full flag reference.
- [low] **Trim the duplicated items from catalog.md 'Tradeoff Analysis Technique'** — catalog.md lines 146-156 repeat three points already in SKILL.md: 'mutually exclusive and collectively complete' (SKILL.md line 40), 'present the bottom line in stakeholder outcomes' (SKILL.md lines 42-44), and 'guard the decision with fitness functions' (SKILL.md lines 54-61). Keep the unique items (entangled dimensions, modeling coupling points, short qualitative scale, out-of-context scoring, concrete domain scenarios) and replace the three duplicates with a single pointer line: 'Then apply the comparison, bottom-line, and fitness-function steps from the main workflow.'
  - Knowledge preservation: Only verbatim duplicates of SKILL.md content are removed from the reference; all unique technique items stay in catalog.md and the duplicated guidance remains fully present in SKILL.md, which is always loaded when the catalog is.
- [low] **State the ADR path convention once** — The fallback path docs/adr/YYYYMMDD_slug.md appears three times: SKILL.md line 75, adrtemplate.md lines 5-7, and as defaults in newadr.py (--out-dir docs/adr, date format line 62, filename join line 77). Keep it in SKILL.md (needed at decision time without opening the template) and in the script defaults (executable), and drop the restatement in adrtemplate.md in favor of 'Path convention: see SKILL.md / script defaults.' Trivial token saving but removes a third drift point.
  - Knowledge preservation: The convention remains stated in SKILL.md and encoded in the script defaults; only the redundant third copy is replaced with a cross-reference.

## brainstorm — minor-improvements
Footprint: SKILL.md 106 lines / ~6.6KB / ~1.6k tokens; no reference files, scripts, or agents — single-file skill

A lean, well-shaped single-file skill: phased workflow (motivation → exploration → convergence → strategic verdict), a strong structured verdict contract, and high-leverage rules like "explore the codebase instead of asking". At ~1.6k tokens it needs no progressive disclosure. The issues are correctness/routing rather than bloat: the frontmatter description is a near-verbatim copy of the separately-installed grill-me skill (trigger collision and identity mismatch with the body's "creative exploration" framing), Phase 4 points to a /spec command that does not exist in this repo, and the section ordering puts "When to Stop" (with its own closing-summary instruction) before Phase 4, creating two competing endings.

**Strengths:**
- Excellent context-window economy: single file ~1.6k tokens, correctly avoids reference files it doesn't need — no progressive-disclosure work required
- Strong structured output contract: the Phase 4 verdict block (lines 88-99) with BUILD/DEFER/REDUCE SCOPE/REJECT, confidence, scope, and next step is exactly the kind of checklist/contract LLM agents execute reliably
- High-leverage behavioral rules: 'explore the codebase instead of asking' (lines 48-50) and the explicit 'Don't implement anything' guardrail (line 12) prevent the two most common failure modes of brainstorming sessions
- Clear stop conditions ('When to Stop', lines 62-70) — the interview has a deterministic completion test rather than open-ended chatting
- Concrete worked example (button color → deeper need, line 20) anchors the abstract 'peel back layers' instruction

**Recommendations:**
- [high] **Differentiate the frontmatter description from grill-me to fix trigger collision** — The description (SKILL.md line 3) opens with 'Interview the user relentlessly about their input... resolving each branch of the decision tree' and lists 'grill me' as a trigger — nearly word-for-word the description of the installed grill-me skill. Meanwhile the body (line 6-8) frames brainstorm as 'Creative Exploration' and a 'creative thinking partner', and its unique value is Phases 2/4 (alternatives, strategic BUILD/DEFER/REJECT verdict). Rewrite the description to lead with brainstorm's distinct identity: creative exploration of half-baked ideas, expanding the solution space, and closing with a build/defer verdict. Keep triggers unique to this skill ('brainstorm', 'half-baked idea', 'sharpen this idea', 'should we build this') and drop or de-emphasize the verbatim grill-me phrases ('grill me', 'stress-test a plan') so the two skills route deterministically.
  - Knowledge preservation: No knowledge deleted: the interview-relentlessly behavior remains fully specified in the body (Phases 1-3, 'What to Challenge'). Only trigger phrases that duplicate another installed skill are removed from the description; phrases unique to brainstorm are retained and new ones added.
- [medium] **Fix the dangling /spec reference in the verdict block** — Lines 96 and 106 instruct suggesting '/spec <description>' as the next step, but no spec skill exists in this repo (verified: only collaborative_specification matches). Either point to the real follow-on skill (e.g. 'collaborative_specification' or whatever spec workflow the repo standardizes on) or phrase it as 'suggest the repo's spec/PRD workflow' so the agent resolves it at runtime instead of emitting a broken command.
  - Knowledge preservation: The 'hand off to a spec workflow on BUILD/REDUCE' knowledge is preserved exactly; only the broken pointer is replaced with a resolvable one.
- [medium] **Reorder sections to remove the duplicate ending and restore phase flow** — Sections 'When to Explore the Codebase' (48-50), 'What to Challenge' (52-60), and 'When to Stop' (62-70) sit between Phase 3 and Phase 4, and 'When to Stop' line 70 instructs 'Summarize the resolved decision tree at the end' — which competes with Phase 4's closing verdict. Restructure: keep Phases 1-4 contiguous; move the three cross-cutting sections under a single 'Rules throughout' heading after the phases; fold line 70's decision-tree summary into the opening of Phase 4 (the verdict already requires synthesizing the discussion, so the summary naturally precedes the verdict block). This removes one of the two endings and makes the workflow deterministic about what 'done' looks like.
  - Knowledge preservation: Pure relocation and merge: every bullet in the three sections is kept verbatim; the decision-tree-summary instruction is merged into Phase 4 rather than deleted, so the closing output still contains both the resolved decisions and the verdict.
- [low] **Name the interactive question tool explicitly** — Line 10 says 'use the interactive questions tool when available' and line 82 says 'the interactive question tool' — vague references the agent must guess at. Name the concrete tool (AskUserQuestion in Claude Code) with the existing 'when available, otherwise plain text' fallback. Also line 10's requirement to always include an 'Other' option is good and should stay.
  - Knowledge preservation: The when-available fallback and the 'Other option' requirement are kept; only an explicit tool name is added, removing ambiguity without removing any instruction.
- [low] **Fix typo and dedupe the button-color example** — Line 12: 'implementent' → 'implement'. The button-color example appears twice (line 20 in Phase 1 and line 34 in Phase 2) and skews UI-centric while the skill also covers plans/bugs/ideas; keep the line 20 occurrence and swap line 34's for a non-UI example (e.g. a bug whose real issue is the data model) to demonstrate breadth.
  - Knowledge preservation: Both pedagogical points are preserved — line 20 keeps the 'stated request hides a deeper need' example, line 34 still gets a concrete 'challenge the framing' example, just a non-duplicated one that widens coverage.

## claude-runner — minor-improvements
Footprint: SKILL.md 108 lines / ~950 tokens (loaded on trigger); scripts/run_claude.py 619 lines / ~5k tokens (executed, not loaded); agents/openai.yaml 4 lines. No references/ directory; total directory ~731 lines.

claude-runner is a well-built thin-wrapper skill: a lean ~950-token SKILL.md that delegates all mechanical work (CLI invocation, fallback, envelope normalization, atomic output files) to a deterministic Python script. The main problems are accuracy drift between SKILL.md and the script — a self-contradictory fallback-chain claim, an undocumented --output-file flag, and an undocumented -4 return code — plus a cryptic telegraphic "Runtime Compatibility" block and some restatement between the Behavior section and the Options/Output Envelope sections. No progressive-disclosure changes are needed at this size.

**Strengths:**
- Correct architecture for a runner skill: SKILL.md is a thin contract (~950 tokens) and all mechanical steps live in scripts/run_claude.py — deterministic where it should be, no model judgment forced into prose.
- Strong frontmatter description: names the concrete triggers (explicit Claude execution, cross-runner model selection, CLI seat alongside Codex/Gemini/GLM/Qwen) without false-positive bait.
- Good LLM-leverage patterns: Options table with defaults, five concrete copy-pasteable examples (SKILL.md lines 79-85), Return Codes table, and an explicit output-envelope key contract (lines 28-37).
- Script quality is high: atomic JSON output writes (write_json_output_file), envelope normalization with auth_ok inference, prompt/session-file existence validation, OAuth token resolution from login shells, and a guard for bare-mode auth (-4).
- Role overlays are centralized in the script (ROLE_INSTRUCTIONS) so SKILL.md only needs the role name list — no duplicated role prose.
- Consistent with the sibling runner family (codex-runner, qwen-runner, etc. share the same shape), which keeps cross-runner workflows predictable.

**Recommendations:**
- [high] **Fix the self-contradictory fallback-chain claim in Runtime Compatibility** — SKILL.md line 11 states 'Fallback order: codex-runner -> qwen-runner -> kimi-runner -> gemini-runner', but lines 16-19 and the script implement a single fallback to codex-runner only — invoke_fallback (run_claude.py line 145) even passes --disable-fallback to the codex script, so the chain can never extend past codex. An agent reading line 11 will report or rely on fallback behavior that cannot happen. Replace line 11 with the verified behavior: 'Fallback: codex-runner only (invoked with --disable-fallback; the chain does not continue to qwen/kimi/gemini).' If the 4-runner chain is the intended design, implement it in invoke_fallback instead.
  - Knowledge preservation: No unique knowledge is deleted — the inaccurate chain claim is replaced by the verified single-fallback behavior already described in lines 16-19 and the script. If the longer chain is aspirational design intent, keep it as an explicit 'planned, not implemented' note or implement it, so the intent is preserved either way.
- [high] **Document --output-file in the Options table and -4 in the Return Codes table** — The script supports --output-file (run_claude.py lines 558-563, 589-604: writes the full envelope atomically and prints a compact {success, return_code, output_file} stub when combined with --json — ideal for context-window economy in multi-seat workflows), but SKILL.md's Options table (lines 49-64) omits it. Likewise, return code -4 (bare mode without ANTHROPIC_API_KEY/ANTHROPIC_AUTH_TOKEN, run_claude.py lines 399-406) is missing from the Return Codes table (lines 96-103). Add one row to each table; optionally one sentence under Usage noting --output-file + --json keeps large outputs out of the orchestrator's context.
  - Knowledge preservation: Pure knowledge addition: surfaces capabilities that exist only in the script into SKILL.md, where the calling agent actually decides which flags to use. Nothing is removed.
- [medium] **Rewrite the telegraphic Runtime Compatibility block and merge its envelope claims into Output Envelope** — SKILL.md lines 12-14 ('Honor --disable-fallback as fail-fast.', 'Always emit effective_runner, effective_model, fallback_reason.', 'Requirement: auth preflight result field auth_ok.') read like internal spec shorthand, not imperative instructions. The three emitted-field requirements are already covered by the Output Envelope required-keys list (lines 29-37) — fold them there and drop the duplicates. Keep one imperative line for --disable-fallback ('With --disable-fallback, fail fast with a prerequisite message instead of routing'), which also dedupes with the Options-table row at line 64.
  - Knowledge preservation: All four facts survive: envelope-field requirements are consolidated into the existing Output Envelope key list (where they already appear verbatim); the --disable-fallback semantics are kept once in clear imperative form. This is deduplication and relocation, not deletion.
- [medium] **Collapse the Behavior section to its unique facts only** — Behavior items 1-5 (SKILL.md lines 89-93) largely restate earlier content: item 2 repeats the Security Model line 23 and the --safe row line 57; item 4 repeats the --prompt-file/--role/--session-file rows (lines 54, 61-62) and the Usage note line 45; item 5 repeats the Output Envelope key list. The genuinely unique facts are item 3 (--restrict-tools maps to claude --permission-mode plan) and item 6 (with json/stream-json output the native Claude payload stays in stdout, un-reshaped). Reduce Behavior to those two facts (or move the --permission-mode plan mapping into the --restrict-tools Options row at line 60 and the stdout note into the --output-format row at line 56), saving roughly 6-8 lines.
  - Knowledge preservation: Both unique facts (permission-mode plan mapping; native payload pass-through) are retained — either as a 2-item Behavior section or inlined into the corresponding Options-table rows. Everything else removed exists verbatim elsewhere in SKILL.md.
- [medium] **Resolve the dangling reference to runner-envelope.schema.json** — SKILL.md line 28 requires conformance to .agents/skills/_shared/runner-envelope.schema.json, but that file does not exist anywhere in this repository (verified with find; the same dangling reference exists in all 8 sibling runner skills). An agent told to validate against it will burn a turn discovering it is missing. Either commit the schema under a _shared/ directory in the repo, or amend line 28 to state the file is provisioned at install time and that the required-keys list below (lines 30-37) is the authoritative contract when the schema file is absent.
  - Knowledge preservation: The envelope contract is fully preserved — the required-keys list in SKILL.md already encodes it. The fix either materializes the referenced schema file or marks the reference as install-time, adding clarity without removing the contract.
- [low] **Clarify or drop the no-op --safe flag** — In run_claude.py (lines 514-518) --safe is action='store_true' with default=True, so passing the flag changes nothing — permission checks are always on. The Options-table row (SKILL.md line 57) and Behavior item 2 present it as a toggleable option, which can mislead an orchestrating agent into thinking omitting it disables safety. Either remove the flag from the script and table and state 'permission checks are always enabled' once in Security Model (line 23 already says this), or annotate the table row as 'informational no-op; permission checks are always enabled'.
  - Knowledge preservation: The safety guarantee is preserved in the Security Model section (line 23) and Behavior; only the misleading impression of a toggle is removed. If backward CLI compatibility matters for callers already passing --safe, keep the flag in the script and just fix the documentation.
- [low] **Note the purpose of agents/openai.yaml** — agents/openai.yaml (4 lines: display_name 'Claude Runner', default_prompt invoking $claude-runner) is never mentioned in SKILL.md, and its filename ('openai.yaml' inside a Claude skill) is opaque — it appears to be a Codex-app native-subagent interface descriptor matching the sibling codex-runner/agents/openai.yaml. Add one line to SKILL.md (e.g. under Prerequisites or a short Integration note): 'agents/openai.yaml exposes this skill as a native Codex-app subagent seat.' Alternatively document the convention once in the repo README if it applies to all runner skills.
  - Knowledge preservation: Nothing removed; the file stays as-is. The change records currently-tacit knowledge (why the file exists and who consumes it) so future maintainers do not delete it as dead weight.

## clean-code — minor-improvements
Footprint: Single file: SKILL.md, 80 lines / 681 words / ~950 tokens. No reference files, scripts, agents, or templates.

This is a well-built, lean skill: one 80-line SKILL.md with strong trigger metadata, imperative rules, a judgment-driven workflow (correctly script-free, since refactoring is pure model judgment), and a structured output contract. The footprint (~950 tokens) is well within budget, so progressive disclosure is unnecessary. The only real weakness is internal redundancy — three core ideas (behavior preservation, smallest-change preference, and the duplication-vs-similarity distinction) are each stated twice across 'Operating Rules' and 'Workflow', which dilutes instruction salience slightly and adds ~10 lines that can be consolidated without losing any knowledge.

**Strengths:**
- Excellent frontmatter description: covers natural user phrasings (refactor, clean up, improve readability, reduce duplication, improve naming, review maintainability) and includes an explicit negative trigger ('Do not use for broad architecture redesign') that prevents false positives against architecture-redesign skills in the same repo (e.g. improve-codebase-architecture).
- Correct simplicity posture: no scripts, agents, or reference files for a task that is entirely model judgment; nothing mechanical here would benefit from a script.
- Strong LLM-leverage patterns: numbered workflow with classification step (lines 27-31), a dense one-paragraph smell checklist (line 56), concrete naming examples instead of abstract prose (lines 60-63), and a named-field output contract (lines 70-80).
- Safety-first framing is unambiguous and imperative: behavior preservation, characterization tests before risky restructuring (line 19), and an explicit behavior_changes: none reporting field that forces the agent to attest to preservation.
- Handles the review-only branch explicitly twice where it matters: early exit at line 25 and output format at line 80.
- Anti-over-abstraction guidance (lines 17, 47) counteracts a known LLM refactoring failure mode (extracting helpers for merely-similar code).

**Recommendations:**
- [medium] **Deduplicate behavior-preservation rules stated in both Operating Rules and Workflow step 4** — The preservation requirement appears three times: line 12 ('Preserve external behavior unless...'), line 13 ('Keep public APIs, side effects, error behavior, and performance characteristics stable...'), and line 45 ('Preserve edge cases, error messages, observable timing assumptions, and data shapes'). Merge into a single Operating Rule enumerating the full preservation surface (external behavior, public APIs, side effects, error behavior/messages, performance and timing assumptions, edge cases, data shapes), and reduce Workflow step 4 to a one-line pointer ('Re-verify the preservation rule above'). Saves ~3 lines and, more importantly, gives the agent one canonical list instead of three partial ones.
  - Knowledge preservation: Pure deduplication: every preserved property named across lines 12, 13, and 45 (APIs, side effects, error behavior/messages, performance, timing, edge cases, data shapes) is retained in the single merged rule; nothing is deleted, only consolidated.
- [low] **Merge the duplicated 'smallest change' and 'duplication vs similarity' instructions** — Two more idea-pairs are each stated twice: (a) line 14 'Prefer the smallest useful change over a broad rewrite' duplicates the step-3 heading at line 33 'Choose the smallest valuable improvements'; (b) line 17 'Remove duplication only when duplicated code represents the same concept and has the same reason to change' duplicates line 47 'Do not over-abstract code that is merely similar rather than conceptually duplicated'. Keep each idea once in Operating Rules (the line-17 phrasing is the more precise of pair b — keep it and drop line 47, or vice versa) and let the workflow step reference it implicitly. Similarly, line 19 (characterization tests when risky) overlaps with line 40 ('Improve or add tests when risk justifies it') — line 19 carries the unique detail (characterization tests, before deep structural change), so keep line 19 and drop line 40 or fold it in. Net saving ~4-5 lines / ~60 tokens.
  - Knowledge preservation: Each pair expresses one concept twice with slightly different wording; the merge keeps the more information-dense phrasing of each pair (line 17's 'same reason to change' criterion, line 19's 'characterization tests' technique), so no unique criterion or technique is lost.
- [medium] **Add a stop/escalation condition for scope creep during refactoring** — The skill defines what is out of scope (redesign, line 31) but not what to do when the agent discovers mid-refactor that the smallest safe change requires crossing that line (e.g. the tangle in step 3 bullet 5 spans modules, or characterization tests are infeasible). Add one line to Operating Rules or Workflow step 2, e.g.: 'If the safe path requires redesign, dependency changes, or behavior changes beyond the request, stop and report the option to the user instead of proceeding.' This is the one genuinely missing high-leverage instruction — it converts an implicit norm into an explicit stop condition, which coding agents follow far more reliably.
  - Knowledge preservation: Purely additive (one line); no existing content is touched.

## codex-mission-control — minor-improvements
Footprint: SKILL.md 171 lines / ~1.7k tokens (loaded on trigger); scripts/start_mission.py 174 lines / ~1.3k tokens (executed, not loaded); agents/openai.yaml 6 lines (~60 tokens, host metadata only). No reference files. Total well within budget.

A well-built, honest orchestration skill. It is already lean (~1.7k tokens), uses a deterministic script for the one mechanical step (unique ledger creation), and is unusually good at anti-hallucination guardrails ("Separate Thread Reality", "Configuration Reality" both stop the agent from pretending subagents or threads exist when the runtime does not expose them). The main issues are a non-portable hardcoded user path to the helper script, ~15-20 lines of internal repetition across sections (ledger-before-delegation, fork_context policy, the worker report contract, and run-scoped-path uniqueness each appear in 2-3 places), and a local __pycache__ artifact that would ship with filesystem copies.

**Strengths:**
- Excellent context-window economy: 171-line SKILL.md, no bloated reference files, ledger template lives in the script rather than in loaded context.
- Deterministic-vs-model-driven split is correct: start_mission.py handles the collision-prone mechanical step (unique run dir, mission id, ledger scaffold, JSON output of paths), while delegation/splitting decisions stay model-driven with explicit criteria (Delegation Rules, lines 65-81).
- Strong anti-hallucination guardrails: First Move step 2.4 (line 26) and Separate Thread Reality (lines 98-105) explicitly forbid implying parallel execution or thread creation that did not happen; Configuration Reality (lines 107-112) preempts the 'patch a TOML to summon subagents' failure mode.
- Concrete, copyable contracts: Worker Prompt Pattern (lines 130-148) and Handoff Contract (lines 114-126) are structured output contracts an LLM exploits well.
- Frontmatter description triggers on the right phrasings (preserve context, avoid compaction, run subagents, split workstreams, handoffs) and Gotcha 1 (line 167) actively defends against the main false positive: 'asking for depth is not asking for subagents'.
- Script is defensive and clean: slugify bounds, exist_ok=False collision detection, --dry-run, JSON output that hands the agent every path it needs.

**Recommendations:**
- [high] **Remove the hardcoded user-specific script path** — SKILL.md lines 36 and 42 invoke python3 /Users/robson/.agents/skills/codex-mission-control/scripts/start_mission.py. This breaks for any other user or install location and silently falls through to the manual fallback. Replace with a relative reference, e.g. 'run scripts/start_mission.py from this skill's directory' or a placeholder like <skill_dir>/scripts/start_mission.py, since hosts expose the skill's own directory when the skill loads.
  - Knowledge preservation: Pure relocation of the path reference; both command examples (default root and --root /tmp/codex-missions variant) are kept verbatim apart from the path prefix. No information lost — portability gained.
- [medium] **Merge 'First Move' and 'Start A Mission' and deduplicate the ledger-first rule** — The instruction 'create the mission ledger before delegating meaningful work' plus the OS-temp-root escape hatch appears twice: First Move step 1 (line 21) and Start A Mission opening (lines 33, 39). Fold Start A Mission's script invocation into First Move step 1 (or make Start A Mission the single home and have First Move step 1 just reference it). Similarly, the manual fallback (lines 45-50) re-describes what the script does; compress to: 'If the script is unavailable, replicate its structure by hand — read scripts/start_mission.py for the exact ledger template and directory layout.' That also fixes a real gap: today the manual path gives no ledger section structure, while the full template (Runtime Preflight, Workstreams, Thread Registry, Subagents, Handoffs, Decisions, Verification tables) exists only inside the script.
  - Knowledge preservation: Deduplication plus an explicit pointer to the script as the canonical template source. The 4-step manual recipe's unique content (slug, timestamp+suffix, sibling handoffs/worker-reports dirs) is fully encoded in the script the agent is told to read, so nothing is deleted — it gains a progressive-disclosure path to the richer template.
- [medium] **Deduplicate fork_context, worker report contract, and unique-path rules to single homes** — Three rules each appear in two or three places: (a) fork_context=false default + 'fork only when worker truly needs history' at line 81 (Delegation Rules) and line 91 (Native Subagent Runtime item 5) — keep the runtime-section version, which is the actionable one; (b) the worker report contract 'result, files changed, tests run, risks, next handoff' at line 16 (Core Model item 5) and lines 142-147 (Worker Prompt Pattern) — keep the prompt pattern as the single source and have Core Model item 5 say 'compact report per the Worker Prompt Pattern'; (c) run-scoped unique paths at line 15 (Core Model item 4), lines 47-48 (manual steps), and line 168 (Gotcha 2) — Gotcha 2 adds unique rationale (collisions across repeated runs) so keep it, but Core Model item 4 and the manual steps become references. Saves roughly 10-15 lines of repeated instruction.
  - Knowledge preservation: Each rule keeps exactly one authoritative statement containing the full content; other occurrences become one-clause cross-references. The unique rationale in Gotcha 2 (collision explanation) and the parallel-edit warning in runtime item 6 are retained verbatim.
- [low] **Keep __pycache__ out of the skill directory** — scripts/__pycache__/start_mission.cpython-314.pyc (99 lines binary) sits inside the skill. It is gitignored so the repo is clean, but skills distributed by directory copy (e.g. to ~/.agents/skills/) will carry it. Delete it and, if the install flow is a copy script, exclude __pycache__ there.
  - Knowledge preservation: A compiled cache artifact contains no knowledge; the source start_mission.py is untouched.
- [low] **Tighten the frontmatter description's opening clause** — Line 3: 'as a context light manager that creates a run scoped ledger, discovers native subagent tools, spawns bounded workers when authorized, and writes compact handoffs' is implementation detail the trigger matcher does not need; the trailing 'Use when...' clause carries all the trigger weight and is good. Optionally shorten the first sentence to 'Coordinate Codex missions as a context-light manager: run-scoped ledger, native subagent discovery, bounded workers, compact handoffs.' Marginal token saving per session since descriptions of all installed skills load into every context.
  - Knowledge preservation: All concepts (ledger, discovery, bounded workers, handoffs, authorization gate) remain named in the description; only connective prose is compressed. The 'Use when' trigger list is kept unchanged.

## codex-runner — minor-improvements
Footprint: SKILL.md 111 lines / ~900 tokens; references/prompting.md 199 lines / ~1.6k tokens (loaded on demand — currently never); agents/openai.yaml 4 lines; scripts/run_codex.py 581 lines (executed, not loaded into context). Total context cost when triggered: ~900 tokens.

A fundamentally well-built runner skill: a deterministic 581-line Python wrapper does all mechanical work, and SKILL.md is a lean ~900-token interface doc with tables, examples, and return codes. The two real defects are (1) a high-quality 199-line prompting reference (references/prompting.md) that SKILL.md never points to, making it unreachable dead weight, and (2) a "Runtime Compatibility" section whose probe-order claim (codex -> qwen -> kimi -> gemini -> claude) contradicts both the numbered steps directly below it and the script's actual implementation (codex -> claude only). A handful of small dedupe and doc-sync fixes would finish the job.

**Strengths:**
- Excellent script/model division of labor: run_codex.py deterministically handles arg validation, prompt assembly, role overlays, fallback invocation, envelope normalization, and atomic JSON output writes — SKILL.md stays a thin interface doc instead of prose workflow.
- Frontmatter description triggers well: covers explicit Codex requests, cross-runner workflow selection, and disambiguates against native Codex subagents; no false-positive bait.
- LLM-friendly structure throughout SKILL.md: Options table, Return Codes table, five concrete copy-pasteable examples (lines 81-87), explicit required envelope keys (lines 29-37).
- references/prompting.md content is genuinely high-leverage: task-typed XML recipes, an anti-patterns list, and effort-level guidance — exactly the kind of material that belongs in a progressive-disclosure reference file.
- Clear security posture: --full-auto is opt-in only, --restrict-tools maps to read-only sandbox, and the Security Model section states the data-exfiltration surface plainly (SKILL.md line 23).
- Roles are validated by argparse choices (run_codex.py line 476), so the SKILL.md role list cannot silently drift into invalid invocations.

**Recommendations:**
- [high] **Link the orphaned prompting reference from SKILL.md** — references/prompting.md (199 lines of task-typed Codex prompt recipes) is referenced by zero files anywhere in the repo — a grep for the filename and for 'codex-runner/references' returns nothing. An agent triggered on this skill will never know it exists. Add one line to SKILL.md, e.g. after the Usage section: 'Before composing non-trivial prompts (reviews, implementations, research seats), read references/prompting.md for task-type XML recipes and anti-patterns.' The file even says 'Read this file when crafting prompts for Codex' (prompting.md line 7) — but nothing routes the agent there.
  - Knowledge preservation: Pure addition: the reference file is untouched; one pointer line makes ~1.6k tokens of existing knowledge reachable via progressive disclosure.
- [high] **Fix the self-contradictory Runtime Compatibility section** — SKILL.md line 12 states 'Probe order: codex -> qwen -> kimi -> gemini -> claude', but the numbered steps immediately below (lines 16-19) and the script (run_codex.py lines 290-311) implement only codex -> claude-runner fallback. Lines 11 and 13-14 ('Requirement: fallback chain beyond Claude-only behavior', 'keep runner provenance fields mandatory') read as pasted contract-spec fragments, not instructions. Rewrite the section to match reality: 'If codex CLI is missing, the script automatically falls back to claude-runner (run_claude.py) unless --disable-fallback is set, in which case it exits non-zero with return_code -2.' If the qwen/kimi/gemini chain is a cross-runner contract that lives elsewhere (claude-runner SKILL.md line 11 documents its own different chain), say where it lives instead of restating it incorrectly here.
  - Knowledge preservation: The multi-runner chain knowledge is preserved by relocating it as an explicit cross-reference to the runner that owns it (or to a shared contract doc); the codex->claude fallback, --disable-fallback semantics, and provenance-field requirements are all retained in the rewritten section. Nothing is deleted, only corrected and attributed.
- [medium] **Document --output-file in the Options table** — run_codex.py lines 508-513 implement --output-file (atomic JSON write via tempfile+os.replace, lines 80-93), and when combined with --json it switches stdout to a compact {success, return_code, output_file} summary (lines 545-555) — exactly the pattern orchestrating agents should use to keep large Codex outputs out of their context window. SKILL.md's Options table (lines 49-66) omits it entirely. Add one table row plus one sentence noting the compact-summary behavior, and consider one example line showing --json --output-file together.
  - Knowledge preservation: Pure addition: surfaces existing script capability into the doc; no existing content removed.
- [medium] **Deduplicate sandbox/full-auto guidance to one normative location** — The restrict-tools/full-auto policy is stated three times: Security Model (SKILL.md line 23, last two sentences), Options table rows (lines 56-57), and Behavior items 2-3 (lines 92-93). Keep the normative statement in Security Model, keep the terse table rows, and delete Behavior items 2-3. While there, fold Behavior item 6 (envelope fields, line 96) into the Output Envelope section (lines 26-37) which already lists the same keys plus more. The Behavior section then shrinks to the genuinely unique facts: item 1 (executes codex exec), item 5 (overlay/continuation support), and item 7 (native JSONL stream passes through unreshaped — the only place that fact appears, must stay).
  - Knowledge preservation: Strict deduplication: every fact survives in exactly one of Security Model, Options table, or Output Envelope. The unique JSONL-passthrough fact (line 97) is explicitly retained. Saves ~8 lines without losing any information.
- [medium] **Reconcile prompting.md effort guidance with the wrapper's actual flags** — prompting.md lines 186 and 188-199 give --effort level guidance ('--effort xhigh is expensive...'), but run_codex.py exposes no --effort flag and no -c/config passthrough to set model_reasoning_effort. An agent following the reference will construct an invalid invocation. Either (a) add a --effort flag to run_codex.py mapping to the Codex CLI config, or (b) add one sentence to the Effort Level Guidelines section stating effort is configured via Codex CLI config (not this wrapper) and how. Option (a) is preferable since the table implies per-run tuning.
  - Knowledge preservation: The effort-level table and 'tighten prompt before raising effort' advice are kept verbatim; the fix either implements the missing mechanism or adds a clarifying sentence — no guidance is removed.
- [low] **Verify the runner-envelope schema path or make the key list self-sufficient** — SKILL.md line 28 says all --json responses 'must conform to .agents/skills/_shared/runner-envelope.schema.json', but no runner-envelope.schema.json exists anywhere in this repo (verified by find). If the schema ships only at install time into .agents/skills/_shared/, add '(present in installed deployments)' so an agent does not burn a turn searching for it; if it is simply missing, bundle it or drop the path and let the existing required-keys list (lines 29-37) stand as the contract. Same applies to the .agents/skills/codex-runner/... invocation paths (lines 42, 82-87) — if those are install-time paths, that convention is fine but worth a one-line note for agents running from the source repo.
  - Knowledge preservation: The required-keys list — the actual operative contract — is kept either way; the schema reference is clarified or backed by a bundled file rather than deleted.

## coding-design-plan — minor-improvements
Footprint: SKILL.md only: 56 lines / ~650 tokens (3.3 KB). No reference files, scripts, agents, or templates — the entire skill loads in one shot.

This is a well-built, minimal skill that is close to optimal for its purpose. It is a single 56-line SKILL.md (~650 tokens) composed almost entirely of numbered checklists, a structured output contract, explicit escalation conditions, and anti-overengineering gotchas — exactly the patterns LLM agents exploit well. There is no dead content, no reference files to mis-route, and the size makes progressive disclosure unnecessary. The only material weakness is the frontmatter description: it triggers on task properties (ambiguous, boundary-touching, etc.) but omits the explicit user phrasings ("plan this", "design plan", "how should I approach") that should also activate it.

**Strengths:**
- Exceptional context-window economy: ~650 tokens total, no supporting files needed at this size — progressive disclosure correctly not used.
- Checklist-first structure throughout (Workflow lines 12-19, Decision Checks lines 25-31, Output Contract lines 41-46, Gotchas lines 52-56) — no prose walls.
- Clear conditional gating: Decision Checks apply only 'when the task is larger than a local edit' (line 23), and Escalation (line 35) gives concrete, enumerable ADR triggers rather than vague guidance.
- Structured output contract with named snake_case fields (lines 41-46) plus an explicit fast path for same-turn implementation (line 48) — deterministic where it should be, model-driven where judgment is needed.
- Gotchas section (lines 52-56) actively counteracts the skill's own failure mode (over-architecting routine fixes), e.g. 'Do not turn routine bug fixes into architecture reviews' and 'Do not present an option as free.'
- Integrates with the sibling skill pragmatic-coding-session (its lines 35 and 93 hand off here), and the description's trigger conditions match that handoff vocabulary (boundaries, public contracts, persistence).

**Recommendations:**
- [medium] **Add explicit user-phrasing triggers to the frontmatter description** — The description (SKILL.md line 3) triggers only on task properties ('ambiguous, broad, touches module or service boundaries...'). It never names the direct invocations users actually type: 'plan this change', 'make an implementation plan', 'design this before coding', 'how should I structure/approach this'. A user explicitly asking for a coding plan may not activate the skill, while the property-based clauses already cover proactive activation well. Append a clause such as: 'Also use when the user asks for an implementation plan, design plan, or approach before coding.' Keep the existing property list intact.
  - Knowledge preservation: Pure addition to the description string; all existing trigger conditions and body content remain unchanged.
- [low] **Optionally consolidate the triple mention of verification** — Verification appears three times: Workflow step 8 (line 19, 'Convert the main concern into verification before editing'), Decision Check 7 (line 31, 'Fitness: what cheap objective test... proves the design holds?'), and Output Contract field 5 (line 45). The Output Contract occurrence is legitimately separate (it is the output field), but step 8 and check 7 say nearly the same thing in two registers. If tightened, fold check 7's concrete check types (static check, contract check, migration check, smoke check) into Workflow step 8 and drop check 7, or vice versa. This is marginal at 56 lines — only do it if editing the file anyway; the current layering (workflow = always, checks = larger tasks) is defensible as-is.
  - Knowledge preservation: Deduplication by merging: the unique enumeration of check types from line 31 (static/contract/migration/smoke checks) is moved into the surviving line, so no concrete knowledge is deleted — only the duplicate framing.

## coding-implementation-guard — minor-improvements
Footprint: SKILL.md only: 61 lines / 3,593 bytes / ~850 tokens. No reference files, scripts, agents, or templates.

This is a well-built, lean guard skill: four tight numbered checklists (workflow, state/data, code quality, gotchas) plus a structured output contract, all in ~850 tokens with no dead weight and no need for progressive disclosure at this size. The conditional gating is good — the State And Data Checks section is explicitly scoped to edits touching persistence/async surfaces. The two real weaknesses are trigger phrasing in the frontmatter description (written as agent-state description rather than matchable user/task phrasings) and a fuzzy activation condition on the Output Contract.

**Strengths:**
- Entire skill fits in ~850 tokens; no reference files needed and none missing — progressive disclosure is correctly not used at this size.
- All four sections are imperative numbered checklists, not prose walls; each item is a distinct, judgment-level instruction rather than over-specification of basics.
- State And Data Checks (lines 22-32) is properly gated with an explicit condition ('Apply this pass when the edit touches databases, files, queues...'), so the expensive branch only runs when relevant.
- Output Contract (lines 46-53) gives a concrete 4-field structured reporting format — a high-leverage LLM pattern.
- Gotchas section encodes genuine anti-patterns (scope creep, speculative abstraction, indirection-as-cure) that complement rather than duplicate the positive checklists; e.g. Workflow #7 (where coupling belongs), Code Quality #6 (which smells to weaken), and Gotchas #5 (when not to weaken) are three distinct decisions, not redundancy.
- Clear escalation condition: Workflow step 9 ('Stop for redesign only when the current plan cannot be implemented safely').

**Recommendations:**
- [medium] **Rewrite frontmatter description with matchable user/task phrasings** — SKILL.md line 3 reads 'Use when an agent is actively editing nontrivial code, changing behavior, touching stored state...'. Trigger matching runs against the user's request, and users never phrase requests as 'an agent is actively editing nontrivial code'. Reword to lead with task verbs users actually use: e.g. 'Use when implementing a feature, fixing a bug, refactoring, or writing code that touches stored state, APIs, async work, retries, or migrations.' Keep the existing scope nouns (stored state, APIs, async, retries, migrations, refactoring while preserving design) — they are good discriminators against trivial edits. This is the only change with measurable triggering benefit.
  - Knowledge preservation: Pure rewording: every scope term in the current description (stored state, APIs, async work, retries, migrations, refactoring preserving the chosen design, behavior changes) is retained verbatim; only the framing shifts from agent-state to task phrasing.
- [low] **Make the Output Contract activation condition deterministic** — Line 48: 'When this skill shaped the implementation, include:' is ambiguous — the agent cannot reliably decide post-hoc whether the skill 'shaped' anything, so the contract will fire inconsistently. Replace the condition with a deterministic one, e.g. 'After completing edits made while this skill is active, report:'. The four fields (changed, behavior, risk_guarded, verification) are fine as-is.
  - Knowledge preservation: All four contract fields and their definitions (lines 50-53) are kept unchanged; only the one-line trigger condition is tightened from a judgment call to an unconditional post-edit step.

## coding-review-simplify — minor-improvements
Footprint: SKILL.md only: 78 lines / 587 words / ~800 tokens; no reference files, scripts, agents, or templates

This is a well-built, lean skill: a single ~800-token SKILL.md of pure judgment heuristics for a post-implementation review-and-simplify pass, with no need for scripts or progressive disclosure at this size. Structure is strong — numbered checklists, an explicit output contract, conditional branches (Coupling Pass "only when the diff feels tangled"), a findings severity bar, and restraint rules in Gotchas. Only two minor issues exist: small internal duplication between Workflow steps 4/7 and the dedicated Simplification/Coupling sections, and a frontmatter description whose phrases ("during code review", "simplify") collide with sibling skills (full-review, clean-code) and built-in /code-review and /simplify commands, risking wrong-skill activation.

**Strengths:**
- Excellent context-window economy: entire skill fits in ~800 tokens, so single-file design (no reference files) is the correct architecture — progressive disclosure would add overhead, not save it.
- Imperative, checklist-driven throughout: every section is a numbered list, no prose walls (Workflow lines 12-20, Coupling Pass lines 34-38, Simplification Pass lines 55-60).
- Clear structured output contract (lines 62-71) with named fields (outcome/changes/verification/remaining_risk) and an explicit escalation value ('needs design escalation').
- Good conditional gating: Coupling Pass is explicitly rare-branch ('Use this only when the diff feels tangled or crosses a boundary', line 32) and Review Modes (lines 22-28) scale effort to diff size deterministically.
- Findings Bar (lines 40-49) sets a concrete reporting threshold plus a 4-part finding format — prevents noise, a common failure of review skills.
- Gotchas section (lines 73-79) encodes restraint ('Do not rewrite clear code into clever code', 'Do not expand scope') — high-leverage anti-patterns stated as hard rules.
- Pure-judgment task correctly left model-driven; no mechanical steps exist that would benefit from scripts.

**Recommendations:**
- [medium] **Sharpen frontmatter description to avoid collisions with sibling review skills** — The description (SKILL.md line 3) includes 'during code review' and 'when the user asks to simplify', which overlap with full-review ('PR review', 'review my changes'), clean-code ('simplify structure'), and the built-in /code-review and /simplify commands in the same environment. Reword to emphasize this skill's distinct niche: a lightweight, single-agent final pass on a just-completed change before handoff — e.g. 'Final review-and-simplify pass on a just-completed implementation before handoff. Use after an agent finishes coding work...' and drop the bare phrase 'during code review'. Keep all existing trigger nouns (tighten, audit maintainability, remove unnecessary abstraction, verify architecture fit, check data risk, concrete diff).
  - Knowledge preservation: Pure rewording of one frontmatter sentence; every existing trigger keyword is retained, only the ambiguous generic phrases are replaced with more specific equivalents. No body content touched.
- [low] **Deduplicate Workflow steps 4 and 7 against the dedicated passes** — Workflow step 4 (line 15) restates the Simplification Pass (lines 51-60) and step 7 (line 18) restates the Coupling Pass criteria (lines 34-38: strength/locality/degree). Replace step 7 with a pointer: 'If the diff feels tangled or crosses a boundary, run the Coupling Pass below.' For step 4, first fold its unique items not currently in the Simplification Pass (dead code, broad helpers, noisy comments, indirection) into that section's checklist, then reduce step 4 to 'Run the Simplification Pass below.' This removes ~3 lines, eliminates drift risk between the summary and the canonical checklist, and makes the Workflow a clean dispatch table into the named sections.
  - Knowledge preservation: Step 7's content is already fully present in the Coupling Pass section (strength/locality/degree), so it is pure deduplication. Step 4's unique items (dead code, broad helpers, noisy comments, indirection) are explicitly relocated into the Simplification Pass checklist before the step is collapsed — nothing deleted.

## collaborative_delivery — minor-improvements
Footprint: SKILL.md 88 lines / ~1.1k tokens; 3 reference files 96 lines / ~0.9k tokens total (workflow_contract.md 21, output_contract.md 44, engineering_rules.md 31); routing.toml 118 lines; 3 Python scripts 994 lines (~9k tokens, only run, not read into context); agents/openai.yaml 6 lines

A well-engineered skill with an unusually honest participation-accounting design: deterministic scripts handle every mechanical step (panel execution, native-response recording, artifact validation), status enums make "prompt generated" explicitly not equal to "model participated", and a validation gate blocks dishonest completion claims. The main defect is broken progressive disclosure: SKILL.md never mentions any of the three references/ files, so engineering_rules.md and output_contract.md (which contain unique gating knowledge like the per-phase artifact audit structure and the DDD/clean-architecture rules) are orphaned and will never be loaded. Secondary issues are a triplicated required-outputs list, a frontmatter description that triggers on virtually any coding request, and substantial overlap between workflow_contract.md and SKILL.md.

**Strengths:**
- Honest participation model: panel status enums (ok, awaiting_native_execution, dry_run, fallback_used) plus explicit rules that prompt generation and fallback output never count as participation — a failure mode this design class usually gets wrong.
- Strong determinism/judgment split: scripts/panel_round.py, scripts/record_native_response.py, and scripts/validate_artifacts.py handle all mechanical steps; the model handles synthesis, code edits, and review judgment.
- Completion gate is enforced, not advisory: SKILL.md line 88 requires running validate_artifacts.py and the script returns non-zero on missing artifacts or incomplete phases, with an honest-failure escape hatch.
- Role-based routing with model names confined to assets/routing.toml — instructions stay stable when providers change, and fallback is disabled per-role so a missing model cannot be silently substituted.
- record_native_response.py refuses to overwrite non-empty responses without --replace, and panel_summary.json is append-only (phase_runs), preserving evidence.
- SKILL.md itself is lean (~1.1k tokens) with concrete copy-pasteable commands, exact artifact paths, and an explicit list of incomplete-phase statuses (line 22).

**Recommendations:**
- [high] **Wire up the three orphaned reference files with read-X-when-Y pointers** — SKILL.md contains zero references to references/workflow_contract.md, references/output_contract.md, or references/engineering_rules.md (verified by grep — no file in the skill mentions them). output_contract.md has unique knowledge SKILL.md lacks: the 8-item per-phase artifact 'Presence audit' structure (lines 14-23), the full panel-status semantics (lines 25-33), and external transcript handling rules (lines 38-45). engineering_rules.md has the spec-driven/DDD/clean-architecture/TDD rules that the green/refactor phases allude to ('preserving clean architecture and domain boundaries', SKILL.md line 38) but never operationalize. Add to SKILL.md: 'Read references/output_contract.md before writing any phase artifact or interpreting panel_summary.json statuses' and 'Read references/engineering_rules.md before the red/green/refactor phases'.
  - Knowledge preservation: Purely additive — no content removed; existing reference files become reachable instead of dead weight.
- [medium] **Deduplicate the required-outputs list (currently in three places)** — The identical six-artifact list (execution_log.md … panel_summary.json) appears in SKILL.md lines 79-84, output_contract.md lines 5-10, and routing.toml line 6 (required_outputs, which is what validate_artifacts.py actually enforces). Keep the list once in SKILL.md (it is the operative checklist) and replace output_contract.md's copy with 'Required artifacts: see SKILL.md / routing.toml [skill].required_outputs (enforced by validate_artifacts.py)'. Same pattern for the seven required phases, listed in both SKILL.md line 36 and routing.toml line 5.
  - Knowledge preservation: Deduplication with a pointer to the single source of truth that the validator reads; the list itself survives in SKILL.md and routing.toml.
- [medium] **Tighten the frontmatter description to reduce false-positive triggering** — The description ('…the user wants implementation, bug fixing, refactoring, tests, code review…') matches essentially every coding request, but this is a heavyweight workflow: routing.toml configures 6-8 required roles per phase across 7 mandatory phases (~50 required role responses, including external CLI runners with 1200s timeouts, and ~4 manually-recorded native responses per phase). Sibling skills (tdd, full-review, diagnose) cover the lightweight phrasings. Reword to anchor on the multi-model/panel intent, e.g. 'Multi-model panel-gated delivery workflow with mandatory anchor review at every phase. Use when an approved task plan exists and the user explicitly wants multi-model collaborative delivery, panel-reviewed implementation, or auditable red-green-refactor with recorded model participation.'
  - Knowledge preservation: Rewording only — all trigger concepts (implementation, bug fixing, refactoring, tests, review, controlled delivery) remain, qualified by the multi-model context that distinguishes this skill from siblings.
- [medium] **Fold workflow_contract.md's overlapping principles into output_contract.md, keeping only its unique items** — Of workflow_contract.md's 11 principles, items 7, 8, 10, 11 restate SKILL.md lines 22, 30, 52, 64-73, and item 9 restates output_contract.md line 44. Unique knowledge is: portability statement (lines 3, 7-9), roles-not-model-names rule (item 4, partially in SKILL.md line 20), 'run independent role rounds before reconciliation' (item 5, also in routing.toml workflow string), and 'preserve dissent in the decision log' (item 6, also in routing.toml line 7). Move the genuinely unique lines (portability contract, independent-rounds, preserve-dissent) into output_contract.md or SKILL.md and delete the duplicates, eliminating one file from the disclosure tree.
  - Knowledge preservation: Every unique principle is relocated (portability + independence + dissent rules move to output_contract.md or SKILL.md); only verbatim duplicates of statements already present in SKILL.md / routing.toml / output_contract.md are removed.
- [low] **Make script paths in SKILL.md install-location independent** — SKILL.md lines 57 and 67 and line 88 hardcode '.agents/skills/collaborative_delivery/scripts/…', but workflow_contract.md line 3 declares the skill 'usable when copied by itself into any .agents/skills location', and other harnesses install skills elsewhere (e.g. ~/.claude/skills). Phrase commands as 'python3 <this skill folder>/scripts/panel_round.py …' or note 'paths below assume installation under .agents/skills; adjust to this skill's actual folder'. The scripts themselves already resolve skill_root from __file__, so only the documentation is brittle.
  - Knowledge preservation: Same commands and arguments retained; only the path prefix is generalized with a one-line note.
- [low] **Fix pseudo-headings and the duplicated 'Workflow' label in SKILL.md** — Lines 28 ('Core rule'), 32 ('Workflow'), and 44 ('Quality bar') are bare paragraphs acting as headings, and line 32's 'Workflow' duplicates the '## Workflow' section title at line 24. Promote them to '###' headings and drop the redundant inner 'Workflow' label. Cheap structural win: checklists and gates scan better as headings for both humans and the agent.
  - Knowledge preservation: Formatting only; no text removed beyond the one duplicated word 'Workflow'.
- [low] **Document the cross-skill script duplication to prevent silent drift** — panel_round.py, record_native_response.py, and validate_artifacts.py are byte-identical across collaborative_delivery, collaborative_discovery, collaborative_specification, and collaborative_task_design (verified with diff). Duplication is intentional per the portability contract, but there is no marker saying so — a fix applied to one copy will silently diverge. Add one line to each script docstring (or to workflow_contract.md's successor) noting the canonical copy and the sibling skills that mirror it.
  - Knowledge preservation: Additive documentation; all four copies remain to honor the self-containment contract.

## collaborative_discovery — minor-improvements
Footprint: SKILL.md 85 lines / ~1.1k tokens; 3 reference files 95 lines / ~0.8k tokens total (workflow_contract.md 21, output_contract.md 43, engineering_rules.md 31); assets/routing.toml 92 lines / ~0.9k tokens; 3 scripts ~995 lines (panel_round.py 646, record_native_response.py 197, validate_artifacts.py 151) — scripts only enter context if read; agents/openai.yaml 6 lines.

A well-engineered skill: SKILL.md is already lean (~1.1k tokens), mechanical steps are pushed into honest, well-guarded Python scripts, model routing is externalized to editable config, and there is a real completion gate. The main defects are a factual contradiction in phase ordering between SKILL.md and routing.toml, three reference files that SKILL.md never tells the agent to read (one of which, engineering_rules.md, is irrelevant to a discovery-only skill and duplicated byte-identically in three sibling skills), and the panel-status taxonomy half-duplicated between SKILL.md and the unreachable output_contract.md.

**Strengths:**
- Strong honest-participation design: panel_round.py and record_native_response.py refuse to count prompt generation, dry runs, or fallback output as model participation, and validate_artifacts.py is wired into SKILL.md as an explicit completion gate (line 85).
- Good determinism/judgment split: panel execution, native-response recording, and artifact validation are scripts; question selection, reconciliation, and brief writing are model-driven.
- Role-based routing in assets/routing.toml keeps model names out of instructions (SKILL.md lines 20, 30), making the skill reconfigurable without editing prose.
- SKILL.md is short (85 lines) with concrete, copy-pasteable command examples (lines 54-69) and an explicit required-outputs checklist (lines 75-81).
- Frontmatter description triggers well: 'product ambiguity, feature exploration, scenario discovery, tradeoff exploration, or when the user is not yet sure what should be built' covers the right phrasings and cleanly separates from the sibling specification/task_design/delivery skills.
- Scripts are defensive: record_native_response.py validates role-is-native and role-is-configured, refuses silent overwrite without --replace, and keeps panel_summary.json synchronized; panel_summary uses append-only phase_runs so evidence is not overwritten.

**Recommendations:**
- [high] **Fix phase-order contradiction between SKILL.md and routing.toml** — SKILL.md line 36 says run phases in order intake, divergence, user_interview, cross_critique, convergence, and workflow steps 4-6 (lines 37-39) also place cross-critique after the interview loop. assets/routing.toml line 5 declares required_phases = [intake, divergence, cross_critique, user_interview, convergence] — cross_critique before user_interview. validate_artifacts.py only checks set membership, so the run passes either way, but an agent following SKILL.md and a human reading routing.toml will disagree on the canonical sequence. Align routing.toml line 5 to the SKILL.md narrative order (or vice versa) and state once which file is authoritative.
  - Knowledge preservation: No information is deleted; one of two contradictory orderings is corrected so both files state the same sequence.
- [high] **Wire the orphaned references/ files into SKILL.md with read-when guidance** — SKILL.md never mentions references/workflow_contract.md, references/output_contract.md, or references/engineering_rules.md, so an agent will never load them. output_contract.md lines 11-22 (per-phase artifact audit fields) and 23-31 (full panel status taxonomy) and 37-43 (transcript/secret handling) contain unique operational knowledge that is currently unreachable. Add two lines to SKILL.md, e.g. under Required outputs: 'Read references/output_contract.md before writing phase artifacts (audit fields, status taxonomy, transcript handling)' and under Routing: 'Read references/workflow_contract.md when porting or reconfiguring this skill.'
  - Knowledge preservation: Pure addition of pointers; all reference content stays where it is and becomes reachable via progressive disclosure instead of dead weight.
- [medium] **Deduplicate the panel-status rules and anchor-mandate between SKILL.md and the references** — SKILL.md line 22 inlines a partial status list (prompt_only, awaiting_native_execution, dry_run, fallback_used, missing runner, failed role) that duplicates output_contract.md lines 23-31; the mandatory-anchor rule is stated at SKILL.md line 20, line 30, workflow_contract.md item 7, and again in routing.toml's workflow string (line 7). The required-outputs list appears in SKILL.md lines 77-81, output_contract.md lines 5-9, and routing.toml line 6. Keep one canonical inline statement in SKILL.md (the short form at line 22 plus the anchor rule at line 30), replace the rest with 'full taxonomy: references/output_contract.md', and note that routing.toml required_outputs is the machine-read source consumed by validate_artifacts.py.
  - Knowledge preservation: Consolidation, not deletion: the complete taxonomy lives in output_contract.md (made reachable per the previous recommendation); SKILL.md keeps a one-line summary plus pointer, so every status meaning survives in exactly one authoritative place.
- [medium] **Resolve engineering_rules.md — irrelevant to discovery and duplicated in three sibling skills** — references/engineering_rules.md (31 lines: SDD, DDD, clean architecture, TDD) is never referenced by SKILL.md, and SKILL.md line 46 explicitly forbids production code and implementation tasks in this skill. The file is byte-identical (md5 17e3139d) in collaborative_delivery, collaborative_specification, and collaborative_task_design. Either (a) remove it from this skill, since the knowledge survives verbatim in three siblings including delivery where it is actually used, or (b) if portability-in-isolation matters (workflow_contract.md line 3), keep it and add one line to SKILL.md: 'The feasibility role may consult references/engineering_rules.md when judging implementation risk.'
  - Knowledge preservation: Option (a): identical copies remain in collaborative_delivery/specification/task_design, so zero unique knowledge is lost repo-wide. Option (b): file is kept and gains a read-when trigger, converting dead content into reachable content.
- [low] **Replace plain-text pseudo-headings inside the Workflow section** — SKILL.md lines 28 ('Core rule'), 32 ('Workflow' — duplicating the H2 at line 24 and confusing), and 42 ('Quality bar') are bare prose lines acting as headings. Make them '###' subheadings (or bold), and rename the inner 'Workflow' to 'Steps'. This makes the core rule and quality bar scannable checkpoints rather than buried prose.
  - Knowledge preservation: Verbatim restructuring; every sentence is retained, only markup changes.
- [low] **Use a skill-root placeholder instead of hardcoded install path in command examples** — SKILL.md lines 55 and 65 hardcode .agents/skills/collaborative_discovery/scripts/..., but workflow_contract.md line 3 says the skill must work when copied to any .agents/skills location, and in this repo the skill actually lives at repo root (collaborative_discovery/), so the example commands fail verbatim. The scripts already self-locate via get_skill_root(), so write the examples as python3 <skill_root>/scripts/panel_round.py with a one-line note that <skill_root> is this skill's directory.
  - Knowledge preservation: Both example commands are retained in full with one path segment generalized; the portability requirement in workflow_contract.md is unchanged and now actually honored.

## collaborative_specification — minor-improvements
Footprint: SKILL.md 89 lines / ~1.1k tokens; 3 reference files 98 lines / ~1.0k tokens total; assets/routing.toml 113 lines / ~1.2k tokens; agents/openai.yaml 6 lines; 3 scripts ~995 lines / ~9k tokens (never loaded into context unless read)

A well-engineered skill: SKILL.md is already lean, mechanical steps (panel execution, native-response recording, completion validation) are fully scripted with honest participation tracking, and routing is externalized to an editable TOML that the scripts treat as the source of truth. The main weakness is broken progressive disclosure: all three reference files are orphaned (SKILL.md never says when to read them), while the participation/status rules and the required-outputs list are each duplicated across two or three files. Fixes are deduplication and pointer additions, not restructuring.

**Strengths:**
- Strong determinism where it matters: scripts/panel_round.py executes panels, scripts/record_native_response.py records native output with overwrite protection and summary sync, and scripts/validate_artifacts.py is an explicit completion gate cited in SKILL.md L87-89 — the model is never asked to self-certify participation.
- Honest anti-hallucination contract: explicit non-participation statuses (prompt_only, awaiting_native_execution, dry_run, fallback_used) defined in SKILL.md L22 and enforced in code (panel_round.py OK_STATUSES, fallback/independence check L314-323), with fallback disabled for runner seats.
- Single source of truth for configuration: routing.toml drives phases, roles, required outputs, and mandatory anchors; both validators read it rather than re-encoding the lists in Python.
- Frontmatter description triggers well: covers PRD, product spec, technical spec, acceptance criteria, codebase fit analysis, and disambiguates against the sibling discovery/task-design/delivery skills via the 'after discovery ... before task planning' framing.
- SKILL.md is already small (~1.1k tokens) with concrete runnable command examples (L55-70) and a clear stop condition (L41: stop before task breakdown, hand off to task design skill).

**Recommendations:**
- [high] **Add 'read X when Y' pointers for the three orphaned reference files** — SKILL.md never mentions references/workflow_contract.md, references/engineering_rules.md, or references/output_contract.md — an agent following SKILL.md alone will never load them. Add three one-line pointers: read references/output_contract.md when writing phase artifacts or interpreting panel_summary.json statuses; read references/engineering_rules.md before the domain_definition and backend_definition phases so contracts follow the DDD/clean-architecture/TDD rules; read references/workflow_contract.md when copying the skill elsewhere or editing assets/routing.toml.
  - Knowledge preservation: Pure addition (~3 lines to SKILL.md). No content removed; previously unreachable knowledge in the three reference files becomes reachable.
- [medium] **Deduplicate panel participation/status rules into output_contract.md** — The 'prompt generation is not participation / record native responses' rule is stated three times: SKILL.md L22 (dense prose paragraph), references/output_contract.md 'Panel status rules' L28-35 plus 'Native response helper' L36-38, and references/workflow_contract.md principles 8, 10, 11 (L14-17). Keep output_contract.md as the authoritative detailed status list; shrink SKILL.md L22 to the core sentence ('A phase is complete only when every required role has status ok or native_response_recorded in panel_summary.json') plus a pointer to output_contract.md; collapse workflow_contract.md principles 8/10/11 into one principle referencing output_contract.md.
  - Knowledge preservation: All six status semantics and the helper behavior survive verbatim in output_contract.md (the most detailed of the three copies); SKILL.md and workflow_contract.md keep one-line summaries plus a file pointer, so nothing unique is deleted — only repetition.
- [medium] **Remove the third copy of the required-outputs list** — The eight required artifacts are listed identically in SKILL.md L76-85, references/output_contract.md L3-12, and assets/routing.toml required_outputs (L6, which validate_artifacts.py actually reads). Drop the copy in output_contract.md L1-12 and replace with 'Required artifacts: see required_outputs in assets/routing.toml (mirrored in SKILL.md Required outputs)'. Same for workflow_contract.md L19-21 (primary output + artifact dir), which duplicates SKILL.md L76 and routing.toml L3.
  - Knowledge preservation: The list remains in two places — SKILL.md (human/agent-facing) and routing.toml (machine-enforced source of truth) — so the knowledge is preserved and a future edit has one fewer copy to drift.
- [medium] **Fix the role list in SKILL.md to include delivery_review** — SKILL.md L20 enumerates roles as synthesis_anchor, adversarial_anchor, product, domain, interface, backend, security — but assets/routing.toml defines an eighth role, delivery_review (L89-92), which participates in every configured phase (L94-113). An agent reasoning from SKILL.md alone will under-count required roles. Add delivery_review to the L20 list.
  - Knowledge preservation: Pure addition correcting an omission; aligns prose with routing.toml, no deletion.
- [low] **Document the unreachable kind="cli" provider path** — scripts/panel_round.py run_direct_cli_role (L352-421, ~70 lines) handles providers with kind="cli", command, args, and prompt_transport="stdin" — but no provider in assets/routing.toml uses it and no doc mentions it, so it is dead with default config and invisible to anyone editing routing. Add one sentence to workflow_contract.md (or a TOML comment in routing.toml): providers may alternatively use kind="cli" with command/args/prompt_transport for direct CLI invocation without a runner wrapper.
  - Knowledge preservation: Surfaces existing script capability as documented knowledge instead of leaving it discoverable only by reading 646 lines of Python; no code or behavior removed.
- [low] **Make command examples portable per the skill's own contract** — workflow_contract.md L3 says the skill 'must be usable when copied by itself into any .agents/skills location', but SKILL.md L56, L66, and L89 hardcode .agents/skills/collaborative_specification/ paths. Replace with '<skill_root>/scripts/...' and one note that <skill_root> is this skill's install folder. Also worth one line in SKILL.md's completion gate mentioning validate_artifacts.py --allow-missing-phases for partial in-progress runs (currently only discoverable in the script's argparse help, L109-113).
  - Knowledge preservation: Path semantics preserved via an explicit <skill_root> definition; the --allow-missing-phases flag knowledge is promoted from script-internal help text to SKILL.md, adding rather than removing information.

## collaborative_task_design — minor-improvements
Footprint: SKILL.md 87 lines / ~1.0k tokens; 3 reference files (workflow_contract.md 21, engineering_rules.md 31, output_contract.md 45 lines) ~0.9k tokens total; assets/routing.toml 102 lines ~1.0k tokens; 3 scripts 994 lines (~9k tokens, never loaded into context); agents/openai.yaml 6 lines. Total repo footprint ~1,286 lines, context-loaded portion is lean.

A well-built, honest multi-model panel skill: SKILL.md is already lean (~1k tokens), the scripts are deterministic and rigorously prevent fake participation (prompt_only/dry_run/fallback_used all block completion), and a validation gate closes the loop. The main defect is broken progressive disclosure: SKILL.md never instructs the agent to read any of its three reference files, so engineering_rules.md (the core SDD/DDD/Clean-Architecture/TDD methodology) and output_contract.md (per-phase artifact structure and status semantics) are unreachable knowledge. Secondary issues are small in-file repetitions and undocumented script capabilities. No deletions needed — fixes are pointers, dedup, and relocation.

**Strengths:**
- Honest participation model: panel_round.py explicitly refuses to count prompt generation, dry runs, or fallback output as model participation, and validate_artifacts.py re-verifies role statuses rather than trusting the 'complete' flag (defense in depth at validate_artifacts.py:79).
- Deterministic where it should be: all mechanical steps (panel execution, native-response recording, artifact validation) are scripted; judgment steps (task slicing, architecture mapping, dissent preservation) are model-driven.
- SKILL.md is already lean at 87 lines with heavy machinery pushed into scripts/ and config into assets/routing.toml — good context economy for the loaded file.
- Frontmatter description triggers well: concrete input artifacts (PRD, technical spec, bug description, discovery brief) and concrete outputs (implementation tasks, test plans, architecture decisions, delivery phases); distinct from sibling skills collaborative_discovery/specification/delivery, low false-positive risk.
- Role-based routing (synthesis_anchor, adversarial_anchor, specialists) with model names confined to editable assets/routing.toml — clean separation of policy from mechanism.
- Clear completion gate with an exact command (SKILL.md:87) and an explicit honesty escape hatch ('report the failure honestly').
- record_native_response.py refuses to overwrite non-empty responses without --replace and validates role/phase against routing config — good guardrails against accidental evidence corruption.

**Recommendations:**
- [high] **Add 'read X when Y' pointers for the three reference files (currently orphaned)** — SKILL.md never mentions references/engineering_rules.md, references/output_contract.md, or references/workflow_contract.md. engineering_rules.md contains the actual SDD/DDD/Clean-Architecture/TDD rules that SKILL.md:8 invokes by name but never makes reachable. output_contract.md contains the per-phase artifact 'Presence audit' structure (lines 13-24) and the authoritative panel status semantics (lines 27-33) — neither is discoverable. Add to SKILL.md: 'Read references/engineering_rules.md before architecture_mapping and task_slicing'; 'Read references/output_contract.md before writing phase artifacts and when interpreting panel_summary.json statuses'; 'Read references/workflow_contract.md only when porting or editing this skill'.
  - Knowledge preservation: Pure addition of pointers; all reference content stays exactly where it is and becomes reachable instead of dead. No text deleted.
- [medium] **Deduplicate repeated statements inside SKILL.md** — Three repetitions in 13 lines: 'routing is editable in assets/routing.toml' appears at SKILL.md:18, :20, and :30; the mandatory-anchor-participation rule appears at :22 and :30; the 'every role must produce a real response' rule appears at :22 and :30. Merge the 'Core rule' paragraph (line 28-30) into the 'Routing and configurability' section (lines 16-22) and state each rule once. Also fix the confusing structure where the plain-text label 'Workflow' (line 32) repeats the '## Workflow' heading (line 24) — promote 'Core rule', the numbered steps, and 'Quality bar' to proper subheadings or fold them. Saves roughly 8-10 lines.
  - Knowledge preservation: Strict deduplication: each rule keeps exactly one canonical statement in SKILL.md; no unique constraint is removed.
- [medium] **Make output_contract.md the single canonical home for artifact list and status semantics** — The required-outputs list is stated three times: SKILL.md:75-83, references/output_contract.md:3-11, and routing.toml required_outputs (which the scripts actually enforce). The failure-status enumeration in SKILL.md:22 ('prompt_only, awaiting_native_execution, dry_run, fallback_used...') duplicates output_contract.md:27-33. Keep the artifact list in SKILL.md (agents need it without an extra read since it is the deliverable contract) but replace output_contract.md:3-11 with a one-line pointer to routing.toml as source of truth; in SKILL.md:22 replace the status enumeration with 'any status other than ok / native_response_recorded blocks the phase — see references/output_contract.md for status semantics'.
  - Knowledge preservation: Each fact retains one canonical location (routing.toml for the enforced list, output_contract.md for status semantics) with pointers from the others; nothing is deleted, only relocated/referenced.
- [low] **Document --fail-on-incomplete and --roles in the SKILL.md runner example** — panel_round.py supports --fail-on-incomplete (panel_round.py:498-502, returns exit code 2 when a required role is missing/failed) and --roles override (line 490), but SKILL.md never mentions either. Adding --fail-on-incomplete to the example at SKILL.md:54-60 makes the per-phase gate deterministic via exit code instead of relying on the agent to parse panel_summary.json output. One added flag plus one sentence.
  - Knowledge preservation: Pure addition surfacing existing script capability that is currently only discoverable via --help or reading 646 lines of source.
- [low] **Use portable path phrasing in SKILL.md command examples** — references/workflow_contract.md:3 declares the skill 'usable when copied by itself into any .agents/skills location', but the examples at SKILL.md:55 and :65 hardcode the prefix .agents/skills/collaborative_task_design/. The scripts already resolve their own skill root via __file__ (panel_round.py:69-70), so the examples work from any install path. Phrase as 'python3 <skill-root>/scripts/panel_round.py' with one note that <skill-root> is this skill's folder.
  - Knowledge preservation: Rewording only; the canonical default location remains documented in workflow_contract.md.
- [low] **Document the unused kind="cli" provider branch as an extension point** — panel_round.py:352-421 (run_direct_cli_role) implements a direct-CLI provider kind with prompt templating and stdin transport, but routing.toml uses only native_codex and runner kinds, and no doc mentions cli. It reads as dead code to an auditor. Add one line to references/workflow_contract.md (or a routing.toml comment): providers support kind = native_codex | runner | cli, with cli for direct command execution via command/args/prompt_transport keys.
  - Knowledge preservation: Code stays as-is; the addition records knowledge (the cli provider contract) that currently exists only implicitly in source, protecting it from future 'dead code' deletion.

## council — minor-improvements
Footprint: SKILL.md 295 lines / 1,737 words / ~2.6k tokens; agents/openai.yaml 4 lines / ~60 tokens (host interface config, not loaded into agent context). No reference files or scripts. Total ~2.7k tokens.

A well-structured single-file workflow skill: numbered steps, concrete prompt templates, explicit decision thresholds, host-aware seat mapping, and a strong anti-skip "Mandatory Invocation" guard. Its size (~2.6k tokens) is acceptable for a workflow that uses every section on every run, so progressive disclosure is not strictly required. The main problems are internal duplication (the seat output contract and several rules appear 2-4 times), a contradictory failure policy (quorum-of-3 "continue" vs Gemini "hard stop"), and a "Host Mapping" intro written as author notes rather than agent instructions. Deduplication alone would cut roughly 25% of the file with zero knowledge loss.

**Strengths:**
- Strong anti-degradation guard: the Mandatory Invocation section (lines 17-25) directly counters the most common failure mode of orchestration skills — the agent answering directly instead of running the workflow — and the frontmatter description reinforces triggering on trivial-looking requests with concrete example triggers (lines 287-294) including degenerate cases like 'council where is the login screen'.
- Concrete output contracts: verbatim seat prompt and Codex final-decision prompt templates (lines 249-277) plus a numbered 5-field return contract give deterministic, parseable seat outputs.
- Explicit decision thresholds (lines 279-285) with three confidence bands remove ambiguity about when to ask vs proceed.
- Host-aware tool routing: Codex-host vs Claude-host sections (lines 48-69) specify exact native-vs-runner choices including spawn_agent fork_context=false and reasoning_effort=xhigh, with a fallback policy.
- Good information-hygiene rules: the blind-round constraint enumerates exactly what NOT to pass to seats (lines 115-120), and Step 3 forbids smuggling context into the moderator report (line 165).
- Lean footprint: one file, no orphaned reference sprawl, ~2.6k tokens — within reason for an always-fully-used workflow skill.

**Recommendations:**
- [high] **Resolve the contradictory seat-failure policy (quorum vs Gemini hard-stop)** — Line 43 says 'Do not hard-stop on single seat failure; continue with lowered confidence' with a minimum quorum of 3 seats, but line 74 says 'If Gemini cannot run through its runner or exposed tools, stop and explain the missing prerequisite instead of silently dropping the seat.' An agent hitting an unavailable Gemini CLI receives directly conflicting instructions. Pick one policy and state it once in the Fallbacks section — e.g. 'On any single seat failure (including Gemini), record a seat_unavailable reason, continue if >=3 seats remain, and surface the missing prerequisite to the user in the final presentation; stop only if quorum drops below 3.'
  - Knowledge preservation: Both pieces of knowledge are preserved and merged: the quorum-of-3 continue rule, the requirement to record seat_unavailable reasons, and the requirement to explain (not silently drop) the Gemini prerequisite all survive in the unified policy — only the contradiction is removed.
- [medium] **Deduplicate the seat output contract and Codex decision prompt** — The 5-field 'Return: 1. Task understanding ... 5. Confidence from 0-100' block appears verbatim twice (Step 2 lines 133-140 and Prompting Pattern lines 261-265), and Step 4's list of what Codex must return (lines 180-185) restates the Codex prompt template at lines 270-277. Make the Prompting Pattern section the single canonical source, move it directly under Step 2 / Step 4 (or have those steps say 'use the seat prompt / final-decision prompt in Prompting Pattern'), and delete the redundant copies. Saves ~25 lines.
  - Knowledge preservation: Pure deduplication: the canonical prompt templates remain verbatim in one location; the steps gain explicit pointers to them, so no field of the contract or prompt wording is lost.
- [medium] **Rewrite Host Mapping intro as agent instructions and fix dangling host references** — Lines 41-44 read like author notes ('Define native seat paths, runner fallback order, and minimum quorum of 3 seats') rather than imperative agent instructions, and line 41 mentions Qwen CLI and Kimi CLI hosts that have no corresponding subsection (Codex and Claude hosts are the only ones defined). Rewrite as: a one-line 'Detect the host first' instruction, the unified failure policy from recommendation 1, and a catch-all rule for other hosts, e.g. 'On any other host (Gemini CLI, Qwen CLI, Kimi CLI, etc.), use runner skills for all four seats.' Also add 'seat_unavailable reasons' to the Step 3 moderator-report section list (lines 154-163), where line 44 says it belongs but the report template omits it.
  - Knowledge preservation: All facts relocate rather than vanish: quorum=3, seat_unavailable recording (moved into the Step 3 report field list where it is actually used), host detection order, and Qwen/Kimi host coverage (converted from a dangling mention into an actionable catch-all fallback rule).
- [medium] **Consolidate the four repetitions of the 'always run the full workflow' rule** — The mandatory-run rule appears in the frontmatter description (line 3), the Mandatory Invocation section (lines 17-25), Operating Rules bullet line 30, and Step 1 line 80. Keep it in the frontmatter (needed for the trigger decision) and in Mandatory Invocation (the enforcement detail); delete the Operating Rules bullet and reduce Step 1 line 80 to its unique content: 'If confidence is already above 95%, build the clarified prompt directly and continue to Step 2.' Similarly, --auto behavior is stated twice (lines 29-31 vs 98-101) and the blind-round rule twice (line 36 vs lines 115-122) — keep the detailed version in the step, replace the Operating Rules duplicates with one-line pointers.
  - Knowledge preservation: Deduplication with pointers: each rule keeps one authoritative statement (Mandatory Invocation for the always-run rule; Step 1 for --auto interview behavior; Step 2 for the blind-round exclusion list). The unique fragment in line 80 (skip-to-Step-2 path when already clear) is retained.
- [low] **Merge 'Recommended Seat Configuration' into Host Mapping** — Line 148 ('Codex native seat: spawn_agent(... fork_context=false, reasoning_effort="xhigh")') duplicates lines 57-60 in the Codex Host section, and the Opus/Sonnet/Gemini bullets (lines 144-147) overlap the host sections' runner choices. Fold the unique flags (--role planner, --restrict-tools for codex-runner; --role planner for gemini-runner) into the corresponding Host Mapping bullets and delete the Recommended Seat Configuration subsection.
  - Knowledge preservation: The unique CLI flags (--role planner, --restrict-tools) move into the Host Mapping bullets that already name those runners; the duplicated spawn_agent parameters are kept once in the Codex Host section.
- [low] **Document the purpose of agents/openai.yaml** — agents/openai.yaml defines a Codex app interface (display name, default prompt) but SKILL.md never mentions it, so its role is opaque to maintainers and auditors. Add a YAML comment at the top of the file (or one line in SKILL.md's Host Mapping section) stating it is the Codex-host interface/registration config and is not meant to be read at runtime.
  - Knowledge preservation: Purely additive: the existing 4 lines of config are untouched; a comment records currently-implicit knowledge about why the file exists.

## data-systems-coding-lens — good-as-is
Footprint: SKILL.md 145 lines / ~1.1k tokens (loaded on trigger); evals.json 20 lines / ~150 tokens (test asset, not loaded at runtime). No reference files, scripts, or templates — total directory footprint ~1.2k tokens.

This is a well-built, lean judgment-lens skill that needs no structural changes. At ~1.1k tokens, SKILL.md sits comfortably below the size where progressive disclosure into reference files would help, and every section earns its place: ordered checklists with concrete "ask" questions, conditional gates for rare branches, output templates per work type, and a short final-response contract. Being entirely model-driven is correct for this skill — there are no mechanical steps that would benefit from scripts. Only two low-impact tightening opportunities exist, neither essential.

**Strengths:**
- Excellent context economy: the entire skill is ~1.1k tokens with zero filler prose; no reference files are needed at this size, so the flat single-file structure is the right call rather than a gap.
- Strong trigger description: the frontmatter enumerates the concrete surfaces that should activate it (databases, queues, caches, migrations, retries, consistency, production data risk) and states what the skill produces (implementation checks, review findings, verification steps), which limits false positives.
- High LLM leverage throughout: every Implementation Lens section (SKILL.md lines 28-88) is a check-list plus 5 numbered failure-mode questions rather than prose; the questions are concrete and high-signal (e.g. line 61 'Can a retry create duplicate money, emails, notifications, jobs, or records?').
- Clear conditional gating for rare branches: 'Distributed Behavior' (line 66) explicitly scopes itself to multi-process/multi-system work, and 'When To Stay Lightweight' (lines 135-137) gives an explicit opt-out for UI copy, styling, one-off scripts, and docs-only edits — a proper stop condition that prevents over-application.
- Structured output contracts: three fill-in templates (lines 94-122) keyed to work type (implementation note, review finding, decision record) plus a one-line final-response contract (lines 143-145) make the skill's effect on output deterministic and verifiable.
- Eval coverage exists (evals.json) with three prompts spanning the skill's main modes: background-job implementation, migration PR review, and cache design — each with expected-output criteria that map directly to SKILL.md sections.

**Recommendations:**
- [low] **Fold 'When To Stay Lightweight' into the First Pass scope guidance** — First Pass (SKILL.md line 22: 'If the task is small, keep this pass brief...') and the 'When To Stay Lightweight' section (lines 135-137) both govern when to scale the lens down, but sit 113 lines apart. Merging the lines-135-137 content into the end of First Pass would put all scope-calibration guidance at the decision point where the agent first applies it, saving a heading and ~3 lines. Optional — the current split also works because line 137 acts as a late guardrail.
  - Knowledge preservation: Pure relocation and merge: the explicit exemption list (UI copy, styling, one-off scripts, docs-only edits, throwaway utilities) and the re-engagement condition (persistent data, background work, production operational risk) are kept verbatim inside the First Pass section; nothing is deleted.
- [low] **Move the copyright disclaimer out of the runtime instruction flow** — Line 10 ('Do not quote or summarize any copyrighted source. Use this as an original engineering workflow.') is an authoring-time provenance note, not a runtime instruction — the skill contains no copyrighted material for the agent to quote, so it spends tokens on a branch that never executes. If retained for legal hygiene, compress to a single short clause appended to the intro sentence on line 8, or move it to an HTML comment.
  - Knowledge preservation: The constraint is retained, just relocated/compressed (intro clause or comment) so the provenance intent stays in the file; no information is removed from the repository.

## ddd-strategic — minor-improvements
Footprint: SKILL.md 142 lines / ~1.8k tokens; 1 reference file (references/context-patterns.md) 98 lines / ~1.2k tokens; ~3k tokens total

A well-built review-lens skill: four clearly scoped lenses with imperative checks, a concrete repo context map, a structured output contract with explicit stop conditions ("if a lens is clean, write clean — don't invent findings"), and a reference file for the deep catalog. The frontmatter description triggers well and explicitly disambiguates from ddd-tactical, model-lens, and architect-lens (all of which exist and whose cross-referenced lens numbers check out). The one real weakness is substantial duplication between SKILL.md Lens 3 (lines 79–102) and the integration-pattern catalog in references/context-patterns.md (lines 30–77) — same pattern definitions and even the same repo examples appear in both places, which costs always-loaded context and creates a two-place maintenance hazard.

**Strengths:**
- Excellent frontmatter description: covers concrete user phrasings (adding/moving a service, naming feels off, integrating with a third party) and explicitly disambiguates from sibling skills ddd-tactical/model-lens/architect-lens, reducing false-positive triggering across the four DDD/architecture lenses in this repo.
- Strong output contract (SKILL.md lines 119-138): fixed section headings, file:line citation requirement, one-line verdict, and explicit anti-hallucination stop condition ('don't invent findings', 'if it's clean, say so' at lines 21 and 140).
- Concrete repo grounding: the greenspark-aws context map (lines 23-38) and repo-specific expectations (ACL for erp-sync-workflow, OpenAPI layer as OHS, common/gslogic as shared kernel) turn abstract DDD theory into checkable assertions — high LLM leverage.
- Progressive disclosure exists and is pointed to: line 141-142 tells the agent the reference holds the pattern catalog, boundary heuristics, and book provenance.
- Cross-skill references are accurate: ddd-tactical Lens 4/5/6 referenced at SKILL.md lines 75, 102, and references line 98 all exist in ddd-tactical/SKILL.md.

**Recommendations:**
- [medium] **Deduplicate the integration-pattern catalog between SKILL.md Lens 3 and the reference file** — SKILL.md Lens 3 (lines 79-102) re-explains ACL, OHS, Conformist, Shared Kernel, and Separate Ways with near-identical content to references/context-patterns.md lines 30-55, including the same repo examples in both files (ERP connectors in erp-sync-workflow need an ACL: SKILL.md 86-87 vs ref 44-46; OpenAPI/RTK Query layer as OHS: SKILL.md 90-91 vs ref 48-50; common/gslogic as shared kernel: SKILL.md 95-97 vs ref 37-38). Compress Lens 3 to a checklist of what to flag (one line per pattern, keeping the repo-specific expectations and the 'flag foreign model deep inside our domain' check), and let the reference own the full definitions. Saves roughly 12-15 always-loaded lines and removes the risk of the two copies drifting apart.
  - Knowledge preservation: All pattern definitions already exist verbatim in references/context-patterns.md; before trimming, merge any phrasing unique to SKILL.md (e.g. 'the ACL is where foreign vocabulary is mapped to ours' nuance, conformist 'risky for core subdomains' caveat — the latter is already at ref line 42) into the reference. Nothing is deleted, only relocated/deduplicated.
- [low] **Deduplicate the smaller overlaps in Lens 1, Lens 2, and Lens 4** — Three more partial duplications: (a) 'start wider and split later / logical vs physical boundary cost' appears at SKILL.md 54-57 and ref 21-24; (b) the ubiquitous-language smells list at ref 87-88 restates SKILL.md Lens 2 bullets 64-75; (c) subdomain-drift directions at SKILL.md 108-114 vs ref 90-97. Keep the actionable check in SKILL.md and the rationale/book provenance in the reference — e.g. SKILL.md Lens 1 keeps 'start wider for volatile areas' as one line and drops the cheap/expensive refactoring explanation already at ref 21-24. Apply only where the SKILL.md copy adds no unique check.
  - Knowledge preservation: Each overlapping rationale is retained in references/context-patterns.md (which also holds unique content the skill should keep: Partnership, Context Map, one-team-per-context ownership rule, Ch. 15 coupling traps, generic→core drift). Unique SKILL.md checks (term-collision-as-boundary-signal examples, 'where the language lives' bullet at 76-77) stay in SKILL.md.
- [low] **Make the reference pointer conditional and move it where it's needed** — The only pointer to references/context-patterns.md is the last sentence of SKILL.md (lines 141-142), phrased as a generic 'see X for Y'. Convert to read-when guidance and place it at the top of Lens 3 (where it becomes load-bearing after recommendation 1): e.g. 'Read references/context-patterns.md before classifying an integration relationship or when a finding needs the full pattern definition / book provenance.' This makes progressive disclosure deterministic instead of optional.
  - Knowledge preservation: Pure relocation and rewording of an existing pointer; no content removed.
- [low] **Add a one-line fallback for non-greenspark repos to the context map** — The 'Map: contexts in greenspark-aws' section (SKILL.md lines 23-38) hardcodes one repository. This matches a pattern in sibling skills (model-lens also references greenspark-aws), so it is acceptable if the skills are repo-pinned — but a single line such as 'If this repo is not greenspark-aws, first derive the context map from the service/module layout' makes the skill degrade gracefully instead of producing wrong findings against a stale map.
  - Knowledge preservation: The full greenspark-aws map stays as-is; the change is purely additive (one fallback instruction).

## ddd-tactical — minor-improvements
Footprint: SKILL.md 169 lines / ~2.4k tokens; 1 reference file (references/decision-trees.md) 122 lines / ~1.3k tokens; ~3.7k tokens total

This is a well-constructed, lean review-lens skill: strong disambiguating frontmatter, six clearly scoped lenses with imperative rules, a concrete output contract, explicit "skip clean lenses / say clean" anti-padding instructions, and repo-grounded guidance. Its main weakness is high overlap (~70-80%) between SKILL.md and references/decision-trees.md — the reference largely restates the six lenses rather than holding only deep-dive material — and the single pointer to the reference lacks a "read when" condition. One genuinely unique, operationally important fact (pattern migrations also run downward, core → supporting) lives only in the rarely-read reference. No structural changes are needed; targeted deduplication and pointer tightening suffice.

**Strengths:**
- Frontmatter description (SKILL.md:3) is excellent: covers concrete user phrasings ("how should this be structured", "service accreting business rules", "reviewing an aggregate"), and explicitly disambiguates from model-lens, architect-lens, and code-review to prevent false-positive triggering.
- Output format (SKILL.md:142-165) is a concrete structured contract with file:line citations, per-lens sections, and a one-line verdict — exactly the pattern coding agents exploit well.
- Anti-padding discipline is explicit twice (SKILL.md:17-19, 167-168): skip non-applicable lenses, write "clean" rather than inventing findings — a high-leverage effectiveness instruction.
- Conditional lens scoping is built in: Lens 3 "only when it's a domain model" (line 77), Lens 5 flagged as EDA-critical for this repo (line 110) — judgment-driven where judgment is needed; no unnecessary scripts for what is inherently a model-driven review task.
- Validation cross-check (SKILL.md:50-53) — subdomain type vs. pattern mismatch as a signal — is a genuinely high-leverage heuristic stated once, imperatively.
- Repo-specific grounding (core-api, backend/common, gslogic, EventBridge/eda-worker) makes findings actionable rather than abstract.

**Recommendations:**
- [medium] **Deduplicate references/decision-trees.md down to its unique content** — The reference file restates nearly every lens already in SKILL.md: the decision tree (ref lines 6-35 vs SKILL.md 35-58), the architecture/testing mapping (ref 37-55 vs SKILL.md 60-75), the aggregate rules (ref 57-72 vs SKILL.md 77-94), value-object rules (ref 74-83 vs SKILL.md 96-108), event publishing (ref 85-105 vs SKILL.md 110-124), and migration signals (ref 107-122 vs SKILL.md 126-140). Since SKILL.md is the always-loaded file and is already operationally self-sufficient, slim the reference to only what it uniquely adds: the verbatim ASCII tree figure (ref 8-27), the "WHERE version = @expected" optimistic-lock example (ref 67-68), the "commands as methods vs command objects" note (ref 71-72), "identity by value, no synthetic id" (ref 83), the outbox NoSQL-embedding variant and 4-step relay sequence (ref 92-97), saga-vs-process-manager distinction detail (ref 99-104), the bidirectional-migration note (ref 121-122), and the book chapter/figure provenance. This roughly halves the reference and removes the cost of loading near-duplicate text when the agent does open it.
  - Knowledge preservation: Pure deduplication: every fact removed from the reference already exists verbatim or near-verbatim in SKILL.md; the eight unique items listed are explicitly retained in the slimmed reference. No information is deleted from the skill as a whole.
- [medium] **Surface the bidirectional-migration fact in SKILL.md Lens 6** — The only operationally significant knowledge that exists solely in the reference is ref lines 121-122: "Migrations also run the other way (core → supporting) — then it's fine to simplify a domain model back down." SKILL.md Lens 6 (lines 126-140) covers escalation and "don't over-escalate" but never says de-escalation is a legitimate recommendation. Since the agent is unlikely to open the reference during a routine review, add one sentence to Lens 6, e.g. after line 140: "Migration also runs downward — if a subdomain demoted from core to supporting, recommend simplifying the domain model back down."
  - Knowledge preservation: Knowledge is copied/relocated from the reference into the always-loaded SKILL.md (one sentence, ~20 tokens); nothing is deleted. It can then be dropped from the reference as part of the dedup in recommendation 1, having moved to a more visible home.
- [low] **Give the reference pointer a "read when" condition** — SKILL.md's only pointer to the reference is the closing sentence (lines 168-169): "See references/decision-trees.md for the full trees, the aggregate/value-object checklist, and the book provenance." This tells the agent what is there but not when to load it. Rewrite as conditional guidance, e.g.: "Read references/decision-trees.md when the pattern classification is borderline (you need the verbatim trees/cross-checks), when reviewing aggregate code in depth (full checklists incl. optimistic-lock and outbox mechanics), or when citing book provenance." After the dedup in recommendation 1, the listed contents should match the slimmed file.
  - Knowledge preservation: Rewording only — the pointer still names everything the reference contains; no content is removed, the trigger conditions are added.
- [low] **Gather repo-specific notes into one labeled subsection** — Repo-specific guidance is scattered: "In this repo" paragraph about core-api/backend/common/gslogic (SKILL.md:55-58), the greenspark-aws/EventBridge/eda-worker framing of Lens 5 (SKILL.md:110-112), Amplify as a generic-subdomain example (SKILL.md:29-30), and the EventBridge mapping in the reference (ref line 105). This is valuable knowledge but mixes universal DDD doctrine with one codebase's layout, which slightly muddies the lenses and would break silently if the skill is reused elsewhere. Consolidate into a single "Repo mapping" subsection in SKILL.md (the Lens 1 and Lens 5 notes can each be a one-line pointer to it). Only do this if the skill is meant to be portable; if it is permanently bound to greenspark-aws, current placement is acceptable and this can be skipped.
  - Knowledge preservation: Pure relocation: all repo-specific facts (route shapes, where domain models belong, EventBridge/eda-worker/erp-sync-workflow mapping) move intact into one subsection that each lens references; nothing is deleted.

## design-integrity — minor-improvements
Footprint: Single file: SKILL.md, 85 lines / 693 words / ~900-1000 tokens. No reference files, scripts, agents, or templates.

A lean, well-structured guidance-only skill that encodes Brooks-style design discipline as operating rules, a 7-step workflow, a pre-finalization checklist, a structured output contract, and anti-pattern gotchas. At ~900 tokens in one file, it needs no progressive disclosure and is appropriately model-driven throughout (no mechanical steps that would benefit from scripts). The main weaknesses are an over-broad frontmatter description whose bare-noun triggers (feature, API, refactor, module) risk firing on nearly every coding request, and some duplication between Operating Rules and the Workflow that scatters the concrete threshold for when alternatives are required.

**Strengths:**
- Excellent context-window economy: 85 lines / ~900 tokens total, single file, no progressive-disclosure machinery needed and none added — right-sized for a pure-judgment skill.
- Strong LLM-leverage patterns: numbered operating rules, a 10-item pre-finalization checklist (SKILL.md:52-65), a named-field output contract (SKILL.md:67-78), and explicit anti-patterns in Gotchas (SKILL.md:80-85).
- Good scope guards in both directions: the description's negative trigger ('Do not use for tiny mechanical edits unless the design choice is the problem', SKILL.md:3) plus Gotcha 1 ('Do not turn every small bug fix into an architecture exercise') prevent over-application; checklist item 3 explicitly allows skipping the alternatives step for small changes.
- Clear escalation condition: Operating Rule 7 (SKILL.md:20) states precisely when to ask the user (product intent, operational risk, tradeoffs the codebase cannot reveal) rather than a vague 'ask if unsure'.
- Workflow is correctly model-driven: every step (frame problem, discover constraints, search design space, choose, validate) is genuine judgment work; there are no mechanical steps that should have been scripted, so the absence of scripts is correct, not an omission.
- Output contract handles both modes (planning/review vs. while-implementing) with a deliberately lighter contract for the implementing case (SKILL.md:78), avoiding ceremony where it would slow work down.

**Recommendations:**
- [high] **Tighten frontmatter triggers to design-intent phrasings to reduce false positives** — SKILL.md:3 lists bare nouns as triggers: 'architecture, API, data model, module, UI flow, feature, refactor...'. Words like 'feature', 'API', 'refactor', and 'module' appear in almost every coding request, so the skill will be a candidate match for routine implementation tasks the skill itself says to avoid (Gotcha 1). Rephrase triggers as intent-bearing phrases: 'design or review an architecture, API surface, data model, or module boundary', 'evaluate a refactor's shape', 'compare design alternatives/tradeoffs', 'design before code', keeping the strong unique triggers (Brooks, The Design of Design, conceptual integrity, system shape) verbatim and keeping the existing negative trigger sentence.
  - Knowledge preservation: Every existing trigger concept (architecture, API, data model, module, UI flow, feature, refactor, design review, Brooks, The Design of Design, tradeoffs, alternatives, system shape, design before code) is retained in the rewritten description — only contextualized into phrases so they match design requests rather than any request mentioning the noun. The 'do not use' clause is kept unchanged.
- [medium] **Move the concrete 'when alternatives are required' list from Operating Rule 5 into Workflow step 4** — Operating Rule 5 (SKILL.md:18) holds the concrete trigger list for comparing alternatives ('architecture, data flow, public APIs, persistence, permissions, UI workflow, or cross module contracts'), while Workflow step 4 (SKILL.md:36-38) uses the vague threshold 'meaningful changes', and checklist item 3 (SKILL.md:58) restates the same rule a third time. Relocate the concrete list into step 4 as the definition of 'meaningful' ('Produce at least two plausible solution shapes when the change affects architecture, data flow, public APIs, persistence, permissions, UI workflow, or cross-module contracts'), delete Rule 5, and keep checklist item 3 as the verification echo. This removes one of three statements of the same rule and puts the precise threshold exactly where the agent needs it.
  - Knowledge preservation: The trigger list is moved verbatim into Workflow step 4, not deleted. The rule itself survives in two places (workflow step + checklist item 3) instead of three, with no loss of any condition.
- [low] **Consolidate the two 'when to involve the user' instructions** — Operating Rule 7 (SKILL.md:20, when to ask for input) and Gotcha 4 (SKILL.md:85, do not ask until tradeoffs are explained in practical terms) govern the same interaction but live in sections 60 lines apart. Merge Gotcha 4's condition into Rule 7: 'Ask for user input only when a decision depends on product intent, operational risk, or a tradeoff the codebase cannot reveal — and only after explaining the tradeoff in practical terms.' Then drop Gotcha 4. This gives one complete, unambiguous escalation rule instead of two partial ones.
  - Knowledge preservation: Both conditions (the three escalation triggers from Rule 7 and the explain-tradeoffs-first requirement from Gotcha 4) are merged into a single sentence; no condition is dropped, only co-located.
- [low] **Trim the preamble to one sentence** — Lines 8-10 spend two paragraphs on stance-setting ('make software design work deliberate before implementation momentum hardens the wrong shape' and the Brooks-inspired framing). The four Brooks principles in line 10 (discovers requirements, searches alternatives, protects conceptual integrity, judgment under constraints) are each already operationalized as Workflow steps 1, 4 and Operating Rules 1, 2. Compress to one sentence that names the Brooks lineage and the goal, e.g. 'Brooks-inspired design discipline: shape the design deliberately before implementation momentum hardens the wrong shape.' Saves ~40 tokens of restated content on every activation.
  - Knowledge preservation: The Brooks attribution and the 'before momentum hardens the wrong shape' framing are kept in the compressed sentence; the four enumerated principles are pure restatements of Operating Rules 1-2 and Workflow steps 1 and 4, so deduplication loses no unique knowledge.

## design-patterns — minor-improvements
Footprint: SKILL.md 84 lines / ~1,313 words / ~1.7k tokens; references/patterns.md 284 lines / ~2,954 words / ~3.9k tokens (loaded on demand); plus a stray 6KB .DS_Store. Total ~5.6k tokens, of which only ~1.7k is always-loaded.

This is a well-architected knowledge skill that gets the fundamentals right: a lean always-loaded SKILL.md that acts as a triage layer (guardrails, principles, symptom-to-pattern routing table, output contract), with the heavy 23-pattern catalog correctly relegated to references/patterns.md behind explicit "read only the relevant entries" guidance. The anti-overengineering stance is the skill's core value and is consistently enforced — every catalog entry has a mandatory "Overkill when" section. Remaining issues are small: a slightly redundant frontmatter description, minor duplication between the guardrail section and the output contract, one catalog entry (Compound/MVC) unreachable from the routing table, and a .DS_Store artifact.

**Strengths:**
- Correct progressive disclosure: SKILL.md is a routing/triage layer (~1.7k tokens) and the full catalog lives in references/patterns.md with explicit load guidance at SKILL.md:73 — "Read only the entries relevant to the situation; it's organized so you can jump to one pattern."
- The symptom → pattern table (SKILL.md:44-71) is exactly the right LLM-leverage structure: concrete pressure phrasings ("sprawling if/else", "new ConcreteThing() scattered") mapped to candidates, doubling as an index into the reference file.
- Strong anti-overengineering guardrails placed first (SKILL.md:10-19), reinforced by a uniform "Overkill when" section in every one of the 23 catalog entries — this counteracts the most likely failure mode (pattern-happy recommendations).
- Clear output contract (SKILL.md:75-84) forcing pressure → simplest option → pattern → cost, with an explicit "none needed is a success" escape hatch.
- Frontmatter description triggers well: covers user phrasings ("which pattern fits here", "make this extensible", "this is getting hard to change"), symptom descriptions, and includes a negative trigger ("Do NOT use for pure bug-fixing, formatting...").
- references/patterns.md has a consistent entry schema (Intent / Reach for it when / Structure / Overkill when / Related), an anchor-linked contents list (lines 12-16), and disambiguation between commonly confused patterns (Strategy vs State, Decorator vs Proxy vs Adapter).

**Recommendations:**
- [medium] **Add a routing row for Compound/MVC to the symptom table** — references/patterns.md:216-224 has a full Compound/MVC entry, but the symptom → pattern table in SKILL.md:48-71 has no row that routes to it — it is the only catalog entry unreachable from the triage layer. Add one row, e.g. "Interactive app where data, presentation, and input handling are tangled → Compound/MVC (Observer + Strategy + Composite)".
  - Knowledge preservation: Pure addition of a pointer; no existing content is touched. The MVC knowledge already in patterns.md becomes discoverable through the same routing mechanism as every other entry.
- [low] **Tighten the frontmatter description by deduplicating trigger phrasings** — The description (SKILL.md:3) is ~120 words and repeats near-identical triggers: "which pattern fits here" / "is there a pattern for this" / "what pattern" / "design pattern", and "refactoring for extensibility" / "make this extensible" / "hard-to-extend classes". Collapse each cluster to one representative plus the literal keyword (e.g. keep "which pattern fits here", "design pattern", named-pattern mentions, "make this extensible", the symptom list, and the Do-NOT clause). Saves ~30-40 always-loaded words across every session with no loss of trigger coverage.
  - Knowledge preservation: Deduplication only — each distinct trigger concept (asking for a pattern, naming a specific GoF pattern, extensibility pressure, rigidity symptoms, the negative trigger) keeps at least one representative phrasing; no concept is dropped.
- [low] **Deduplicate the cost/none-is-valid guidance between guardrail and output sections** — SKILL.md:19 ("the honest answer... is sometimes none... A pattern recommendation should always come with the cost it imposes") restates what the Output shape section already encodes at lines 81-84 (the "Pattern (if warranted)" and "The cost" bullets, plus "'none needed' is a success"). Also, step 3 of "How to use this lens" (line 27, "Confirm the pattern earns its complexity") repeats the guardrail at line 15-16. Keep the operative rules once in the Output shape contract and shrink line 19 to a one-line pointer. Saves ~50-60 words.
  - Knowledge preservation: Consolidation, not deletion: every unique rule (always state cost; "none" is a valid and successful answer; pattern must earn complexity) survives in exactly one canonical location, the Output shape section, which is the part the agent acts on when producing a recommendation.
- [low] **Remove .DS_Store from the skill directory** — /Users/robson/Development/rar-skills/design-patterns/.DS_Store (6KB) is a macOS Finder artifact. Delete it and add .DS_Store to the repo .gitignore so packaged/distributed skills stay clean.
  - Knowledge preservation: The file contains no skill knowledge — it is Finder metadata; removal loses nothing.

## dynamic-harness — minor-improvements
Footprint: SKILL.md 157 lines / ~1.9k tokens; 1 supporting file (agents/openai.yaml, 4 lines / ~50 tokens, host-consumed metadata not loaded into model context). Total skill directory: 161 lines.

A well-constructed orchestration skill: lean at ~1.9k tokens, almost entirely imperative, with strong structured contracts (Workflow Brief template, Agent Design schema, output report checklist), explicit budget caps, and clear stop/escalation conditions. The main flaws are a project-specific paragraph (GreenSpark AWS, line 36) hard-coded into an otherwise portable skill, mild duplication between "First Move" step 4 and the "Runtime Adaptation" section, trigger-phrase overlap with the sibling deep-research and diagnose skills, and an undocumented agents/openai.yaml file that SKILL.md never mentions. None of these require restructuring — the skill is close to good-as-is.

**Strengths:**
- Lean single-file design: at 157 lines / ~1.9k tokens, the whole skill fits comfortably in context without needing progressive disclosure; no bloated reference files.
- Strong structured contracts the model can exploit: Workflow Brief template (lines 22-34), Agent Design assignment schema (lines 77-87), and a numbered Final Response report contract (lines 149-156).
- Explicit numeric guardrails rather than vague prose: budget tiers (lines 130-136), hard caps of 16 concurrent / 1000 total agents framed as bounds not goals (line 128), and mandatory pre-loop definitions of progress signal, stop condition, and escalation condition (lines 123-128).
- Good failure-mode hygiene: line 42 forbids inventing native workflow function names, line 15 mandates honesty when no subagents were spawned, line 146 requires stating exactly what was not verified, and lines 94-96 cover quarantine of untrusted content and keeping destructive actions in the main agent.
- Decision guidance is judgment-driven where it should be (workflow type selection, lines 45-71, with composition advice) and mechanical where it should be (templates, caps, report format).
- Frontmatter description enumerates concrete user phrasings ($dynamic-harness, tournament, fan out and synthesis, adversarial verification, etc.) giving strong recall for the intended invocations.

**Recommendations:**
- [high] **Extract the GreenSpark-specific paragraph out of SKILL.md** — Line 36 ("For GreenSpark AWS tasks, obey root and nested AGENTS.md, start topic discovery from docs/REPO_KNOWLEDGE_MAP.md... npm run local:env... local:verify... fixture backed reproduction") is project-specific knowledge embedded in a generic, portable orchestration skill. It is loaded on every invocation in every repo, and it sits mid-section inside "Workflow Brief" where it is unrelated to the brief template above it. Move it to /Users/robson/Development/rar-skills/dynamic-harness/references/greenspark.md (or better, into the GreenSpark repo's own AGENTS.md) and replace it with one conditional line: "If working in the GreenSpark AWS repo, read references/greenspark.md first."
  - Knowledge preservation: All four facts (AGENTS.md obedience, REPO_KNOWLEDGE_MAP.md entry point, local:env/local:verify command order, fixture-first reproduction policy) are moved verbatim to a reference file loaded on demand; SKILL.md keeps an explicit read-X-when-Y pointer so the knowledge remains reachable.
- [medium] **Merge First Move step 4 into the Runtime Adaptation section** — Line 15 (tool-search for subagent tools, serial fallback, no-fake-parallelism rule) and lines 40-43 (Runtime Adaptation: Codex multi_agent_v1 tools, Claude Code native workflows, no invented function names) both cover runtime/tool discovery. Consolidate into a single "Runtime" section: discovery (line 15) -> Codex path (line 40) -> Claude Code path (lines 41, 43) -> fallback and honesty rules (lines 15, 42). First Move then shrinks to four steps about the task itself, which is what that section is for.
  - Knowledge preservation: Pure relocation and deduplication: every rule (tool search invocation, serial emulation disclosure, named Codex tools, native workflow preference rationale, no invented API names, workflow-artifact placement) survives in the merged section; only the duplicated discovery instruction is collapsed.
- [medium] **Disambiguate frontmatter triggers that collide with sibling skills** — The description (SKILL.md line 3) lists "deep research" and "root cause analysis" as bare triggers, but this repo also ships a deep-research skill and a diagnose skill that own those phrasings. A user saying "do a root cause analysis on this bug" likely wants diagnose, not a multi-agent harness. Qualify the overlapping phrases, e.g. "multi agent deep research" and "root cause analysis at scale / with competing hypotheses", keeping all other triggers as-is.
  - Knowledge preservation: No trigger concept is removed; the overlapping phrases are narrowed with qualifiers so the skill still activates for harness-shaped requests while ceding single-agent phrasings to the skills that own them.
- [low] **Document the purpose of agents/openai.yaml** — SKILL.md never references /Users/robson/Development/rar-skills/dynamic-harness/agents/openai.yaml, and the file itself has no comment explaining what consumes it (it appears to be Codex host interface metadata: display_name, short_description, default_prompt). Add a one-line YAML comment at the top of the file (e.g. "# Codex agent interface metadata; consumed by the Codex host, not read at runtime by the model") so future auditors know it is host config, not an orphaned reference file the agent should be told to read.
  - Knowledge preservation: Additive only: the existing four lines are untouched; a comment records currently-implicit knowledge about who consumes the file.
- [low] **Optionally collapse the triple-stated evidence-over-confidence rule** — The same principle appears three times: line 57 ("Give verifiers the rubric and evidence, not the worker's confidence"), line 115 (judges compare outputs "not agent reputations or verbosity"), and line 141 ("Never accept a worker output only because it is confident"). Each instance is short and contextually placed, so this is borderline; if tightening, keep the canonical statement in Synthesis Rules (line 141) and reduce the other two to brief role-specific clauses. Skip this if it would make any section less self-contained.
  - Knowledge preservation: Deduplication only: the canonical rule remains in Synthesis Rules, and the verifier-specific (rubric+evidence inputs) and judge-specific (no reputation/verbosity bias) nuances are kept as short clauses in their phases, so no distinct guidance is lost.

## explain-architecture — minor-improvements
Footprint: SKILL.md 115 lines / ~820 words / ~1.1k tokens; 1 reference file (references/viewpoints.md) 112 lines / ~690 words / ~0.9k tokens; total ~2k tokens

A compact, well-engineered skill: a strong trigger description with explicit anti-trigger ("for REVIEWING use model-lens"), a clear 5-step workflow, a concrete output skeleton, and genuine progressive disclosure into one reference file. The main weakness is that greenspark-aws-specific knowledge (service names, layer mapping, seam examples, worked example) is interleaved with the generic method in SKILL.md, so every invocation — including on unrelated repos — pays for a branch that only applies to one codebase. There is also light duplication of diagram guidance between the two files and a late, weakly-timed pointer to the reference file.

**Strengths:**
- Excellent frontmatter description: covers natural user phrasings ("lay of the land", "what talks to what", "onboard me") and includes an explicit disambiguation against the sibling model-lens skill, reducing false-positive triggering.
- Lean overall footprint (~2k tokens total) with real progressive disclosure: per-audience templates, the worked example, and skill-boundary notes live in references/viewpoints.md, not SKILL.md.
- Deterministic-where-needed, model-driven-where-needed: explicit default (newcomer + whole-repo), "ask at most one short question", "ground every claim in files you actually read", cite as file:line, and subagent fan-out for large scopes.
- Concrete output contract: the Step 3 skeleton (SKILL.md:71-95) is a checklist-style template the agent can fill, plus prose rules that prevent scope creep (one idea per explanation, don't editorialize).
- Step 4 stop condition ("offer a diagram, don't auto-generate") prevents a common failure mode of unsolicited heavy artifacts.

**Recommendations:**
- [medium] **Extract greenspark-aws-specific knowledge into a repo reference file** — SKILL.md mixes the generic method with greenspark-aws specifics: the service list in Step 0 (SKILL.md:24-25), the seam examples in Step 1 items 3 and 5 (EventBridge, shared common/TypeORM, RTK Query, OpenAPI codegen, org-scoping; SKILL.md:42-48), and the full layer mapping in Step 2 (SKILL.md:57-63) with the escape hatch "for a non-greenspark repo, derive the equivalent layers" (SKILL.md:64-65). Create references/greenspark.md holding the service list, layer mapping, seam inventory, and the worked example currently at references/viewpoints.md:62-91. In SKILL.md keep generic phrasing (e.g. "HTTP routes, message buses/events, shared data layer, cron/workers") plus one line: "If working in greenspark-aws, read references/greenspark.md for the known service map and layer mapping." This trims SKILL.md by ~15-20 lines, makes the skill portable across the user's repos, and turns the rare branch into an on-demand load.
  - Knowledge preservation: Pure relocation: every service name, layer assignment, seam example, and the worked example move verbatim into references/greenspark.md; SKILL.md gains an explicit read-X-when-Y pointer so the knowledge remains reachable exactly when relevant.
- [low] **Deduplicate diagram guidance between SKILL.md Step 4 and viewpoints.md** — SKILL.md:106-113 and references/viewpoints.md:95-103 ("Diagram options") state the same three options (inline Mermaid, visual-explainer:generate-web-diagram, visual-explainer:project-recap) and the same rule ("offer; never auto-generate") twice. Keep the canonical version in SKILL.md Step 4 and fold in the one unique detail from viewpoints.md — `graph LR` for structure vs `sequenceDiagram` for flows — then delete the viewpoints.md section.
  - Knowledge preservation: Deduplication with merge: the only unique content in the viewpoints.md copy (the Mermaid diagram-type hint) is merged into SKILL.md Step 4 before the duplicate section is removed; no information is lost.
- [low] **Move the viewpoints.md pointer to Step 0 with read-when guidance** — The only pointer to references/viewpoints.md is the last line of SKILL.md (line 115), after the agent has already executed Steps 0-4 in reading order. Relocate it into Step 0 as an imperative: "Once audience is fixed, read references/viewpoints.md for that viewpoint's template and length guidance before exploring." This makes the progressive disclosure actionable at the moment the choice is made, rather than an afterthought the agent may skip.
  - Knowledge preservation: The pointer is moved, not removed; viewpoints.md content is untouched and becomes more reliably consumed because the read instruction now sits at the decision point it serves.
- [low] **Trim the triple-stated model-lens boundary to two mentions** — The "this skill explains, model-lens judges" boundary appears three times: frontmatter description (SKILL.md:3), Step 3 rules (SKILL.md:103-104), and references/viewpoints.md:109. The frontmatter mention is load-bearing for trigger disambiguation and Step 3's "don't editorialize" rule is load-bearing for behavior; the viewpoints.md "Boundaries with neighboring skills" section should keep only its unique content (improve-codebase-architecture and init boundaries, viewpoints.md:110-113) and drop the repeated model-lens line, or reduce it to a cross-reference.
  - Knowledge preservation: Deduplication only: the model-lens boundary remains stated in the two places where it changes behavior (triggering and output rules); the unique neighboring-skill boundaries (improve-codebase-architecture, init) are kept verbatim.

## frontend-design — minor-improvements
Footprint: SKILL.md 46 lines / ~4.7 KB / ~1.1k tokens; LICENSE.txt 177 lines (Apache 2.0, never loaded into context); no scripts, references, agents, or templates

A lean, well-targeted creative-guidance skill (derived from Anthropic's upstream frontend-design skill) that fits comfortably in context at ~1.1k tokens with no progressive-disclosure needs. Its guidance is concrete and high-leverage (banned fonts, banned purple-gradient cliches, specific motion/typography directives) and is appropriately model-driven for a judgment-heavy task. The one real defect is a structural editing artifact: an "Execution Contract" block was wedged between the "## Frontend Aesthetics Guidelines" heading (SKILL.md:27) and its body (line 33), leaving a dangling empty heading and an under-specified output contract. A couple of mild duplications could be tightened, but the skill needs no restructuring beyond that.

**Strengths:**
- Excellent context-window economy: single ~1.1k-token SKILL.md, no reference files needed for a judgment-driven creative task — nothing to progressively disclose
- Strong frontmatter description: covers the user phrasings that should trigger it (websites, landing pages, dashboards, React components, HTML/CSS layouts, styling/beautifying any web UI) without false-positive bait
- High-leverage anti-pattern guidance LLMs genuinely need: explicit bans on Inter/Roboto/Arial/system fonts, purple-gradient-on-white, and convergence on common choices like Space Grotesk across generations (SKILL.md:40-42)
- Correctly model-driven throughout — no mechanical steps exist that would warrant scripts, so the absence of scripts is a feature, not a gap
- Concrete, actionable aesthetic directives (e.g. staggered page-load reveals via animation-delay beat scattered micro-interactions; Motion library for React; CSS-only for HTML) rather than vague exhortations to 'be creative'

**Recommendations:**
- [high] **Fix the dangling heading: relocate the misplaced Execution Contract block** — SKILL.md:27 ('## Frontend Aesthetics Guidelines') is immediately followed by SKILL.md:28 ('## Execution Contract'), leaving the first heading with an empty body and splitting the aesthetics section from its 'Focus on:' content at line 33. This reads as a merge/insertion artifact (the surrounding text matches the upstream Anthropic skill verbatim). Move the Execution Contract block (lines 28-31) to its own properly placed section — e.g. after 'Design Thinking' or at the end as a final 'Deliverables' section — so 'Frontend Aesthetics Guidelines' flows directly into 'Focus on:'.
  - Knowledge preservation: Pure relocation — all four contract lines (runnable code, accessibility notes, verification commands, tradeoffs; degraded fallback artifact; desktop/mobile summary) are moved intact to a correctly positioned section; the aesthetics section regains its body with zero text loss.
- [medium] **Convert the Execution Contract into an explicit output checklist** — SKILL.md:29-31 is ambiguous as written: 'Include desktop/mobile behavior summary as mandatory field' references a 'field' but no output format is defined anywhere; 'degraded fallback artifact when requested framework/tooling is unavailable' is terse to the point of ambiguity (fallback to what — plain HTML/CSS?). Rewrite as a structured contract, e.g. 'Every delivery must include: 1) runnable code, 2) accessibility notes, 3) commands to run/verify it, 4) tradeoffs made, 5) a one-paragraph desktop vs. mobile behavior summary. If the requested framework/tooling is unavailable, ship a working plain HTML/CSS/JS fallback and say so.' Checklists with numbered items are followed far more reliably than prose contract fragments.
  - Knowledge preservation: All five contract requirements are retained verbatim in meaning; the change only restructures them into an enumerated checklist and resolves the undefined 'mandatory field' reference by stating the expected output shape explicitly.
- [low] **Deduplicate the repeated 'avoid generic AI aesthetics' and 'intentionality over intensity' statements** — The anti-generic-AI message appears three times: frontmatter description (line 3), intro (line 7: 'avoid generic AI slop aesthetics'), and line 40 ('NEVER use generic AI-generated aesthetics...'). Similarly, line 19 ('Bold maximalism and refined minimalism both work - the key is intentionality, not intensity') and line 44 ('Match implementation complexity to the aesthetic vision... Elegance comes from executing the vision well') make the same point twice. Keep the strongest instance of each (the concrete NEVER list at line 40; the complexity-matching paragraph at line 44, which adds the maximalist-needs-elaborate-code / minimalist-needs-restraint detail) and trim the intro at line 7 to one sentence. Saves roughly 60-80 tokens.
  - Knowledge preservation: Deduplication only: each unique fact (the banned-pattern list, the complexity-matching rule, the maximalism/minimalism parity point) survives in its single strongest location; only literal restatements are removed.
- [low] **Resolve trigger overlap with the bundled frontend-design plugin skill** — Outside the directory but observed during the audit: the skill list exposes both this skill ('frontend-design') and a plugin skill ('frontend-design:frontend-design') with near-identical names and descriptions, differing only in that the local one adds 'artifacts, posters' and 'styling/beautifying any web UI'. Two skills with the same trigger surface create nondeterministic activation. Either uninstall/disable one, or differentiate this skill's description (it is the superset) and note in SKILL.md that it supersedes the plugin copy.
  - Knowledge preservation: No content in this directory changes; if the plugin copy is disabled, this local SKILL.md already contains a superset of its trigger phrasings and guidance, so no knowledge is lost.

## full-review — minor-improvements
Footprint: SKILL.md 276 lines / ~2.4k tokens; 9 reference files 968 lines / ~7.5k tokens total (largest: review_output_schema.json 198, bug_finders.md 160, panel_roles.md 150, filtering_pipeline.md 120, conditional_specialists.md 118); 2 scripts 170 lines; agents/openai.yaml 4 lines. Whole skill ~1,418 lines / ~11k tokens, but progressive disclosure means only ~2.4k loads on trigger.

This is a well-engineered orchestration skill: SKILL.md is a lean phase-driven controller, heavy persona/specialist/pipeline material is correctly pushed into references loaded per-phase, mechanical steps are scripted, and the trigger description is rich without bait. The real problems are internal consistency defects rather than structure: four different confidence thresholds (0.5, 0.60, 0.65, 0.70) disagree across SKILL.md and filtering_pipeline.md, the findings-file env var contract differs between SKILL.md ($FINDINGS_DIR/<seat>.json) and bug_finders.md ($FINDINGS_FILE), the JSON schema types `evidence` as a string while the merge pipeline treats it as an array, and one reference file (github_comment_format.md) is never pointed to. Fixing these is low-cost and high-leverage; no structural overhaul is needed.

**Strengths:**
- Excellent progressive disclosure: SKILL.md is a 276-line orchestrator with explicit 'read references/X' pointers at the exact phase each is needed (structural_quality_review.md in Phase 2/3, bug_finders.md and panel_roles.md in Phase 3, filtering_pipeline.md in Phase 5).
- Frontmatter description covers a wide, realistic set of user phrasings (PR review, bughunt, ultrareview, security review, maintainability audit, review my branch) with no false-positive bait.
- Deterministic/model-driven split is right: collect_context.sh and diff_line_map.py handle mechanical context gathering and diff parsing; judgment phases (intent gate, structural review, verification) stay model-driven with concrete evidence bars.
- bug_finders.md False Positive Awareness list (lines 13-35) is rare, high-value distilled knowledge that directly suppresses the most common LLM review failure mode.
- Strong structured-output contracts: JSON schema with source enums, a worked corroboration example in filtering_pipeline.md (line 74), explicit stop condition ('If the diff is empty, say so and stop', SKILL.md line 106), and graceful-degradation rules for missing external runners (lines 189-191).
- Quick mode is defined once as a compact delta over the full workflow (SKILL.md line 65) instead of a duplicated parallel path.

**Recommendations:**
- [high] **Unify the four conflicting confidence thresholds into one source of truth** — SKILL.md line 38 sets `confidence_threshold` default 0.65; filtering_pipeline.md section 4 (lines 84-86) says Default 0.60 / Quiet 0.70; SKILL.md line 35 sets quiet_mode default true (making the pipeline's 'Default 0.60' row unreachable and 0.65 never used); the Confidence Rubric (SKILL.md line 267) says suppress below 0.5; quick mode raises to 0.8 (line 65) but quick mode is absent from the pipeline's threshold table. Pick one canonical table (filtering_pipeline.md section 4 is the natural home), add quick-mode and the knob-override semantics to it, and make SKILL.md's knob row and Phase 5 step 5 say 'threshold per references/filtering_pipeline.md section 4'. Reword the rubric's 'Below 0.5: Suppress' row to clarify it is a scoring band, not a second filter, or fold it into the same table.
  - Knowledge preservation: No threshold knowledge is deleted: all modes (default, quiet, quick, explicit knob) move into the single filtering_pipeline.md table; SKILL.md keeps the knob name and a pointer. The 0.5 rubric band is preserved as scoring guidance with its relationship to filtering made explicit.
- [high] **Fix the findings-file contract mismatch between SKILL.md and bug_finders.md** — SKILL.md Phase 3 (line 153) instructs each seat to write `$FINDINGS_DIR/<seat-name>.json`, but bug_finders.md line 152 says 'Write output to `$FINDINGS_FILE`' — an env var the skill never defines. A sub-agent given only bug_finders.md has no resolvable path. Change bug_finders.md to either name the convention explicitly ('write to $FINDINGS_DIR/<source-field-value>.json') or state that the orchestrator passes the concrete file path in the seat prompt, and make SKILL.md say which.
  - Knowledge preservation: Pure deduplication/alignment: the file-naming convention already in SKILL.md becomes the single contract restated identically in bug_finders.md; nothing is removed.
- [medium] **Resolve evidence string-vs-array conflict between schema and pipeline** — review_output_schema.json lines 79-81 type `evidence` as a string, but filtering_pipeline.md merge rule 2 (line 63) says 'Combine `evidence` arrays from both comments (deduplicate identical strings)'. A faithful merger produces JSON that fails the schema. Either change the schema to `array of string` (preferred — merging is core to the design) or change the pipeline rule to 'concatenate evidence strings with a separator'.
  - Knowledge preservation: Both the merge semantics and the schema field survive; only the type declaration is corrected so the two documents agree.
- [medium] **Add a 'read when' pointer for the orphaned github_comment_format.md** — references/github_comment_format.md (7 lines of inline-comment guidance) is never referenced anywhere — SKILL.md Phase 6 line 258 discusses inline review output and Codex `::code-comment` directives but does not point to it. Add to Phase 6: 'When mirroring findings as GitHub inline comments, follow references/github_comment_format.md.' Alternatively, since it is only 7 lines, inline it as 3 bullets in Phase 6 and delete the file.
  - Knowledge preservation: Either option keeps all five guidance bullets: as a referenced file with explicit load guidance, or relocated verbatim into the Phase 6 delivery section.
- [medium] **Deduplicate the verified/corroborated confidence-boost rules** — The +0.10 verified boost and +0.05 per corroborating source appear in SKILL.md Confidence Rubric (line 269) and again in filtering_pipeline.md (lines 5-8 and 70-74). Keep the operational definition in filtering_pipeline.md (where it is applied, with the worked example) and reduce SKILL.md line 269 to 'Verification and corroboration boosts are applied per references/filtering_pipeline.md.'
  - Knowledge preservation: The numeric rules remain fully specified in filtering_pipeline.md including the worked example; SKILL.md keeps a pointer so the knowledge stays discoverable from the rubric.
- [low] **Compress duplicated 'be ambitious / code judo' stance and structural criteria in SKILL.md** — The identical two-sentence stance appears in SKILL.md Phase 2 (line 135), panel_roles.md Maintainer Gardener (line 91), structural_quality_review.md (lines 7-11), and external_prompt_template.md rule 7. The external-template copy is justified (external models cannot read files), but SKILL.md line 135 and panel_roles.md line 91 both sit next to an instruction to read structural_quality_review.md, which already opens with the same stance. Similarly, SKILL.md Phase 5 line 231 (structural findings allowed when...) and Phase 4 line 213 (indicator list) paraphrase structural_quality_review.md's 'What To Flag' and 'Evidence Requirements'. Trim SKILL.md lines 135, 213, and 231 to one-line pointers ('apply the blocking bar / evidence requirements in references/structural_quality_review.md'). Saves roughly 120-150 tokens from the always-loaded file.
  - Knowledge preservation: structural_quality_review.md is established as the canonical full text (it already contains every duplicated sentence and criterion); SKILL.md and panel_roles.md retain pointers, and the intentional copy in external_prompt_template.md is kept because external runners receive the prompt verbatim.
- [low] **Replace SKILL.md Phase 1 manual git command list with a script-fallback pointer** — SKILL.md lines 99-104 spell out the exact git/gh commands per input type, duplicating what scripts/collect_context.sh already encodes (its usage text documents all four modes). Since line 90 already says 'Prefer scripts/collect_context.sh', compress lines 99-104 to: 'If the script does not cover the input, replicate the equivalent commands for the matching mode in scripts/collect_context.sh (pr/commit/range/local); for branch review find the merge base, falling back from main to master.' Saves ~80 tokens from the always-loaded file.
  - Knowledge preservation: Every command survives verbatim inside collect_context.sh (lines 49-89), which the agent is already directed to; the only unique fact in the prose — the main-to-master fallback — is kept in SKILL.md explicitly since the script defaults to main without fallback.

## gemini-runner — minor-improvements
Footprint: SKILL.md 105 lines / ~900 tokens (loaded on trigger); scripts/run_gemini.py 586 lines / ~4.5k tokens (executed, never loaded); agents/openai.yaml 4 lines / ~40 tokens. Total directory ~695 lines.

gemini-runner is a lean, well-factored runner skill: a ~900-token SKILL.md that documents the CLI contract, and a deterministic Python wrapper that handles all mechanical work (prompt assembly, role overlays, fallback chain, envelope normalization). The main defects are a block of pasted author requirement-notes at the top of the Runtime Compatibility section that read as tasks rather than documentation, a reference to a shared envelope schema file that does not exist anywhere in the repo, and one undocumented script flag (--output-file). These are fixable with small, knowledge-preserving edits; no structural rework is needed.

**Strengths:**
- Excellent progressive disclosure: all mechanical logic (role overlay text, fallback chain, envelope normalization, auth detection) lives in scripts/run_gemini.py and is never loaded into context; SKILL.md only documents the contract.
- Deterministic-where-it-should-be: fallback routing, seat_unavailable handling, atomic JSON output, and envelope field normalization are all scripted (run_gemini.py:48-81, 312-370), not left to model judgment.
- Clear stop/honesty rule at SKILL.md:19 ('Never claim the Gemini/Google seat participated when a fallback provider produced the output') — a high-leverage guardrail for consensus workflows.
- Concrete, copy-pasteable examples (SKILL.md:72-80) covering plain prompts, roles, session continuation, and --agy-continue.
- Frontmatter description triggers on the right phrasings (Gemini execution, Antigravity CLI, consensus Gemini seat) with the 'agy installed' qualifier to limit false positives.
- Return-code table (SKILL.md:91-99) and required-envelope-keys list (SKILL.md:28-37) give callers an unambiguous output contract.
- Honest documentation of CLI limitations: SKILL.md:52-53 and :89 explicitly state that --model and --output-format are metadata-only hints because agy print mode does not support those flags — prevents agents from assuming model selection works.

**Recommendations:**
- [high] **Replace the pasted requirement-notes at SKILL.md:11-14 with the one fact they contain that is documented nowhere else** — Lines 11-14 ('Requirement: full fallback order including qwen and kimi seats. Standardize envelope fields: runner, effective_runner, fallback_reason, auth_ok. Keep disable-fallback strict and explicit. Document seat-unavailable behavior for councils.') are author TODO/spec notes pasted into the doc, not agent instructions. An agent triggered on this skill may misread 'Document seat-unavailable behavior for councils' as a task to perform. Three of the four lines duplicate content already in SKILL.md: the fallback order is in step 3 (line 18), the envelope fields are in Output Envelope (lines 29-37), and --disable-fallback is in the Options table (line 59). The only unique knowledge is seat-unavailable behavior, which the script implements (run_gemini.py:78-80 sets status='seat_unavailable' on return_code -2, and the fallback loop at :333-345 skips unavailable seats, recording them in fallback_attempts) but SKILL.md never documents. Replace the four note lines with one factual sentence, e.g.: 'When agy is missing and fallback is disabled (or all fallbacks are unavailable), the envelope carries status: seat_unavailable and return_code -2; council orchestrators must treat that seat as absent. Skipped fallback seats are listed in fallback_attempts.'
  - Knowledge preservation: Three of four lines are exact duplicates of step 3, the Output Envelope key list, and the Options table — removed via deduplication. The fourth (seat-unavailable semantics) is converted from a vague note into explicit documentation of behavior already implemented in run_gemini.py:78-80 and :333-354, so net knowledge increases.
- [medium] **Fix the dangling schema reference at SKILL.md:28** — SKILL.md:28 says all --json responses must conform to '.agents/skills/_shared/runner-envelope.schema.json', but no _shared directory or runner-envelope.schema.json exists anywhere in /Users/robson/Development/rar-skills (verified with find). Every sibling runner skill repeats the same dangling reference, so fix it repo-wide: either (a) actually create _shared/runner-envelope.schema.json from the normalize_envelope contract in run_gemini.py:48-81 and keep the pointer, or (b) drop the schema path and keep only the required-keys list at SKILL.md:29-37, which already states the contract. Option (a) is preferable since 8 runner skills duplicate the same 9-line key list — a single shared schema would deduplicate across the suite.
  - Knowledge preservation: Option (a) relocates the envelope contract into a real shared schema file (single source of truth); option (b) keeps the existing inline key list. Either way no field knowledge is lost — the current state is the lossy one, since the referenced file does not exist.
- [medium] **Document the --output-file flag in the Options table** — run_gemini.py:529-534 implements --output-file (atomic JSON write via write_json_output_file at :88-101), and :561-571 shows it changes --json stdout to a compact pointer object {success, return_code, output_file} instead of the full envelope. SKILL.md's Options table (lines 47-59) omits it entirely. This is a high-value flag for council orchestrators that want to keep large stdout out of their context window. Add one table row: '--output-file | Write the full JSON envelope atomically to this path; with --json, stdout becomes a compact {success, return_code, output_file} pointer | None'.
  - Knowledge preservation: Pure addition — surfaces existing script capability into SKILL.md; nothing removed.
- [low] **Deduplicate the Behavior section against Options and Output Envelope** — Behavior items 3-5 (SKILL.md:86-88) restate facts already present: item 3 repeats the Options table rows for --prompt-file/--session-file/--agy-continue/--role; item 4 repeats the Output Envelope required keys; item 5's first half repeats envelope semantics. Keep the unique facts — the exact command shape (line 84), 'does not request a permission bypass' (line 85), the runner=gemini / effective_runner=agy provenance rule (line 88), and the unsupported-flags list (line 89) — and delete the restatements. Saves roughly 5 lines and removes a drift hazard where two sections describing the same flags can diverge.
  - Knowledge preservation: Only restated content is removed; the four unique facts (command shape, no permission bypass, provenance rule, unsupported agy flags) are retained verbatim in the trimmed section.
- [low] **Align the argparse epilog example with the metadata-only --model semantics** — run_gemini.py:445 shows the epilog example '%(prog)s "Write a function" --model Gemini-3.5-Flash', which implies model selection works, contradicting the --model help text at :485-488 and SKILL.md:52 which state agy uses its configured model and the flag is metadata only. Change the example to demonstrate a flag that has real effect (e.g. --role or --session-file), or annotate it '(metadata label only)'.
  - Knowledge preservation: The accurate explanation of --model semantics already exists at run_gemini.py:485-488 and SKILL.md:52; the edit removes only a misleading example, not knowledge.
- [low] **Note the misleading agents/openai.yaml filename (repo-wide convention)** — /Users/robson/Development/rar-skills/gemini-runner/agents/openai.yaml contains correct Gemini content (display_name 'Gemini Runner (agy)') but is named openai.yaml. All 8 runner skills share this filename, so it appears to be a host-tool naming requirement (likely a Codex-app agent manifest). If the filename is mandated by the consuming tool, leave it; if not, rename to a provider-neutral name like agent.yaml across all runner skills in one commit to avoid confusion during maintenance.
  - Knowledge preservation: Rename only — file content (display name, description, default prompt) is unchanged.

## gemma-runner — minor-improvements
Footprint: SKILL.md 110 lines / ~850 tokens; scripts/run_gemma.py 22 lines (~150 tokens, thin shim over qwen-runner); agents/openai.yaml 4 lines. Total ~1k tokens — small, but ~65% of SKILL.md duplicates qwen-runner/SKILL.md verbatim.

gemma-runner is a well-conceived thin wrapper: a 22-line script that delegates to qwen-runner's verified implementation with a Gemma default model, keeping all mechanical logic deterministic and out of the model's hands. The SKILL.md has two real defects: an unfinished pasted-notes block (lines 87-91) that directly contradicts the documented and implemented no-fallback behavior, and heavy verbatim duplication of qwen-runner/SKILL.md (options, roles, envelope keys, return codes) that is already drifting (gemma omits --ephemeral that qwen lists). It also references a schema file that does not exist in the repo. Fixes are small and mostly involve deduplication and resolving the contradiction; the core design is sound.

**Strengths:**
- Deterministic thin-shim architecture: scripts/run_gemma.py is 22 lines that reuse qwen-runner's 621-line verified implementation via run_qwen.main(default_model=..., runner_name="gemma") — zero logic duplication in code, exactly the right script/model split.
- Frontmatter description triggers cleanly: covers explicit Gemma requests, 'Gemma seat' in workflows, and cross-runner councils, with no false-positive bait toward Qwen/GLM requests (those have their own runners).
- Structured output contract: the required envelope keys (runner, effective_runner, effective_model, effective_provider, auth_ok, fallback_reason, success, return_code) and the return-code table match the actual implementation in run_qwen.py, giving councils a comparable cross-runner contract.
- Honest Gotchas section (lines 107-110) flags that --bare/--safe are compatibility no-ops and --output-schema is prompt-enforced — exactly the kind of expectation-setting that prevents agent confusion.
- Already lean overall (~850 tokens) with concrete copy-pasteable examples using real flag combinations (lines 79-85).

**Recommendations:**
- [high] **Resolve the contradictory pasted-notes block under Behavior/Runtime Compatibility** — SKILL.md lines 87-91: '## Behavior' is an empty header immediately followed by '## Runtime Compatibility' containing raw requirement notes ('Define optional fallback policy when qwen/gemma auth unavailable. If fallback occurs, keep runner=gemma and set effective_runner to actual provider.'). This directly contradicts line 95 ('Never falls back to another provider') and the implementation: qwen-runner/scripts/run_qwen.py line 225 discards disable_fallback and line 77 emits status seat_unavailable — no fallback path exists. An agent reading lines 89-90 could wrongly expect or fabricate fallback semantics. Merge the unique facts into the numbered Behavior list as a single statement: 'Never falls back to another provider; if qwen or Gemma auth is unavailable, the envelope is returned with status seat_unavailable and fallback_reason set, and a failing Gemma smoke test should block the seat.' Delete the stray '## Runtime Compatibility' header. Note: glm-runner/SKILL.md lines 87-91 and qwen-runner/SKILL.md lines 34-37 contain the same class of pasted-notes defect and should get the same treatment.
  - Knowledge preservation: All unique facts in the notes (seat_unavailable envelope on missing auth, runner identity stays 'gemma', envelope must carry fallback_reason/effective_model/effective_runner) are merged into the Behavior list and the existing Output Envelope section; only the contradictory 'if fallback occurs' phrasing — which describes behavior the code does not implement — is corrected, not lost.
- [medium] **Replace duplicated qwen-runner reference material with a pointer (progressive disclosure)** — SKILL.md lines 45-66 (20-flag options list), 68-77 (roles list), 26-37 (envelope keys), and 98-105 (return-code table) are verbatim copies of qwen-runner/SKILL.md lines 43-64, 68-77, 20-31, and 95-102. qwen-runner is already a hard runtime dependency (run_gemma.py imports its module), so replace these with: 'Options, roles, output envelope, and return codes are inherited from qwen-runner — read .agents/skills/qwen-runner/SKILL.md when you need flag details.' Keep only Gemma-specific deltas inline (default model, the no-fallback/seat-blocking rule, the two Gotchas). This cuts SKILL.md to roughly 40 lines and eliminates the drift already visible: gemma's list omits --ephemeral which qwen-runner line 59 documents — either a real difference that must be stated explicitly, or copy-rot that this change eliminates. Apply the same pattern to glm-runner and minimax-runner, which carry identical duplicated blocks.
  - Knowledge preservation: No knowledge is deleted: the canonical copy of every list lives in qwen-runner/SKILL.md, which the shim already depends on at runtime, and SKILL.md gains an explicit 'read X when Y' pointer. The --ephemeral discrepancy is resolved by stating it explicitly rather than leaving silent divergence between two copies.
- [medium] **Fix or bundle the dangling envelope schema reference** — SKILL.md line 28 requires conformance to '.agents/skills/_shared/runner-envelope.schema.json', but no _shared directory or runner-envelope.schema.json exists anywhere in this repo (verified with find). Either add the schema file under a repo-level _shared/ directory so the contract is machine-checkable, or annotate the line to say the inline key list is authoritative when the schema file is absent. Same dangling reference exists in qwen-runner/SKILL.md line 22 and glm-runner/SKILL.md line 28.
  - Knowledge preservation: The envelope contract (the 8 required keys) already exists inline and in run_qwen.py's _normalise_envelope behavior; bundling the schema file adds a verifiable artifact, and the annotation option keeps the existing inline list as the preserved source of truth.
- [low] **Document the consumer of agents/openai.yaml or its role** — /Users/robson/Development/rar-skills/gemma-runner/agents/openai.yaml (4 lines of display metadata: display_name, short_description, default_prompt) is never referenced from SKILL.md and has no documented consumer. Add one line to SKILL.md (or a repo-level README note covering all runner skills) stating which harness reads agents/*.yaml, so future maintainers know it is load-bearing rather than dead.
  - Knowledge preservation: The file itself is untouched; the change only adds a sentence recording its purpose, converting implicit tribal knowledge into written knowledge.

## glm-runner — minor-improvements
Footprint: SKILL.md 110 lines / ~800 tokens; scripts/run_glm.py 22 lines / ~150 tokens; agents/openai.yaml 4 lines / ~40 tokens. No reference files. Total ~1k tokens, with only SKILL.md loaded on trigger.

glm-runner is a well-built thin-wrapper skill: a 22-line script that delegates to the shared qwen-runner implementation with a GLM default model, and a lean ~800-token SKILL.md. The main defects are a malformed double-heading section ("Behavior" / "Runtime Compatibility", lines 87-92) containing leftover spec-note fragments that contradict the numbered Behavior list, a pointer to a schema file (.agents/skills/_shared/runner-envelope.schema.json) that does not exist anywhere in this repo, and option-list drift versus the shared script (missing --ephemeral). The heavy duplication of options/roles/return-codes across the five Qwen-backed runner SKILL.md files is a maintenance liability already showing drift, though each individual file stays small.

**Strengths:**
- Code-level simplicity is exemplary: run_glm.py is 22 lines and delegates entirely to qwen-runner/scripts/run_qwen.py, so all behavior has a single implementation source.
- SKILL.md is already lean (~800 tokens) and uses LLM-friendly structures throughout: option checklist, return-code table, three concrete invocation examples with absolute-style paths, and a Gotchas section flagging non-obvious flag semantics (--bare/--safe are no-ops, --output-schema is prompt-enforced).
- Frontmatter description triggers cleanly: explicit GLM request, GLM seat in a workflow, cross-runner provider selection — parallel with sibling runner skills, low false-positive risk.
- Required envelope keys are listed inline (lines 29-37), so the agent does not need to load the external schema file to produce conforming output.
- Clear stop condition for orchestrators: "Never falls back to another provider. A failing GLM smoke test should block the seat" (line 95) and the provenance rule "Never claim GLM participation when fallback provider produced output" (line 90).

**Recommendations:**
- [high] **Fix the malformed Behavior / Runtime Compatibility section (SKILL.md lines 87-92)** — Lines 87-88 are two stacked headings ("## Behavior" immediately followed by "## Runtime Compatibility") with no content between them, followed by editing-note fragments: "Requirement: optional fallback policy and provenance requirements." This fragment contradicts line 95 ("Never falls back to another provider") and reads as pasted spec notes, not instructions. Merge into a single "## Behavior" section: keep the existing numbered list (lines 93-96) and convert the two real rules into list items, e.g. "5. Never claim GLM participation when a fallback provider produced output." and "6. Always include fallback_reason and effective_model in --json output." Delete only the orphan heading and the self-contradicting "Requirement:" fragment. (The sibling qwen-runner/SKILL.md lines 34-37 has the same pasted-notes defect in its Usage section — worth fixing in the same pass.)
  - Knowledge preservation: Both substantive rules (no-false-GLM-attribution provenance rule; fallback_reason + effective_model required in output) are kept verbatim as numbered Behavior items. Only the duplicate heading and the contradictory "optional fallback policy" fragment — which conflicts with the authoritative line 95 — are removed; the authoritative never-fallback rule remains.
- [medium] **Resolve the dead schema reference (SKILL.md line 28)** — SKILL.md says all --json responses must conform to .agents/skills/_shared/runner-envelope.schema.json, but no _shared directory or runner-envelope.schema.json exists anywhere in /Users/robson/Development/rar-skills (verified with find). Either (a) add the schema file to a _shared directory in this repo so the path resolves when skills are installed, or (b) reword line 28 to make the inline key list normative, e.g. "The wrapper --json envelope always contains these top-level keys:" — the keys are real (verified in run_qwen.py lines 58-77). Option (b) is simpler and removes a broken progressive-disclosure pointer.
  - Knowledge preservation: All eight required envelope keys are already enumerated inline at lines 30-37 and stay in SKILL.md unchanged; only the unreachable file pointer is replaced (or made real). No envelope knowledge is lost — the inline list becomes the single source of truth, matching what run_qwen.py actually emits.
- [low] **Sync the Supported Options list with the shared script: add --ephemeral (SKILL.md lines 47-66)** — run_qwen.py (lines 520-524) accepts --ephemeral as a compatibility alias for --no-session-persistence, and qwen-runner/SKILL.md lists it (line 59), but glm-runner/SKILL.md omits it even though run_glm.py inherits the full argparse. Add "--ephemeral (alias for --no-session-persistence)" to the list. This is drift evidence, not just a gap: the five Qwen-backed runner SKILL.md files each hand-copy the option list.
  - Knowledge preservation: Pure addition — restores an existing script capability that the doc lost; nothing is removed.
- [medium] **Deduplicate the inherited sections against qwen-runner via an explicit read-on-demand pointer** — Lines 45-66 (Supported Options), 68-77 (Roles), 98-105 (Return Codes), and part of Gotchas are near-verbatim copies of qwen-runner/SKILL.md, and identical copies presumably exist in gemma-runner, minimax-runner, and kimi-runner. Line 47 already says "The GLM runner inherits the verified Qwen runner options" — lean into that: replace the four duplicated blocks with one line, "Options, roles, return codes, and wrapper-envelope semantics are identical to qwen-runner — read .agents/skills/qwen-runner/SKILL.md when you need flag details beyond the examples below," keeping only GLM-specific deltas in glm-runner (default model glm-5.1, never-fallback seat-blocking rule, the --bare/--safe transport gotcha). This cuts ~35 lines (~250 tokens) per derived runner and eliminates the drift class shown by the missing --ephemeral. Caveat: only do this if the runner skills are always installed as a set; if glm-runner can be installed standalone, keep the option names as a bare checklist and point to qwen-runner only for semantics.
  - Knowledge preservation: Every option, role, and return code remains fully documented in qwen-runner/SKILL.md (the canonical wrapper the script literally imports), and argparse --help on run_glm.py exposes the same list at runtime. GLM-unique facts (default model, no-fallback seat rule, transport gotchas, provenance rule) stay in glm-runner/SKILL.md. Nothing is deleted from the corpus — it is relocated to a single canonical file with an explicit "read X when Y" pointer.

## kimi-runner — minor-improvements
Footprint: SKILL.md 120 lines / ~1.0k tokens (loaded on trigger); scripts/run_kimi.py 634 lines / ~4.5k tokens (executed, never needs reading); agents/openai.yaml 4 lines (host-consumed). No reference files.

kimi-runner is a well-structured runner skill: a lean ~1k-token SKILL.md that fully shields the agent from the 634-line wrapper script via an options list, examples, behavior contract, return-code table, and gotchas. The main defect is the Prerequisites section (SKILL.md lines 16-21), which contains leftover authoring/spec scratch text ("Requirement: auth smoke test command...") instead of clean instructions — a pattern that also appears in sibling runners. Two smaller issues: the documented "auth failed -> seat_unavailable" contract is only implemented for the missing-binary case in the script, and the referenced shared envelope schema file does not exist anywhere in the repo. Everything else is appropriately simple and deterministic.

**Strengths:**
- Excellent progressive disclosure: the agent never needs to read the 634-line script; SKILL.md's Supported Options (lines 49-73), Examples (lines 87-95), Behavior (lines 97-105), Return Codes table (lines 107-113), and Gotchas (lines 115-120) cover everything needed to drive it.
- Frontmatter description triggers precisely: covers 'Kimi execution', 'Kimi-backed seat', 'Moonshot Kimi perspective' with no false-positive bait, and is consistent with the seven sibling runner descriptions.
- Deterministic script for the mechanical work: argument validation, prompt assembly (roles, restrict-tools overlay, session/metadata/schema sections), envelope normalization, atomic --output-file writes, and distinct return codes (-1 timeout, -2 missing CLI, -3 invalid input) are all in run_kimi.py, not delegated to model judgment.
- Honest documentation of limitations: no-op compatibility flags are explicitly called out (line 74), --restrict-tools is labeled prompt-level not a sandbox (lines 27, 119), --output-schema labeled prompt-enforced (line 118), and the resume-hint quirk is documented (line 117).
- Hard no-fallback contract is explicit (Behavior item 5, line 103) and matches the script (normalize_envelope sets status=seat_unavailable on return_code -2).

**Recommendations:**
- [high] **Rewrite the corrupted Prerequisites section (SKILL.md lines 16-21)** — Lines 17-20 are leftover requirement/spec notes pasted mid-document: 'Requirement: auth smoke test command and explicit blocked response contract. / If kimi-cli missing/auth failed: success=false, reason=seat_unavailable. / Include remediation commands in failure output. / Preserve disable-fallback semantics.' This is authoring scratch text, not agent instructions, and it loads into context on every trigger. Replace with the clean two-bullet form used by sibling qwen-runner ('kimi-cli installed and in PATH', 'Authentication configured via kimi-cli login') plus one explicit failure-contract line, e.g.: 'If kimi-cli is missing or auth fails: the envelope returns success=false with status seat_unavailable and remediation guidance in stderr; the seat is blocked, never substituted.' Note the same scratch-text pattern exists in qwen-runner's Usage section (lines 34-37) — fix family-wide.
  - Knowledge preservation: All four scratch lines encode real contract knowledge; each is folded into proper prose: the seat_unavailable contract becomes the explicit failure-contract sentence, 'remediation commands in failure output' is already implemented in the script's stderr messages ('Check if kimi-cli is installed and in PATH', run_kimi.py lines 344, 407) and stated in the new sentence, and 'preserve disable-fallback semantics' is already covered by Behavior item 5 (line 103) and Supported Options line 74 — pure deduplication, nothing deleted.
- [medium] **Align the auth-failure contract between SKILL.md and run_kimi.py** — SKILL.md (lines 18 scratch text and Behavior line 103) claims auth failures block the seat with seat_unavailable, but run_kimi.py has no auth probe: only shutil.which('kimi-cli')/FileNotFoundError produce return_code -2 and status=seat_unavailable (normalize_envelope, lines 74-75). An auth failure surfaces as kimi-cli's own nonzero exit with auth_ok=None (normalize_envelope lines 60-67), not seat_unavailable. Either (a) add a cheap auth smoke test in run_kimi.py before the main run that maps auth failure to return_code -2 / status seat_unavailable (this is exactly what the scratch 'Requirement' line asked for), or (b) document the actual behavior in SKILL.md: '-2 / seat_unavailable covers missing CLI; auth failures return kimi-cli's native exit code with auth_ok unset — treat any success=false as a blocked seat.'
  - Knowledge preservation: Option (a) implements the documented contract so no docs change; option (b) replaces an inaccurate claim with an accurate description of the same failure semantics — the no-silent-fallback rule and seat-blocking intent are retained verbatim in Behavior item 5.
- [low] **Resolve the dangling envelope schema reference (SKILL.md line 32)** — '.agents/skills/_shared/runner-envelope.schema.json' is referenced by all eight runner SKILL.md files but the file does not exist anywhere in this repository (verified with find). If the schema is materialized by the install pipeline, add a parenthetical '(provided by the skills install root)'; otherwise bundle it once in a repo-level _shared/ directory so the reference resolves. The inline list of eight required keys (lines 34-42) currently does all the real work, so this is robustness, not a functional break.
  - Knowledge preservation: The eight required envelope keys stay listed inline in SKILL.md; adding the actual schema file or a provenance note adds information rather than removing any.
- [low] **Deduplicate flag caveats between Supported Options and Gotchas** — The no-op compatibility flags are explained twice: SKILL.md line 74 ('--ephemeral, --no-session-persistence, --safe, --bare, --disable-fallback ... do not currently change Kimi CLI behavior') and Gotchas line 120 ('--no-session-persistence is accepted for parity, but ... still records resumable session metadata'). Similarly --restrict-tools is described in Security Model (line 27) and again in Gotchas (line 119). Keep one home each: line 74 covers the parity flags generically; move the Kimi-specific session-persistence detail there and drop the Gotcha, and keep the restrict-tools 'not a hard sandbox' statement only in Security Model with Gotchas pointing to it. Saves ~4 lines; trim only if touching the file for the line 16-21 fix anyway.
  - Knowledge preservation: Pure deduplication: each unique fact (parity flags are inert; Kimi still records session metadata in print mode; restrict-tools is a prompt overlay, not a sandbox) keeps exactly one authoritative location — no fact is deleted, only its second copy.

## minimax-runner — minor-improvements
Footprint: SKILL.md 110 lines / ~850 tokens; scripts/run_minimax.py 22 lines (thin shim delegating to qwen-runner/scripts/run_qwen.py, 621 lines, never loaded into context); agents/openai.yaml 4 lines. No reference files.

A lean, well-shaped runner skill: the script is a 22-line shim over the shared qwen-runner implementation, and SKILL.md stays under 1k tokens with good LLM-leverage patterns (return-code table, envelope key contract, explicit no-fallback rule). The main defects are leftover requirement-tracking notes pasted under "## Prerequisites" (lines 17-20) that read as spec backlog rather than instructions, a reference to a schema file that does not exist anywhere in the repo, a documented "smoke test" mechanism the code does not implement, and ~60 lines of wrapper documentation duplicated nearly verbatim across at least four sibling runner skills (qwen, glm, gemma, minimax).

**Strengths:**
- Thin delegation pattern: scripts/run_minimax.py is 22 lines that reuse qwen-runner's run_qwen.py, so there is one implementation source of truth and the skill adds only the Minimax identity and default model.
- Frontmatter description triggers well: covers explicit Minimax requests, council/workflow seats, and cross-runner selection, and is clearly differentiated from the glm/gemma/qwen sibling descriptions with no false-positive bait.
- Good LLM-leverage structures: Return Codes table (lines 98-105), required envelope key checklist (lines 33-41), concrete runnable examples (lines 85-89), and an explicit stop condition ('Never falls back to another provider... block the seat', line 95).
- Honest Security Model section (line 27) stating what data leaves the machine.
- SKILL.md is already small enough that no progressive-disclosure split is strictly required.

**Recommendations:**
- [high] **Remove leftover requirement-tracking notes from the Prerequisites section** — SKILL.md lines 17-20 ('Requirement: qwen/minimax auth smoke test and explicit blocked response. / Differentiate missing_binary vs auth_failed vs timeout in return semantics. / Output must include effective_provider metadata even in non-fallback mode. / Document council seat accounting behavior.') are development spec notes, not prerequisites or agent instructions. The same artifact pattern appears in sibling skills (qwen-runner SKILL.md under '## Usage', glm-runner under '## Runtime Compatibility'), confirming these are accidental paste-ins from a requirements doc. They confuse the agent reading 'Prerequisites' and waste tokens.
  - Knowledge preservation: Three of the four lines are already documented elsewhere in the file: missing_binary/auth_failed/timeout distinction is the Return Codes table (lines 98-105) plus auth_ok in the envelope (line 38); effective_provider-always-present is line 37; smoke-test/blocked-seat is Behavior item 3 (line 95). The one unique item, 'council seat accounting behavior', should be converted into a real documented sentence in the Behavior section (e.g. 'When the seat is blocked, report it as unavailable so councils can account for the missing seat — never substitute another provider') and the raw requirement lines relocated to a repo-level backlog/TODO file outside any SKILL.md. Nothing is deleted; each line becomes either a pointer to existing text or a proper behavior statement.
- [high] **Deduplicate wrapper documentation shared with qwen-runner via a read-on-demand pointer** — Supported Options (lines 49-70), Roles (lines 72-81), Output Envelope (lines 30-41), Return Codes (lines 98-105), and Gotchas (lines 107-110) are duplicated nearly verbatim in qwen-runner/SKILL.md, glm-runner/SKILL.md, and gemma-runner/SKILL.md (verified by diff — only the provider name differs). Since run_minimax.py already hard-depends on qwen-runner/scripts/run_qwen.py via sys.path import, the doc dependency is honest. Replace those five sections with one line: 'Options, roles, output envelope, return codes, and gotchas are identical to the shared wrapper — read .agents/skills/qwen-runner/SKILL.md when you need flag or envelope details.' Keep only the Minimax deltas: default model, prerequisites, security note, no-fallback Behavior, usage, and examples. This cuts SKILL.md from ~110 to ~45 lines and fixes the multi-file drift risk (the Gotchas wording has already diverged slightly between minimax and glm).
  - Knowledge preservation: All removed content exists verbatim in /Users/robson/Development/rar-skills/qwen-runner/SKILL.md, which becomes the single source of truth; the pointer includes explicit read-when guidance so the knowledge remains reachable on demand. If full standalone portability is required instead, the alternative is a shared reference file (e.g. _shared/runner-common.md) that all runner skills point to — either way every list item survives in exactly one location.
- [medium] **Fix the dangling envelope schema reference** — SKILL.md line 32 says all --json responses must conform to '.agents/skills/_shared/runner-envelope.schema.json', but no _shared directory or schema file exists anywhere in the repo (verified with find). The actual envelope contract lives in qwen-runner/scripts/run_qwen.py. Either ship the schema file at that path, or rewrite line 32 to make the inline key list (lines 33-41) the authoritative contract, e.g. 'The --json envelope (produced by qwen-runner/scripts/run_qwen.py) always includes these top-level keys:'. The same broken path appears in all 8 runner skills, so fix it consistently.
  - Knowledge preservation: The required-keys checklist (lines 33-41) already encodes the full contract inline and is kept as-is; only the unreachable file path is replaced with a reference to the real producer (run_qwen.py) or made real by creating the schema file. No envelope knowledge is lost either way.
- [medium] **Align Behavior item 3 with what the code actually does (no smoke test exists)** — SKILL.md line 95 says 'A failing Minimax smoke test should block the seat', but grep shows no smoke-test logic in run_qwen.py or run_minimax.py — failures surface only as return codes (-1/-2/-3) and auth_ok in the envelope. Reword to describe observable behavior the agent can act on: 'If the run fails (non-zero return_code or auth_ok=false), treat the Minimax seat as blocked and report it unavailable — never substitute another provider.' If a pre-run smoke test is actually wanted, that belongs in run_qwen.py, tracked via the requirement note relocated per recommendation 1.
  - Knowledge preservation: The no-fallback / blocked-seat rule is fully preserved and made more precise by tying it to real signals (return_code, auth_ok). The aspirational smoke-test idea is preserved by moving the 'Requirement: qwen/minimax auth smoke test' line into the backlog location chosen in recommendation 1 rather than presenting unimplemented behavior as fact.
- [low] **Verify the agents/openai.yaml consumer and filename** — /Users/robson/Development/rar-skills/minimax-runner/agents/openai.yaml holds interface metadata (display name, short description, default prompt) under a filename suggesting an OpenAI-host integration convention. SKILL.md never mentions it and nothing in this repo reads it. Confirm which host consumes agents/openai.yaml; if it is a required convention (it appears in sibling skills too), leave it but note the consumer in a one-line comment inside the YAML; if nothing consumes it, it is 4 lines of dead weight to relocate.
  - Knowledge preservation: The 4 lines of metadata are kept in place (or moved with a documented consumer note); the recommendation is verification plus an explanatory comment, not deletion.

## model-lens — minor-improvements
Footprint: SKILL.md 115 lines / ~890 words / ~1.2k tokens; 1 reference file (references/review-rubric.md) 95 lines / ~880 words / ~1.2k tokens; ~2.4k tokens total

model-lens is a lean, well-structured review skill: four clearly-scoped lenses, a concrete output contract, an explicit anti-fabrication guard, and book provenance correctly pushed into a reference file. The main gaps are workflow-critical instructions (how to pick the review target, the report-only/no-auto-edit rule) living only in the on-demand rubric, a hard-coded greenspark-aws layer mapping that breaks portability in this multi-project skills repo, and the "skip clean lenses" instruction stated three times across the two files. All fixes are small relocations and deduplications — no knowledge needs to be cut.

**Strengths:**
- Frontmatter description is excellent: enumerates the four lenses, covers likely user phrasings (review architecture, where code belongs, layering/boundaries, module doing too much, dependency direction, change getting too big), and includes explicit anti-trigger disambiguation vs architect-lens and code-review — both of which actually exist as siblings.
- Strong output contract: SKILL.md lines 92-111 give a literal template with per-lens sections, file:line citation format, and a one-line verdict — exactly the structured-output pattern agents exploit well.
- Anti-fabrication guard ('skip lenses that don't apply', 'write clean — don't invent findings') prevents the classic padding failure mode of review skills.
- Good progressive disclosure instinct: book provenance, chapter citations, and expanded per-lens checklists live in references/review-rubric.md, keeping SKILL.md focused on the operational lenses.
- Concrete over abstract throughout: every lens grounds the book's idea in real code smells (pricing in a Fastify handler, EventBridge payloads in domain code, handleAndSyncAndNotify, utils dumping ground) rather than restating theory.
- No dead files: the single reference is pointed to from SKILL.md line 114, and the cross-referenced skills (architect-lens, clean-code, full-review) all exist.

**Recommendations:**
- [high] **Move the workflow steps and report-only stop condition from the rubric into SKILL.md** — references/review-rubric.md lines 81-87 ('How to run a review') contain the only statement of (a) how to determine the review target (git diff vs branch vs described plan) and (b) the critical stop condition 'this lens reports; it does NOT auto-edit unless the user asks for fixes'. These are needed on every invocation, but live in a file the agent may never open. Move these 5 short steps into SKILL.md (e.g. right after the intro at line 16), replacing the partially-overlapping prose there ('Apply the lenses, then report findings grouped by lens...').
  - Knowledge preservation: Pure relocation: the 5-step procedure and the no-auto-edit rule move verbatim from review-rubric.md to SKILL.md; the rubric section is removed only after its content lands in SKILL.md, with overlapping intro prose in SKILL.md lines 14-16 merged into the steps so nothing is stated twice.
- [medium] **Add a portability fallback to the hard-coded greenspark-aws layer mapping** — SKILL.md lines 18-31 hard-code the layer mapping to one repo (backend/common, gslogic, core-api, dispatch, eda-worker, Amplify), and the rubric checklists reference the same paths plus domain terms like 'weight/SOLAS' (rubric line 27). In this multi-project skills repo the skill will be triggered in other codebases where these paths don't exist. Add one sentence after line 31: 'In other repos, infer the equivalent mapping first (domain/entities = business; routes/services/UI state = application; infra/DB/cloud config = technology) and state it before reviewing.' Keep the greenspark mapping as the worked example.
  - Knowledge preservation: The repo-specific mapping is kept in full as the canonical example; the change is purely additive (one fallback sentence), so all existing repo knowledge (paths, SOLAS, lifecycle rules) is preserved.
- [medium] **Make the rubric-read trigger explicit and load-bearing** — SKILL.md line 114 says 'See references/review-rubric.md for the detailed checklist and the book provenance' — a passive pointer. The rubric's checklists contain unique actionable rules not in SKILL.md (migrations carry schema only, EventBridge boundaries not bypassed by direct calls, abstractions need >=2 real callers, no HTTP status codes in domain logic), so a review run without it loses coverage. Rewrite as an imperative read-X-when-Y instruction: 'Before walking the lenses, read references/review-rubric.md for the per-lens checklists; cite its provenance table when a finding is challenged.'
  - Knowledge preservation: No content changes in either file beyond rewording one pointer sentence; the unique checklist knowledge stays in the reference file and becomes reliably reachable instead of optionally discovered.
- [low] **Deduplicate the 'skip clean lenses' instruction to a single statement** — The same rule appears three times: SKILL.md lines 15-16 ('Skip lenses that don't apply rather than padding'), SKILL.md line 113 ('If a lens is clean, write clean — don't invent findings'), and review-rubric.md line 85 ('Drop lenses with nothing to say — do not manufacture findings'). Keep the strongest single formulation once in SKILL.md (the line-113 version, since it sits next to the output template and specifies the literal output 'clean'), and drop the other two occurrences.
  - Knowledge preservation: Deduplication of an identical rule: the retained formulation ('write clean — don't invent findings') subsumes all three phrasings, so no unique information is lost — note that if the rubric's 'How to run a review' section moves per recommendation 1, step 3 is the duplicate to drop there.
- [low] **Consolidate the triple-stated scope disclaimer** — What-this-skill-is-NOT appears in three places: the frontmatter ('Distinct from architect-lens... and code-review...'), SKILL.md lines 9-12 ('not a bug hunt and not a coupling/connascence audit'), and review-rubric.md lines 89-95 (the full NOT section, which adds clean-code and full-review). Keep the frontmatter version (required for trigger disambiguation) and the rubric's NOT section (the most complete, with all four sibling-skill redirects), and shrink SKILL.md lines 9-12 to the single core question sentence ('does this code sit in the right place, do one job, depend in the right direction, and stay scoped?') plus a pointer to the rubric's NOT section.
  - Knowledge preservation: The most complete version (rubric lines 89-95, covering code-review, full-review, architect-lens, clean-code redirects) is kept intact; only the middle, least-complete restatement in SKILL.md is collapsed into a pointer, so every redirect and the core-question framing survive.

## models-consensus — minor-improvements
Footprint: SKILL.md ~501 lines / ~5.8k tokens; 3 reference files (runner-invocations.md 251 lines ~2.2k tokens, stance-rotation-schedule.md 57 lines ~800 tokens, repo-configuration.md 57 lines ~550 tokens); agents/openai.yaml 4 lines (~80 tokens). Total ~9.4k tokens, of which ~5.8k loads on trigger.

A well-architected skill with genuine progressive disclosure: invocation commands, stance schedules, and repo-specific config live in reference files with clear "see X for Y" pointers, and SKILL.md keeps the orchestration logic. Output contracts, degradation rules, and the preflight discipline are concrete and high-leverage. The main weakness is internal duplication inside SKILL.md — the startup seat-selection/interactive-question rules are stated 4-5 times, response schemas twice, --disable-fallback three times, and artifact-mode behavior twice — which inflates the always-loaded file by an estimated 100-150 lines. All fixes are deduplication and relocation; no unique knowledge needs to be deleted.

**Strengths:**
- Real progressive disclosure: all 9 per-seat CLI invocation blocks, auth rules (--bare, Qwen transport), and the output envelope contract are in references/runner-invocations.md, loaded only when launching runner seats; SKILL.md points to it with explicit context (lines 12, 18, 92, 162).
- Strong structured contracts: required JSON response fields per round, seat table schema (SKILL.md lines 110-121), convergence classifications with heuristic definitions (lines 399-403), confidence bands, and a numbered output contract (lines 482-489).
- Clear stop/escalation conditions: awaiting_user_decision states (lines 33, 427), explicit stop conditions (lines 449-455), and a moderator time budget with cutoff behavior (lines 289-292).
- Graceful degradation is exhaustive and table-like (lines 273-292): every failure mode (missing seat, fallback, shared provider, malformed output) has a deterministic rule, and line 137/499 forbids fabricating missing seats — the key failure mode for this kind of skill.
- Frontmatter description triggers well: covers "multi-model validation", "consensus review", "multiple AI perspectives", and decision/architecture phrasings without false-positive bait; it is also distinct from the sibling council skill.
- Host-agnostic design is handled once in a reference (runner-invocations.md Host Tool Mapping table) rather than scattered per-section.

**Recommendations:**
- [high] **Merge the duplicated startup-selection block in Preflight step 0** — SKILL.md lines 27-33 are a telegraphic restatement of lines 35-71 (same rules: resolve selection before smoke tests, plain-text fallback with selection_source=plain_text_manual, only auto-select on --auto, stop with awaiting_user_decision). This reads like a patched-in summary stacked on the original. Merge into one block: keep the detailed workflow (lines 35-71) and fold in the three facts unique to the summary (selection_source=plain_text_manual, emit selected/omitted seats before smoke tests, awaiting_user_decision stop state).
  - Knowledge preservation: Pure deduplication: the three unique facts in lines 27-33 are folded into the surviving detailed workflow; everything else in those lines is verbatim-equivalent to lines 35-71 and is removed only as a duplicate.
- [high] **Consolidate interactive-question rules into the single Interactive Questions section** — The interactive-tool-first rule is stated five times: SKILL.md line 20 (Host Tool Compatibility), lines 45-46 and 65 (preflight step 0), lines 241-271 (Interactive Questions), line 425 (Handle disagreement), and runner-invocations.md lines 27-28. Keep the full ladder (detect tools, prefer multi-select, fallback order, never plain-text out of convenience, host mappings) only in the Interactive Questions section; replace the other four occurrences with one-line pointers like "use the Interactive Questions protocol". The startup-question specifics currently split between preflight step 0 and lines 264-271 should live in exactly one of those two places.
  - Knowledge preservation: All rules survive in the canonical Interactive Questions section; duplicated restatements become cross-references. The startup-prompt templates (multi-select, single-choice preset, plain-text) are kept once, verbatim.
- [medium] **State response schemas once; have Discussion Protocol reference Schema Validation** — The Round-1 field list appears identically at SKILL.md lines 214-220 and 355-362; the later-rounds list at lines 222-228 and 384-389. Keep the lists in Response Schema Validation (where retry/malformed_output behavior lives) and replace the Discussion Protocol copies with "include the Round-1 schema from Response Schema Validation" — or vice versa. Saves ~16 lines and removes a future drift hazard where one copy gets edited and the other does not.
  - Knowledge preservation: Field lists preserved verbatim in one location; the second occurrence becomes a reference, so no field name or ordering is lost.
- [medium] **Fold the Runner Invocation Summary section into Preflight + the reference file** — SKILL.md lines 154-171 mix three things: (a) a terse restatement of envelope validation already covered in Response Schema Validation (lines 156, 230-233), (b) --disable-fallback mandate already at line 91 and runner-invocations.md line 220, and (c) genuinely unique content — the privacy note about prompt context being sent to providers, the --restrict-tools / no-bypass-flags policy, and the key-flags list (lines 160, 164-170). Keep only (c), either as a short 'Runner Launch Policy' block or moved into runner-invocations.md's Auth and Transport Rules; delete the restatements. Lines 155-158 in particular are telegraphic patch-style prose inconsistent with the rest of the file.
  - Knowledge preservation: Unique content (privacy disclosure, --restrict-tools preference, permission-bypass prohibition, key-flag list, inline-vs-persisted prompt passing) is retained, relocated next to the invocation patterns it governs; only verbatim-duplicated mandates are removed.
- [medium] **Move Cost Governance and Crash Recovery to a references/operations.md loaded on demand** — Cost Governance (SKILL.md lines 173-189) only matters at seat-selection time with >4 seats, and Crash Recovery (lines 191-207) only matters when a prior state file exists. Move both to references/operations.md and leave two one-line triggers in SKILL.md: "When >4 seats are selected, read references/operations.md#cost-governance before launching" and "If state_path exists with status != complete, read references/operations.md#crash-recovery and resume". Trims ~30 always-loaded lines. While doing so, fix an effectiveness gap: the cost formula at line 177 asks for a dollar estimate but supplies no per-1k pricing data — either add a rough pricing table to the new reference or reword the warning to token counts so the instruction is executable.
  - Knowledge preservation: Both sections move verbatim to the reference file; SKILL.md keeps explicit read-when-Y triggers so the branches remain reachable. The pricing fix adds knowledge rather than removing any.
- [low] **Move the stance-to-runner-role mapping fully into stance-rotation-schedule.md** — SKILL.md lines 323-329 map stances to runner roles, but stance-rotation-schedule.md's Round 1 table (lines 9-19) already carries a Runner Role column with the same pairs. Keep the mapping in the reference (adding the one pair unique to SKILL.md: blocked_on_context -> researcher) and shrink the Council Model section to the stance list plus the existing pointer. Saves ~8 lines from the always-loaded file.
  - Knowledge preservation: The blocked_on_context -> researcher mapping (unique to SKILL.md) is added to the reference file before removal; all other pairs already exist in the reference's tables.
- [low] **Update agents/openai.yaml default_prompt to match the current seat catalog** — agents/openai.yaml line 4 names only "Claude Opus 4.8, Claude Sonnet 5.0, Codex, Gemini, and GLM-backed seats" — it predates the Kimi, Gemma, and Minimax seats present in the frontmatter, repo-configuration.md, and stance schedule. Either list all eight or say "all detected seats" to avoid future drift.
  - Knowledge preservation: Pure correction/addition; no knowledge removed. The generic phrasing option also removes a maintenance hazard.
- [low] **Tighten the Artifact Mode / Artifact Policy split** — Preflight step 3 (SKILL.md lines 96-108) defines the persisted/inline decision and lists inline-mode behaviors; the Artifact Policy section (lines 139-152) re-lists inline-mode behaviors (no temp files, no directory requirement, inline report, in-memory digests) with near-identical bullets. Keep the mode-decision logic in preflight step 3 and the path layout + --output-file preference in Artifact Policy; state the inline behavior bullets once, in Artifact Policy, and have step 3 just set the mode.
  - Knowledge preservation: Both bullet sets are unions of the same facts; merging keeps every distinct bullet (including the --output-file zero-byte-artifact rationale at line 146) in a single canonical location.

## opencode-runner — minor-improvements
Footprint: SKILL.md 41 lines / ~330 tokens; no supporting files (no scripts, references, agents, or templates)

opencode-runner is a deliberately minimal, guide-only skill (no bundled wrapper script, unlike sibling runner skills) and is already close to optimal on simplicity and context-window economy at ~330 tokens. The safety model, output contract, and stop condition are all imperative checklists with clear escalation behavior. The one substantive gap is effectiveness: the "Manual Invocation Pattern" section (SKILL.md:24-28) tells the agent to "run the local OpenCode CLI directly" without ever naming the actual command (`opencode run ...`), leaving the highest-leverage mechanical detail to model memory. A small amount of repeated approval-flow phrasing could also be consolidated.

**Strengths:**
- Excellent context-window economy: single 41-line file, no reference sprawl, nothing that needs progressive disclosure at this size.
- Frontmatter description triggers precisely on explicit OpenCode requests, cross-model comparison, and 'OpenCode perspective' phrasings, with no false-positive bait toward generic CLI or code-review requests.
- Clear structured output contract (SKILL.md:30-37) — 4-item summary checklist the host agent can fill deterministically.
- Explicit stop/escalation conditions: refuse unattended-editing wrappers (SKILL.md:22) and stop on missing/unauthenticated CLI without improvising install commands (SKILL.md:39-41).
- The intentional absence of an executable wrapper is stated up front (SKILL.md:8), so an agent will not hunt for a nonexistent scripts/ directory the way sibling runners (codex-runner, qwen-runner) have.

**Recommendations:**
- [high] **Add one concrete OpenCode invocation example to the Manual Invocation Pattern** — SKILL.md:24-28 instructs the agent to 'run the local OpenCode CLI directly' and 'keep the command narrow' but never names the command. Add a 2-3 line fenced example, e.g. `opencode run "<concise prompt>"` and a model-pinned variant `opencode run -m <provider/model> "<prompt>"`, plus a read-only-review variant if one exists. This is the single highest-leverage detail for a runner skill: it removes guesswork/training-data drift about CLI syntax, and every sibling runner (codex-runner SKILL.md:39-43, Examples at 79-87) follows this pattern. The existing 'Do not provide install commands' restriction (SKILL.md:41) is about installation, not invocation, so this stays consistent with the skill's intent.
  - Knowledge preservation: Purely additive — no existing text is removed or altered; all current safety and approval guidance remains intact.
- [low] **Consolidate the triple-stated host-approval requirement** — The host-approval-flow requirement appears three times in 41 lines: SKILL.md:8 ('The host agent must use its normal shell approval, sandbox, and logging flow'), SKILL.md:20 (checklist step 5 'Use the host agent approval flow for the shell command'), and SKILL.md:26 ('When approved by the user and permitted by the host'). Keep the checklist step (line 20) as the canonical imperative statement, and trim line 8 to the unique fact it carries (no bundled wrapper ships with this package) and line 26 to a back-reference ('After the checklist above passes, run...'). Saves a few lines and removes the chance an agent treats the three phrasings as three different requirements.
  - Knowledge preservation: Deduplication only — the requirement is preserved once in its strongest imperative form (the numbered checklist); the unique facts on lines 8 (no bundled wrapper, intentionally) and 26 (capture stdout/stderr, narrow command) are kept verbatim.
- [low] **Optionally state how this skill plugs into cross-runner workflows** — Sibling runners (e.g. codex-runner SKILL.md:10-19) declare fallback/seat behavior for cross-runner workflows like models-consensus and council; opencode-runner's description mentions 'compare OpenCode output with another model' but the body never says what to do when OpenCode is selected as a seat (the Missing Prerequisite section covers absence, not participation). One sentence in the Output Contract or Manual Invocation section — e.g. 'When acting as a seat in a multi-model workflow, return the Output Contract summary to the orchestrating skill verbatim' — closes the loop the description opens. Skip this if cross-runner orchestrators already handle seat output generically.
  - Knowledge preservation: Additive clarification only; the existing Output Contract checklist (SKILL.md:30-37) is reused as-is rather than duplicated into a new format.

## pragmatic-coding-session — minor-improvements
Footprint: SKILL.md 135 lines / ~1.8k tokens; no supporting files (references/scripts/agents/templates directories do not exist)

This is a well-built, lean lens skill: a single 135-line SKILL.md of imperative, numbered checklists organized by phase, with explicit handoff routing to deeper skills and a clear output contract. At ~1.8k tokens it needs no progressive disclosure — splitting it would add overhead, not save it. The remaining issues are small: one dead handoff pointer (codex-security:security-scan), two handoff targets that assume co-installation (tdd, diagnose), a phase list that omits the Reliability And Safety pass, one duplicated instruction, and a frontmatter trigger broad enough to fire on virtually every coding task.

**Strengths:**
- Single-file design at ~1.8k tokens is the right shape for a cross-cutting lens skill; no progressive disclosure needed and none artificially added.
- Every phase section is an imperative numbered checklist (LLM-leverage pattern), not prose — e.g. Debugging lines 71-77 reads as an executable procedure.
- Explicit handoff routing after each phase (lines 35, 50, 65, 79, 93, 106) delegates depth to specialized skills instead of duplicating their content — strong context economy and most named targets verified to exist in the repo.
- Output Contract (lines 118-127) gives a structured response shape with an explicit 'include the relevant parts only' guard against checklist dumping.
- Gotchas section (lines 129-135) provides concrete anti-patterns and stop conditions, including the key one: 'Apply only the phase that matters.'
- Description frontmatter covers symptom-based phrasings (vague, risky, overdesigned, brittle, accidental, hard to test, hard to change) that map well to real user language.

**Recommendations:**
- [high] **Fix or qualify the dead handoff target codex-security:security-scan** — Line 93 routes high-risk reliability/security work to `codex-security:security-scan`, which exists neither in this repo nor in the installed skill list (unlike the other 13 verified targets). An agent following this handoff will fail or hallucinate. Replace with an existing target (e.g. the repo's review skills or the environment's security-review command) or add a one-line fallback such as 'if unavailable, do a manual trust-boundary review per the Reliability checklist above.' Similarly, `tdd` (line 65) and `diagnose` (line 79) live outside this repo; if the repo is distributed standalone, add a brief 'when installed' qualifier or an inline fallback for all external targets.
  - Knowledge preservation: No knowledge is deleted: the intent (escalate security/debug/TDD work to a deeper treatment) is preserved by pointing at a target that actually resolves, or by stating the fallback behavior inline. The original skill names can stay listed as preferred-when-installed.
- [medium] **Add Reliability And Safety to the phase enumeration** — The Session Loop step 1 (line 14) lists six phases — planning, design, implementation, debugging, review, handoff — but the skill defines a seventh pass, Reliability And Safety (lines 81-93), and the Output Contract `phase` field (line 122) implicitly depends on that enumeration. An agent locating 'the current phase' from line 14 has no route into the safety pass. Either add it to the list or append one clause: 'apply the Reliability And Safety pass whenever the change touches state, resources, or trust boundaries, regardless of phase.'
  - Knowledge preservation: Purely additive cross-reference; the Reliability section content is untouched, it just becomes reachable from the dispatch step.
- [low] **Deduplicate the 'do not quote the book' instruction** — Line 10 ('Do not quote or reconstruct source text from The Pragmatic Programmer') and Gotchas line 132 ('Do not quote the book or embed chapter notes') say the same thing. Keep the Gotchas version (it adds 'chapter notes') and trim line 10 to the positive half ('Apply the durable practices as decisions, checks, and verification'), or vice versa.
  - Knowledge preservation: Pure deduplication: the union of both statements (no quoting, no reconstruction, no chapter notes, apply practices instead) is preserved in the single remaining instance.
- [medium] **Narrow the broadest trigger clause in the description** — Frontmatter line 3 includes 'Use when starting or steering a nontrivial coding task' — this fires on essentially every coding request and will compete with more specific skills in the same catalog (collaborative_delivery, tdd, diagnose, clean-code), creating false-positive activations. The symptom-based clause ('when work feels vague, risky, overdesigned, brittle...') and the explicit clause ('asks for pragmatic programming guidance') already carry the intended triggers. Consider rewording the broad clause to something the user would actually say, e.g. 'when the user wants a pragmatic, phase-by-phase operating lens for a coding session' — unless always-on activation is the deliberate intent, in which case leave it.
  - Knowledge preservation: Rewording, not removal: all specific trigger phrasings (symptom words, pragmatic-programming request) remain verbatim; only the catch-all clause is tightened to reduce collisions with sibling skills.

## qwen-runner — minor-improvements
Footprint: SKILL.md 108 lines / ~800 tokens (loaded on trigger); scripts/run_qwen.py 621 lines / ~4.5k tokens (executed, not loaded); agents/openai.yaml 4 lines. Total ~733 lines.

A lean, well-structured runner skill: a single ~800-token SKILL.md backed by a deterministic Python wrapper that three sibling skills (glm, gemma, minimax) reuse via import, exactly as the frontmatter description promises. The main defect is the Usage section (SKILL.md lines 34-37), which contains raw authoring/requirements notes pasted as prose — including one requirement ("separate return codes for missing binary vs auth failure") the script does not actually implement, creating a doc/behavior contradiction. The rest needs only deduplication and small accuracy fixes; no progressive-disclosure restructuring is warranted at this size.

**Strengths:**
- SKILL.md is a single lean file (~800 tokens) — appropriate for a skill this size; no reference files needed and none are missing guidance.
- Frontmatter description triggers precisely: explicit Qwen requests, workflow seats, and the shared-wrapper reuse case, with low false-positive risk.
- Mechanical work is fully scripted (run_qwen.py): envelope normalization, atomic --output-file writes, native stream error detection, file-existence validation with distinct return codes — deterministic where it should be.
- run_qwen.py is genuinely shared: glm-runner, gemma-runner, and minimax-runner import its main() with different defaults, so the skill is the single source of truth for the wrapper.
- Gotchas section (lines 104-108) captures exactly the non-obvious traps: --output-schema is prompt-enforced not native, --restrict-tools is a soft overlay, --json controls only the outer envelope.
- Concrete runnable examples (lines 81-85) and a return-code table (lines 95-102) — patterns agents exploit well.

**Recommendations:**
- [high] **Rewrite the Usage section's pasted requirement notes into imperative guidance (SKILL.md lines 34-37)** — Lines 34-37 ('Normalize script invocation from repo root (ROOT=$(git rev-parse --show-toplevel || pwd)).', 'Requirement: standardized seat_unavailable envelope when qwen/auth missing.', 'Keep no-silent-provider-switch rule explicit.', 'Separate return codes for missing binary vs auth failure.') are authoring artifacts absent from every sibling runner SKILL.md (compare codex-runner/SKILL.md Usage). Convert: (a) make the ROOT normalization an actual command in the bash block: ROOT=$(git rev-parse --show-toplevel || pwd); python3 "$ROOT/.agents/skills/qwen-runner/scripts/run_qwen.py" ...; (b) move the seat_unavailable fact to the Return Codes section ('on -2 the envelope also carries status: seat_unavailable' — this is what normalize_envelope at run_qwen.py:76-77 actually does); (c) delete the no-silent-switch line as it duplicates lines 66 and 93; (d) resolve the stale 'separate return codes' requirement (see next recommendation).
  - Knowledge preservation: No deletion of unique knowledge: the ROOT pattern becomes executable usage, the seat_unavailable contract relocates to Return Codes, the no-fallback rule already exists verbatim at lines 66 and 93, and the unimplemented return-code requirement is resolved explicitly rather than dropped silently.
- [medium] **Align the Return Codes table with actual auth-failure behavior** — SKILL.md line 37 demands separate return codes for missing binary vs auth failure, but the script only distinguishes -2 (CLI missing, run_qwen.py:331-342, 403-405) and folds native API/auth errors into -3 via inspect_native_stream ([API Error: detection, run_qwen.py:119-155, 381-392). Either add a distinct code (e.g. -4 auth failure) in the script and table, or document reality in the table: '-3 also covers native API/auth errors; stderr contains the [API Error: ...] text and auth_ok reflects it'. The current table row '-3 Invalid input or unexpected error' under-describes a behavior agents need when triaging seat failures in councils.
  - Knowledge preservation: Pure addition/correction: the auth-error-to-(-3) mapping currently exists only implicitly in script code; documenting it (or implementing the promised code) makes hidden knowledge explicit. Nothing is removed.
- [low] **Deduplicate the no-fallback rule (SKILL.md lines 66 and 93)** — 'Qwen-backed runners do not fall back to another provider' (line 66) and Behavior item 5 'Never falls back to another provider. Missing CLI or auth failures block the seat explicitly.' (line 93) say the same thing. Keep the fuller Behavior #5 wording and shorten line 66 to only state that --safe/--bare/--disable-fallback are accepted for cross-runner compatibility.
  - Knowledge preservation: Deduplication only — the surviving Behavior #5 sentence contains a strict superset of the information in line 66's second sentence.
- [low] **Fix or annotate the dangling envelope-schema reference (SKILL.md line 22)** — .agents/skills/_shared/runner-envelope.schema.json does not exist anywhere in this repo (verified via find), yet all 8 runner SKILL.mds reference it. Either bundle the schema at a repo-level _shared/ directory, or annotate the line that the schema is provisioned at install time. The inline required-keys list (lines 23-32) is currently the only enforceable contract, which is fine — but an agent told to validate against a nonexistent file will waste a turn searching. This is repo-wide, but qwen-runner is the canonical wrapper, so fixing here first matters most.
  - Knowledge preservation: The required-keys list already preserves the contract in SKILL.md; bundling the schema file adds knowledge, and an install-time annotation prevents misinterpretation without removing the reference.
- [low] **Surface the always-off chat recording and no-op compat flags in Gotchas** — run_qwen.py always passes --chat-recording=false (line 321) regardless of flags, so --ephemeral and --no-session-persistence are accepted no-ops (their argparse help at lines 520-529 even implies otherwise). SKILL.md line 66 lists only --safe/--bare/--disable-fallback as compat flags. Add one Gotchas bullet: 'Chat recording is always disabled; --ephemeral and --no-session-persistence are compatibility no-ops.' Optionally tighten the bare Supported Options list (lines 43-64) by appending '(run --help for per-flag docs)' instead of expanding it into a table — argparse already documents every flag.
  - Knowledge preservation: Surfaces behavior currently buried in script code into SKILL.md — additive. The options list stays intact; per-flag detail remains canonically in argparse help, referenced rather than duplicated.

## react-performance — minor-improvements
Footprint: SKILL.md 198 lines / ~2.8k tokens (always loaded on trigger); references/cheatsheet.md 232 lines / ~2.9k tokens (on-demand). Total ~5.7k tokens, ~430 lines.

A well-constructed, opinionated skill: nine clearly-scoped lenses derived from "Advanced React" (Makarevich), with strong trigger phrasing, an advisor/reviewer mode split, a file:line output contract, and explicit anti-false-positive guards ("skip lenses that don't apply", "don't invent re-renders that don't matter"). The main weakness is substantial verbatim duplication between SKILL.md and the cheatsheet (~40% of lens content appears in both, defeating the progressive-disclosure design), plus two cheatsheet chapters (ch 6 keys/reconciliation, ch 7 HOCs) that no lens or pointer ever directs the agent to read. Fixes are deduplication and entry-point additions, not deletions.

**Strengths:**
- Frontmatter description triggers on the right user phrasings ('why does this re-render', 'should I memoize this', stale state, flicker, modal clipping, race conditions) and explicitly disambiguates from sibling skills (model-lens/architect-lens, design-patterns) — low false-positive risk.
- Lens structure is checklist-driven with detection red flags per lens (e.g. SKILL.md:67-68, 98-99, 129-131) — exactly the pattern coding agents exploit well.
- Clear output contract for reviewer mode: cite file:line, name the rule, propose the concrete fix, skip non-applicable lenses (SKILL.md:18-20, 194).
- Strong stop/anti-overreach conditions: 'composition first, memoization last' golden rule (SKILL.md:35-39) and 'don't invent re-renders that don't matter' (SKILL.md:195-196).
- Repo-specific grounding (React 17 forwardRef, MUI portal behavior, prefer RTK over hand-rolled Context/fetch) prevents the agent from applying React 19 advice or reinventing existing infrastructure.
- Lens headers cite book chapters (ch 1-4, ch 8, etc.) that map 1:1 to cheatsheet sections, giving a natural lookup path; SKILL.md:198 tells the agent when to read the cheatsheet.

**Recommendations:**
- [medium] **Deduplicate SKILL.md lens bodies against the cheatsheet** — Roughly 40% of SKILL.md lens content is repeated verbatim or near-verbatim in references/cheatsheet.md: the ScrollDetector children-as-props example (SKILL.md:59-62 ≈ cheatsheet:46-53), the race-condition fetch snippet (SKILL.md:165-169 ≈ cheatsheet:198-199), Lens 6 useLayoutEffect (SKILL.md:135-144 ≈ cheatsheet ch 12:166-172), Lens 7 portals (SKILL.md:146-154 ≈ cheatsheet ch 13:174-183), and Lens 9 error handling (SKILL.md:175-184 ≈ cheatsheet ch 16:216-232). Since SKILL.md is always loaded and the cheatsheet is loaded on demand, trim each lens in SKILL.md to: detection trigger + rule name + one-line fix direction + 'exact pattern: cheatsheet ch N'. Cut the code snippets from SKILL.md (keep them in the cheatsheet). Estimated saving: 50-70 lines / ~900 tokens off every activation.
  - Knowledge preservation: No knowledge is deleted — every duplicated snippet and explanation already exists in references/cheatsheet.md under the matching chapter section, and each lens header already cites the chapter number, so the pointer path is intact. Anything unique to SKILL.md (detection red flags, repo-specific notes like 'MUI Modal/Popper already portal' — also at cheatsheet:182-183) stays in SKILL.md or is verified present in the cheatsheet before removal.
- [medium] **Give cheatsheet ch 6 (keys/reconciliation) and ch 7 (HOCs) an entry point** — cheatsheet.md:95-112 covers diffing, key-driven remounts/state-reset, and HOCs — knowledge with no corresponding lens or pointer in SKILL.md, so the agent will never be directed to read it (the orphaned-reference failure mode). Keys-in-memo'd-lists and key-forced-remount are common real review findings. Add one line to Lens 1 or 2, e.g. 'Memo'd list items or state mysteriously resetting/persisting across renders → keys & reconciliation, cheatsheet ch 6', and a parenthetical pointer to ch 7 where HOCs are mentioned (the Lens 3 fake-selector note at SKILL.md:104 already mentions HOCs — attach 'see cheatsheet ch 7' there). Net cost: ~2 lines in SKILL.md.
  - Knowledge preservation: Pure addition of pointers; the ch 6/ch 7 content stays exactly where it is in the cheatsheet and becomes reachable instead of dead.
- [low] **Fix the fragile relative link to frontend/CLAUDE.md** — SKILL.md:24 links [frontend/CLAUDE.md](../../../frontend/CLAUDE.md), which only resolves if the skill is installed at exactly three directory levels below the app repo root (e.g. .claude/skills/react-performance/). It does not resolve from this skills repo (verified). Replace with a path-agnostic instruction, e.g. 'see frontend/CLAUDE.md at the app repo root', so the agent globs/searches for it rather than following a broken relative path.
  - Knowledge preservation: The pointer and its purpose (React 17 constraints documented in the app repo) are kept; only the link form changes to one that works regardless of install depth.
- [low] **Trim the 'not a bug hunt' framing paragraph slightly** — SKILL.md:8-12 spends five lines saying what the skill is not before saying what it is. The disambiguation already lives in the frontmatter description ('Distinct from model-lens/architect-lens... design-patterns'). Compress to two lines: the one-sentence question set ('will this re-render more than it needs to...') plus the code-review redirect. Saves ~3 lines; keep the question set verbatim since it is the skill's core framing.
  - Knowledge preservation: The code-review redirect and the core question set are retained; only the redundant restatement of the frontmatter disambiguation is removed (it survives in the description field, which is what routes activation anyway).

## refactor-to-testability — good-as-is
Footprint: Single file: SKILL.md, 134 lines / ~6.1 KB / ~1.5k tokens. No supporting files (no references, scripts, agents, or templates) — none are needed.

This is an exemplary knowledge-skill: a 134-line single-file procedure distilling Dave Farley's five-step legacy-rescue method, with a trigger-rich frontmatter description, an ordered checklist workflow, a closing quick-reference table, and explicit hand-off routing to sibling skills (small-steps/tdd) that I verified exist in the repo. It is already lean enough that progressive disclosure would add overhead rather than save tokens, and the entire body is unique domain knowledge with essentially no over-specification of agent behavior. No structural changes are warranted; the two recommendations below are optional, low-impact polish.

**Strengths:**
- Excellent frontmatter description: covers concrete user phrasings ("this is legacy", "there are no tests", "make this testable"), states the defining move (approval tests first), and explicitly disambiguates against tdd/small-steps/clean-code with a clear negative trigger ("Do NOT use to design new functionality from scratch") — strong precision against false-positive activation.
- Right-sized at ~1.5k tokens in one file — splitting into reference files would cost more than it saves; this is correct application of progressive-disclosure judgment, not an omission.
- Deterministic-where-it-matters: the five steps are explicitly ordered with the rationale for the ordering (line 38: can't simplify without a net, can't see structure under clutter), and each step has a "Done when" exit condition in the quick-reference table (lines 126-132) — checklist over prose wall.
- High-leverage imperative guardrails a less-informed agent would get wrong: "Do not retrofit fine-grained, TDD-style unit tests to legacy code" (line 53), "pin behaviour even if buggy" (lines 45-47), "use IDE automated refactorings" (lines 88-89), "incrementally on the area you actually need to touch, not the whole codebase" (lines 32-34).
- Clean hand-off contract: "After the net is in place" (lines 117-122) routes back to the small-steps/tdd loop for the actual behaviour change, and the cross-referenced skills actually exist in this repo.
- No dead sections, no duplicated content across files (single file), no scripts pretending mechanical work exists where none does — this workflow is genuinely judgment-driven end to end.

**Recommendations:**
- [low] **Optional: one-line escalation guidance for code that resists approval testing** — Step 1 (SKILL.md lines 41-62) assumes the code's output can be captured deterministically. Real legacy code often can't be pinned directly (nondeterministic output: timestamps, UUIDs, random seeds; side-effect-only code with nothing to capture). A single added bullet in step 1 — e.g. "If output is nondeterministic or side-effect-only, normalise/scrub the variable parts or introduce a minimal seam to capture it before pinning" — closes the one branch where an agent following the steps literally can stall. Keep it to 1-2 lines; do not add a reference file for this.
  - Knowledge preservation: Purely additive — no existing text is removed or altered; all five steps and both framing principles remain verbatim.
- [low] **Optional: deduplicate the triple-stated behaviour-preserving rule** — The "every step is behaviour-preserving, confirmed by the net" rule appears three times: the framing principle (lines 28-30), step 2's "Run the approval tests after removals" (line 72), and the closing line (line 134). The step-2 instance could be generalised once in the framing section as "run the net after every transformation, in every step" and dropped from step 2, saving a few lines. However, the repetition is plausibly deliberate emphasis at the moment of first deletion, and the token saving is ~15 tokens — only do this if touching the file anyway.
  - Knowledge preservation: Deduplication, not deletion: the rule (run approval tests after each change) is stated once in the framing principles where it applies to all five steps, so no unique knowledge is lost — line 72's instruction becomes a universal instruction instead of a step-2-only one.

## skill-expert — minor-improvements
Footprint: SKILL.md 162 lines / ~1,700 words / ~2.3k tokens; 2 reference files (references/description-optimization.md 64 lines, references/evaluation.md 109 lines) ~920 words / ~1.2k tokens combined; empty assets/ and scripts/ placeholder dirs

skill-expert is a well-built, mostly self-consistent skill: imperative checklist style throughout, a strong front-loaded frontmatter description, genuine progressive disclosure with two on-demand reference files that both have explicit "read X when Y" pointers, and a structured Delivery Contract. The main issues are small-scale redundancy: the reference pointers are stated twice (Workflow step 9 and the References section), Workflow step 9 partially restates content that already lives in the two reference files, and the "no extra files" principle appears in three places. There is also a self-violation: empty assets/ and scripts/ placeholder directories sit in the skill folder despite line 47 forbidding exactly that.

**Strengths:**
- Frontmatter description follows its own recommended formula (capability + 'Use when' + concrete user verbs: create, write, author, merge, audit, package, troubleshoot, optimize) and front-loads trigger words — good activation coverage with low false-positive bait.
- True progressive disclosure: heavy eval and trigger-tuning material lives in references/evaluation.md and references/description-optimization.md, each with conditional load guidance (SKILL.md lines 107-108 and 159-162); no orphan reference files.
- Imperative, checklist-driven body: 9-step numbered workflow, Quality Bar as a final checklist, Troubleshooting as symptom→fix one-liners, Delivery Contract as an explicit structured-output contract (lines 149-157).
- Good determinism/judgment split: mechanical steps point to repo-local scripts when available (init_skill.py, quick_validate.py, package_skill.py at lines 51-54, 92-96) with explicit manual fallbacks (line 97), while design decisions stay model-driven.
- Reference files practice what they preach: concrete good/bad examples (description-optimization.md lines 16-28), JSON schemas for evals and grading (evaluation.md lines 14-29, 67-85), and explicit workspace layout (evaluation.md lines 33-47).
- SKILL.md at 162 lines is comfortably inside its own recommended budget; no dead or unreachable sections in the prose.

**Recommendations:**
- [medium] **Deduplicate the two reference-pointer blocks** — SKILL.md states when to read each reference twice: Workflow step 9 lines 107-108 ('Read references/evaluation.md for a detailed eval workspace...'; 'Read references/description-optimization.md when optimizing...') and the References section lines 159-162 (richer conditions: 'test, benchmark, compare, or prove a skill is better'; 'asks why a skill does or does not trigger'). Merge the trigger conditions into the References section only, and have step 9 end with a one-line pointer such as 'See References below for when to load the detailed eval and description-tuning guides.'
  - Knowledge preservation: All four trigger conditions are unioned into the single References section; nothing is deleted, only the duplicate phrasing is collapsed and step 9 keeps a forward pointer.
- [medium] **Compress Workflow step 9 by relocating detail already covered (or coverable) in the reference files** — Step 9 (SKILL.md lines 101-106) restates material from the references: line 102 (should-trigger/should-not-trigger prompts with near misses) duplicates description-optimization.md lines 7-14; line 104 (compare against no skill or previous version) duplicates evaluation.md line 51; line 103 (start with 2-3 evals) duplicates evaluation.md line 9. Two bullets are unique and should move into evaluation.md rather than be cut: line 105 ('Review execution traces, not just final answers, to spot wasted work, vague instructions, over-triggering, or missing defaults' — evaluation.md line 59 covers output files but not trace review for these failure modes) and line 106 (iterate-until-justified stopping rule, which is stronger than evaluation.md line 63). Reduce step 9 to ~2 lines: when to evaluate + pointer to the references.
  - Knowledge preservation: Duplicated bullets already exist verbatim-in-substance in the reference files; the two unique bullets (trace-review failure modes, cost-justified stopping rule) are moved into evaluation.md's Run Loop / Benchmark Analysis sections before step 9 is shortened. Zero unique knowledge is deleted.
- [low] **Remove the empty assets/ and scripts/ placeholder directories** — /Users/robson/Development/rar-skills/skill-expert/assets/ and /Users/robson/Development/rar-skills/skill-expert/scripts/ are empty. This directly violates the skill's own rule at SKILL.md line 47 ('Leave out unused placeholder directories and files') and line 136 ('The final directory contains only files that directly support the skill'). Git does not track empty dirs, so packaging is unaffected, but locally it is misleading template residue of the kind line 99 tells authors to grep for.
  - Knowledge preservation: The directories contain no files, so deletion loses nothing; if scripts or assets are added later the dirs are recreated then.
- [low] **Consolidate the triple-stated 'no extraneous files' rule** — The same principle appears at SKILL.md line 19 (Operating Principles: no README/CHANGELOG/INSTALLATION unless required), line 47 (Workflow step 3: leave out unused placeholders), and line 136 (Quality Bar: directory contains only supporting files). Keep the Quality Bar checklist line as the verification point, keep the specific banned-file examples from line 19 (that list is unique knowledge), and fold line 47's placeholder clause into line 19 or 136 so the rule is stated once with examples and checked once.
  - Knowledge preservation: The unique elements — the explicit banned-file-name list (line 19) and the placeholder-directory clause (line 47) — are merged into one statement; the Quality Bar retains a short checklist echo. Only repetition is removed.
- [low] **Relocate the third-party-skill inspection note out of Quality Bar** — SKILL.md line 137 ('External or third-party skills are inspected before use, especially when network access, shell tools, secrets, or hosted execution are involved') is a security practice about consuming foreign skills, not a quality criterion for a skill being authored — it is unreachable as a 'quality bar' check during creation. Move it to Operating Principles next to the related safety line 18 ('never create misleading skills, hidden exfiltration...'), where it applies to audit/troubleshoot invocations of this skill.
  - Knowledge preservation: The sentence is moved verbatim to Operating Principles; no wording or knowledge is lost, and it becomes reachable in the audit/inspect branch where it actually matters.

## small-steps — minor-improvements
Footprint: SKILL.md only: 152 lines / ~1,150 words / ~1.5k tokens body; frontmatter description ~140 words / ~190 tokens (always loaded). No reference files, scripts, agents, or templates.

small-steps is a well-built, single-file cadence skill that is already close to the ideal shape: lean enough to need no progressive disclosure, correctly model-driven (there is nothing mechanical to script in a working-rhythm skill), with a numbered core loop, concrete litmus tests, an anti-patterns checklist, and valid routing pointers to six sibling skills that all exist. The only real inefficiency is that the scope/routing knowledge (defer to tdd, clean-code, etc.) is stated three times — in the frontmatter description, inline in step 2, and again in the closing "A note on scope" section — and the always-loaded description carries some of that duplication. A handful of low-impact dedup/tightening edits would shave the always-resident token cost without losing anything.

**Strengths:**
- Right-sized single file (~1.5k tokens) — no need for reference files or progressive disclosure; everything fits comfortably in context when triggered.
- Strong trigger description: covers user phrasings ('let's build X', 'work in small steps', 'TDD this', 'keep this small'), proactive triggers (change growing large/branchy/scary, pre-commit sanity check), and explicit negative triggers (do NOT use for diagnosis or macro architecture) that prevent false positives.
- Imperative, numbered core loop (lines 31-64) with a clear stop/split condition ('if you can't state the outcome in a sentence, the step is too big; split it') and a predict-the-failure discipline that LLM agents exploit well.
- Concrete litmus tests instead of abstract prose: 'a good test survives a reimplementation' (line 87-89), 'should'-prefixed test names with an example (line 85-86), '~5-10 lines, named so they form sentences' (line 110-111), commit cadence 'every 10-15 minutes, not per-feature' (line 56-57).
- Anti-patterns section (lines 133-144) gives the agent explicit failure modes to self-check against — high leverage per token.
- All six cross-skill references resolve to real skills in the repo/environment, making the routing actionable rather than aspirational.
- Correctly model-driven throughout — a cadence/judgment skill has no mechanical steps that would benefit from bundled scripts, and none were forced in.

**Recommendations:**
- [low] **Trim the routing sentences from the frontmatter description** — The description (SKILL.md lines 3-15) is ~140 words and is loaded into every session's system prompt. Lines 11-13 ('This is the session-cadence skill — it orchestrates HOW you make progress; lean on `tdd` for red-green-refactor mechanics and `clean-code` for naming/structure tidy-ups.') duplicate the body's 'A note on scope' section (lines 146-152) almost verbatim. Keep the negative triggers (Do NOT use for diagnosis / macro architecture — those prevent false-positive activation and must stay in the description), but drop the tdd/clean-code delegation sentence, saving ~35 always-resident tokens. Trigger phrases and the pre-commit sanity-check use case should remain untouched.
  - Knowledge preservation: The tdd/clean-code routing knowledge is fully preserved in the body's 'A note on scope' section (lines 148-152), which the agent reads whenever the skill activates — the description sentence is a pure duplicate, so removal is deduplication, not deletion.
- [low] **Deduplicate the tdd-skill pointer in step 2 of the core loop** — Line 45-46 ('Defer to the `tdd` skill for the red-green-refactor mechanics.') restates what 'A note on scope' (line 148) already says. Either delete the parenthetical in step 2 and rely on the scope section, or keep the inline pointer and shorten the scope section's tdd clause to avoid saying it twice. Saves ~2 lines and removes one of three occurrences of the same routing rule.
  - Knowledge preservation: The routing rule survives in exactly one canonical location ('A note on scope'); no unique information is lost since both occurrences carry identical content.
- [low] **Consider converting the 'Judge each step by ease of change' five properties into the checklist they already are** — Lines 96-111 are good, but the lead-in prose (lines 98-101) could be compressed to one imperative sentence ('Before committing, scan these five levers; if a step made one noticeably worse, refactor now while it's cheap:') — the current phrasing spends ~3 lines on framing that the bullet list itself conveys. This is the only section where prose framing outweighs the payload. Marginal benefit (~30 tokens), so only do this if touching the file anyway.
  - Knowledge preservation: All five properties, their one-line definitions, the 'levers not a gate' framing, the refactor-now trigger, and the ~5-10-line-functions outward sign are all retained verbatim; only the introductory scaffolding is tightened.

## summarize — minor-improvements
Footprint: SKILL.md 28 lines / ~230 tokens; no reference files, scripts, agents, or templates

This is an exemplary lean skill: a single 28-line SKILL.md with a clear privacy boundary, a 7-item output checklist, and sensible omission rules. There is no redundancy, no dead content, and nothing that needs progressive disclosure at this size. The two real gaps are trigger precision (the bare word "summarize" in the description risks false-positive activation on document/article/PR summarization requests, and "compact" collides with Claude Code's built-in /compact and coexisting context-compression skills) and an unspecified output destination (chat reply vs. written file), which leaves the agent to guess on the single most consequential behavioral choice.

**Strengths:**
- Outstanding context-window economy: ~230 tokens total, fully inline, no unnecessary file indirection for a skill this small
- The Output section (SKILL.md lines 16-24) is a numbered checklist rather than prose — exactly the structure LLM agents execute reliably
- Explicit privacy boundary (lines 10-12) with a concrete remediation instruction (replace sensitive values with a redacted label) — high-leverage, unambiguous, imperative
- Good anti-filler guardrails: conditional section omission and the 'if the session is short, say so plainly' rule (lines 28) prevent padded output
- Concrete style guidance (line 26: prefer paths, command names, exact error labels over long quotations) steers toward handoff-useful density

**Recommendations:**
- [medium] **Scope the frontmatter description to session/work summarization to prevent false-positive triggering** — The description (SKILL.md line 3) says 'Use when the user asks to summarize, compact, or prepare continuity notes for the current work.' The bare trigger 'summarize' will match requests like 'summarize this README' or 'summarize this article', which this skill explicitly does not serve (its body is about session handoffs only). 'Compact' also collides with Claude Code's built-in /compact command and other context-compression skills installed alongside it. Reword to something like: 'Create a concise handoff summary from visible session context. Use when the user asks to summarize the session or current work, prepare a handoff, or write continuity notes for resuming later. Not for summarizing documents, articles, or code unrelated to the active session.'
  - Knowledge preservation: Pure rewording of the trigger sentence; all existing trigger intents (summarize work, compact session, continuity notes) remain covered, with an added negative trigger — no information removed.
- [medium] **Specify the output destination (chat reply vs. file)** — SKILL.md line 16 says 'Write a compact Markdown summary' but never states where: print in the reply, or save to a file (and if so, what path/naming). For a handoff artifact this is the single most consequential ambiguity — agents will behave inconsistently across runs. Add one sentence, e.g.: 'Output the summary directly in the reply; only write it to a file if the user asks, defaulting to ./HANDOFF.md.'
  - Knowledge preservation: Pure addition of one sentence; no existing content is changed or removed.
- [low] **Optionally state intended consumer of the summary to sharpen content choices** — The body (line 8) says 'handoff' but never says to whom — a fresh agent session, a human teammate, or the same user later. One clause such as 'written so a fresh agent session can resume the work without other context' would anchor the agent's judgment about what counts as relevant in the 7 sections. This is low-cost (one line) and improves determinism of judgment-driven content selection.
  - Knowledge preservation: Pure addition; existing sections and rules untouched.

## test-lens — minor-improvements
Footprint: SKILL.md 138 lines / ~1,392 words ≈ 1.9k tokens; 1 reference file (references/decision-trees.md) 223 lines / ~1,386 words ≈ 2.0-2.2k tokens (code blocks and tables inflate slightly). Total ~4k tokens, of which only ~1.9k loads on trigger.

test-lens is a well-built knowledge-lens skill: a lean SKILL.md carries the decision rubric (four pillars, three lenses, code quadrant, anti-pattern checklist, 5-step workflow with an explicit report contract), while worked code examples and flowcharts live in a single on-demand reference file. The frontmatter description triggers on the right user phrasings and explicitly disambiguates from tdd/clean-code/code-review. Remaining issues are small: one reference section (integration-test rules) is never signposted from SKILL.md, and there is some verbatim duplication between SKILL.md and the reference (mock rules, code quadrant, coverage warning, sibling-skill disambiguation).

**Strengths:**
- Frontmatter description (SKILL.md:3) is excellent: covers capability, decision use-cases, literal user quotes ("is this a good test", "should I mock this"), and explicit negative triggers ("Distinct from tdd..., clean-code..., code-review...") that prevent false-positive activation against sibling skills.
- Clean progressive disclosure: SKILL.md holds only decision rules; worked before/after code, the mock flowchart, the Humble Object refactor, and the anti-pattern catalog live in references/decision-trees.md, with explicit pointers at SKILL.md:70, :74, and :137-138.
- High LLM leverage throughout: tables (failure modes :36-40, style ranking :62-66, quadrant :92-96), a checklist (:105-113), a deterministic 5-step workflow (:126-135), and a concrete output contract ("file:line → which rule → the fix", plus "If a test is already good, say so plainly" at :18 and "Skip lenses that don't apply rather than padding" at :134-135).
- No dead references: all sibling skills named (tdd, clean-code, code-review, refactor-to-testability, model-lens, architect-lens) exist in the repo or environment.
- Prose is already tight — dense, imperative, no filler; the bolding consistently lands on the load-bearing rules (e.g. "Never assert calls to a stub").

**Recommendations:**
- [medium] **Signpost reference section 6 (integration-test rules) from SKILL.md** — references/decision-trees.md section 6 (lines 200-210: worst-case-path + shortest-happy-path selection, logging-test policy, Test Pyramid) is the only reference section the closing pointer (SKILL.md:137-138) does not mention, so an agent reviewing integration tests has no cue to load it. Add "integration-test rules (path selection, logging, pyramid)" to the SKILL.md:137-138 pointer list, or add a one-line "For integration tests, read references/decision-trees.md §6" under Lens 3.
  - Knowledge preservation: Purely additive — one clause added to SKILL.md; nothing removed. Makes existing knowledge in the reference reachable instead of orphaned.
- [low] **Deduplicate the code quadrant and coverage warning between SKILL.md and the reference** — The ASCII quadrant diagram in references/decision-trees.md §4 (lines 141-153) repeats SKILL.md:92-96 verbatim in content, and the "don't chase 100% coverage" paragraph (ref lines 160-162) repeats SKILL.md:102-103. Replace the diagram and coverage paragraph in the reference with "Quadrant table and coverage rule: see SKILL.md Lens 4", keeping only the unique material: the Humble Object refactor steps and the line "Aim for *nothing* in the top-right" (move that sentence into the retained paragraph). Saves ~15 lines in the reference.
  - Knowledge preservation: Pure deduplication: the quadrant and coverage rule remain in SKILL.md (which is always loaded when the reference is); the reference's only unique sentence ("Aim for nothing in the top-right") is explicitly retained.
- [low] **Trim duplicated mock rules — keep rules in SKILL.md, rationale only in the reference** — SKILL.md:84-86 ("Mock only at the system edge... only types you own — wrap third-party SDKs...") and references/decision-trees.md:65-71 state the same two rules; the reference adds only the rationale (exercises more code / survives SDK upgrades). In the reference, reduce the restatement to the rationale sentences, e.g. "Why the two SKILL.md edge rules hold: ...". Saves ~5 lines and removes drift risk between the two copies.
  - Knowledge preservation: Rules stay verbatim in SKILL.md (the always-loaded file); the rationale sentences — the only unique content in the reference copy — are kept in the reference. No information deleted.
- [low] **Tighten the "Relationship to sibling skills" section in the reference** — references/decision-trees.md:214-223 re-explains tdd, clean-code, and code-review distinctions already covered in the frontmatter (SKILL.md:3) and intro (SKILL.md:9-11). Keep only the three entries with unique content — refactor-to-testability (line 218, overlap on Humble Object), and model-lens/architect-lens (line 222, functional-core/mutable-shell connection) — and drop the tdd/clean-code/code-review bullets, or compress them to one line pointing at the frontmatter.
  - Knowledge preservation: The tdd/clean-code/code-review distinctions are fully preserved in the SKILL.md frontmatter and intro; only the duplicate copies are removed. The three unique sibling relationships (refactor-to-testability, model-lens, architect-lens) are retained in the reference.

## ui-ux-pro-max — minor-improvements
Footprint: SKILL.md 277 lines / ~2.3k tokens (loaded on trigger). Scripts: search.py 114 + core.py 253 + design_system.py 1,067 lines (~11k tokens, executed not read — zero context cost in normal use). Data: 13 domain CSVs + 13 stack CSVs, ~1,450 lines (~25k+ tokens raw, never loaded; accessed via BM25 search script).

This is a well-architected skill: ~25k tokens of design knowledge live in CSVs that are never loaded into context, accessed instead through a deterministic BM25 search script — an exemplary progressive-disclosure pattern. SKILL.md itself is lean (~2.3k tokens) with a clear 4-step workflow. The problems are accuracy drift between SKILL.md and the code (wrong persist paths, a documented domain that errors, an undocumented working domain, 3 undocumented stacks), a frontmatter description far too thin to trigger reliably, and moderate internal duplication between the Common Rules tables, the Pre-Delivery Checklist, and the checklist the script already emits with every --design-system run.

**Strengths:**
- Excellent context economy: all heavy knowledge (67 styles, 96 palettes, 57 font pairings, 13 stack guides) sits in CSVs queried via scripts/search.py — the agent only ever sees top-N BM25 results, truncated to 300 chars per field (search.py:48-49)
- Deterministic script for mechanical steps (search, ranking, design-system aggregation, file persistence) with model judgment reserved for requirement analysis and synthesis — correct division of labor
- Clear imperative 4-step workflow with a REQUIRED entry point (--design-system) and explicit default stack (html-tailwind), plus a concrete end-to-end example
- Common Rules section (SKILL.md:204-241) uses concrete Do/Don't tables with exact values (bg-white/80, slate-600, 150-300ms) — high LLM leverage, not abstract prose
- Sensible stop condition: missing-Python prerequisite check with explicit instruction not to install (SKILL.md:9-17)
- Output truncation and token-optimized formatting are explicitly designed into format_output (search.py:31)

**Recommendations:**
- [high] **Rewrite frontmatter description with trigger phrasing** — SKILL.md:3 is just 'UI/UX design intelligence with searchable database'. It names no user phrasings, so requests like 'build a landing page', 'pick a color palette', 'make this dashboard look professional', or 'choose fonts' may not activate it — and competing skills in this repo (frontend-design) have much stronger descriptions. Replace with e.g.: 'Searchable UI/UX design database (styles, color palettes, font pairings, UX guidelines, chart types, stack best practices) that generates complete design systems. Use when designing or building any web/mobile UI — landing pages, dashboards, components — or when choosing styles, colors, typography, charts, or reviewing/fixing UI quality.'
  - Knowledge preservation: Pure addition to frontmatter; the existing one-line description's content ('searchable database') is retained inside the new text. No body content touched.
- [high] **Fix SKILL.md / code drift: persist paths, dead 'prompt' domain, missing 'icons' domain and 3 stacks** — (a) SKILL.md:60-62 and 70-75 say persistence creates 'design-system/MASTER.md' and 'design-system/pages/checkout.md', but design_system.py:508-519 actually writes 'design-system/<project-slug>/MASTER.md' and '.../pages/'. An agent following the documented retrieval logic will look in the wrong path and conclude no design system exists. Update the paths to include the project slug. (b) SKILL.md:122 documents a 'prompt' domain; verified it errors: "invalid choice: 'prompt' (choose from style, color, ...)". Its content (AI prompts, CSS keywords) is served by the 'style' domain's 'AI Prompt Keywords'/'CSS/Technical Keywords' output columns — fold that note into the style row and delete the prompt row. (c) The 'icons' domain works (verified) and icons.csv has 101 rows of import-code knowledge, but it is absent from the domain table (SKILL.md:109-122) — the agent is never told it can search icons, even though the Common Rules section demands correct SVG icons. Add an 'icons' row. (d) core.py:70-84 registers 13 stacks but SKILL.md:103 and the stack table (124-137) list only 10 — astro, nuxtjs, nuxt-ui are unreachable knowledge (164 CSV lines). Add them.
  - Knowledge preservation: All changes either correct paths to match working code, surface existing-but-hidden knowledge (icons domain, 3 stack CSVs), or relocate the prompt-domain description into the style row where that data actually lives. Nothing deleted from the CSVs or scripts.
- [medium] **Make the script invocation path robust** — Every command in SKILL.md (lines 38, 49, 57, 66, 82, 100, 154, 163-172, 185-188) hardcodes 'python3 skills/ui-ux-pro-max/scripts/search.py', which only works if cwd is the parent of a 'skills/' folder — it fails for ~/.claude/skills installs or any other cwd. Replace with a one-line instruction at the top of the workflow: 'Run all commands from this skill's directory, e.g. python3 <skill-dir>/scripts/search.py ...' (the agent knows the skill's path from the loaded SKILL.md location), then use a relative placeholder in all examples. Note search.py resolves data via Path(__file__) (core.py:14), so it works from any cwd once the script path is right.
  - Knowledge preservation: Command syntax, flags, and examples are unchanged; only the path prefix is generalized. All examples remain runnable.
- [medium] **Deduplicate Common Rules vs Pre-Delivery Checklist vs script-emitted checklist** — Three copies of the same quality rules exist: SKILL.md Common Rules tables (204-241), SKILL.md Pre-Delivery Checklist (244-277), and the 7-item checklist the script appends to every --design-system output (design_system.py:348-359, 448-455, 786-799). Within SKILL.md, at least 8 items appear twice (emoji icons, cursor-pointer, hover layout shift, transition timing, light-mode contrast, focus states, navbar overlap, icon consistency). Keep the Common Rules tables (they carry unique concrete values: bg-white/80, slate-600/#475569, border-gray-200, top-4 spacing, max-w-6xl) and trim the SKILL.md checklist to only items NOT already in the tables (alt text, form labels, color-not-only-indicator, prefers-reduced-motion, responsive breakpoints, no-horizontal-scroll, theme colors vs var() wrapper) with a one-line pointer 'plus every Do column above'. Saves ~25 lines of the hottest context.
  - Knowledge preservation: Every unique rule survives exactly once: concrete values stay in the tables, table-absent checklist items stay in the trimmed checklist, and the script-emitted checklist remains untouched as the runtime reminder. Deduplication only — no rule deleted.
- [low] **Compress Example Workflow, Output Formats, and Tips sections** — The Example Workflow (SKILL.md:141-175) repeats the Step 2-4 commands nearly verbatim — fold its unique value (the concrete beauty-spa query strings and the Vietnamese-input demonstration) into the step sections as inline examples and drop the rest (~25 lines). Output Formats (179-189) is two commands explaining one flag — reduce to a single line under Step 2: 'Add -f markdown for documentation output (default: ascii)'. Tips (193-200) items 3 and 5 restate Steps 3-4; keep items 1, 2, and 6 as a single 'If results are weak, retry with more specific or different keywords' line and fold item 4's unique keywords (z-index, loading) into the ux row of the domain table. Net ~45 lines (~400 tokens) off SKILL.md.
  - Knowledge preservation: Unique content relocated, not removed: example queries move inline to steps, the -f markdown flag stays as one line, retry-with-different-keywords guidance stays, and the unique ux search keywords move into the domain table's Example Keywords column.
- [low] **Update stale docstrings and help text in search.py** — search.py:9-10 docstring says 'Domains: style, prompt, color, chart, landing, product, ux, typography' (prompt invalid; icons/react/web missing) and 'Stacks: html-tailwind, react, nextjs' (10 missing); the --stack help string at search.py:60 repeats the 3-stack list. Since argparse choices are generated from CSV_CONFIG/AVAILABLE_STACKS, just reference those ('see --help for full list') or list correctly. Agents that read the script for usage will currently be misled the same way SKILL.md misleads them.
  - Knowledge preservation: Comment-only correction to match runtime behavior; no functional code or data changes.
