interface:
  display_name: "Implement Tasks"
  short_description: "Approve models, execute the approved queue, and verify"
  default_prompt: "Use $implement-tasks on the approved queue. Present exact implementation and review models, runners, effort levels, and task assignments for my approval before dispatch. Then run implement-and-review per task, integrate dependencies, verify acceptance, and review the seams. Keep delivery within my authorization."
policy:
  allow_implicit_invocation: false
