interface:
  display_name: "To PRD"
  short_description: "Turn settled decisions into a reviewable and approved PRD"
  default_prompt: "Use $to-prd to turn the settled decision record into a draft PRD, ask for approval, and mark it approved before task planning."
policy:
  allow_implicit_invocation: false
