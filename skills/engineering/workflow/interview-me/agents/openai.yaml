interface:
  display_name: "Interview Me"
  short_description: "Ask exactly five independent questions when five exist, otherwise fewer"
  default_prompt: "Use $interview-me to settle this request into a decision record. Ask exactly five independent questions when five exist, otherwise fewer, never more, and do not request extra model opinions."
policy:
  allow_implicit_invocation: false
