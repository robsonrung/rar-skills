# Codex agent interface metadata; consumed by the Codex host, not read at runtime by the model.
interface:
  display_name: "Fable Mindset"
  short_description: "Apply the five-moment engineering posture; it does not select a model"
  default_prompt: "Use $fable-mindset to apply the working posture across intake, diagnosis, decision, implementation, and reporting. This is a posture skill, not a Fable model route."
policy:
  allow_implicit_invocation: false
