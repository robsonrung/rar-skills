# Codex-host interface/registration config for this skill (display metadata and
# default prompt shown in the Codex app). Not meant to be read at runtime.
interface:
  display_name: "Council"
  short_description: "Clarify, compare, plan, then execute"
  default_prompt: "Use $council to clarify this task, compare the four model seats, and propose a plan."
